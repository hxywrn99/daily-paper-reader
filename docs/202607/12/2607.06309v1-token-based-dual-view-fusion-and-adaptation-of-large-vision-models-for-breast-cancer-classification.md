---
title: Token-Based Dual-view Fusion and Adaptation of Large Vision Models for Breast Cancer Classification
title_zh: 基于令牌的双视图融合与大型视觉模型自适应用于乳腺癌分类
authors: "Aysan Ghayouri Pirsoltan, Shima Babakordi, Mohammad Reza Mohammadi"
date: 2026-07-07
pdf: "https://arxiv.org/pdf/2607.06309v1"
tags: ["query:image-fusion"]
score: 9.0
evidence: 基于令牌的双视图融合用于乳腺癌分类，使用深度学习
tldr: "乳腺X线乳腺癌分类需融合CC和MLO两视图信息，现有方法因特征纠缠和交互深度不足导致性能受限。本文提出基于冻结ViT的token双视图学习框架，通过专用融合token在多个Transformer深度进行交叉注意力交互，实现渐进式跨视图信息融合而不破坏视图特有结构。在VinDr-Mammo数据集上BI-RADS分类F1达50.40%、AUC达0.8090，二分类AUC提升0.10，消融实验验证了token融合与多深度交互的有效性。该框架为多视图医学图像分析提供了新的范式。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.06309v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1707, \"height\": 1124, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06309v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 819, \"height\": 753, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06309v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 814, \"height\": 752, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06309v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1607, \"height\": 733, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.06309v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1618, \"height\": 1058, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.06309v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1570, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06309v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1361, \"height\": 309, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06309v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1014, \"height\": 228, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06309v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1164, \"height\": 239, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06309v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 773, \"height\": 222, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.06309v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 776, \"height\": 220, \"label\": \"Table\"}]"
motivation: 现有双视图融合方法在单层进行特征聚合或交叉注意力，导致视图特异与共享表示纠缠，且交互深度受限。
method: 提出token中心双视图框架：在冻结ViT中插入融合token，通过多个Transformer深度的交叉注意力进行渐进式跨视图信息交换。
result: "在VinDr-Mammo上BI-RADS分类F1=50.40%、AUC=0.8090，二分类AUC相比基线提升0.10。"
conclusion: 多深度token融合有效解耦视图信息并增强交互，为多视图医学图像分析提供了高效可扩展的解决方案。
---

## 摘要
通过乳腺X线摄影进行准确的乳腺癌分类需要有效整合头尾位（CC）和内外斜位（MLO）视图的互补信息，这些视图能更完整地描述乳腺异常。然而，现有的多视图学习方法通常依赖于特征级聚合或单阶段交叉注意力，这可能会纠缠视图特定表示和共享表示，并将交互限制在有限的网络深度。为解决这些限制，我们提出了一种以令牌为中心的双视图学习框架，该框架在冻结的视觉Transformer骨干内统一了基于提示的自适应和跨视图融合。该框架将视图间交互重构为结构化的令牌级通信，其中专用融合令牌通过交叉注意力显式编码CC和MLO视图之间的双向信息交换，作为跨视图依赖关系的中间载体，而非依赖直接特征融合。与在单层应用融合的传统方法不同，融合模块插入多个Transformer深度，使得跨编码器层次结构进行渐进和重复的交互。融合令牌被重新整合到令牌序列中，并由后续Transformer层细化，从而在保留视图特定结构的同时促进互补信息的层次化传播。在VinDr-Mammo和CMMD数据集上的实验表明，该方法在线性探测、仅提示自适应和传统融合基线基础上持续改进。在VinDr-Mammo BI-RADS分类任务中，该框架实现了50.40%的F1分数和0.8090的AUC，其中在二分类设置中比双视图融合基线提高了0.10的AUC。消融研究进一步验证了基于令牌的融合和多深度交互设计的有效性。

## Abstract
Accurate breast cancer classification from mammography requires effective integration of complementary information from craniocaudal (CC) and mediolateral oblique (MLO) views, which provide a more complete characterization of breast abnormalities. However, existing multi-view learning approaches typically rely on feature-level aggregation or single-stage cross-attention, which can entangle view-specific and shared representations and restrict interaction to limited network depths. To address these limitations, we propose a token-centric dual-view learning framework that unifies prompt-based adaptation and cross-view fusion within a frozen vision transformer backbone. The framework reformulates inter-view interaction as structured token-level communication, where dedicated fusion tokens explicitly encode bidirectional information exchange between CC and MLO views via cross-attention, serving as intermediate carriers of cross-view dependencies rather than relying on direct feature fusion. Unlike conventional methods that apply fusion at a single layer, fusion modules are inserted at multiple transformer depths, enabling progressive and repeated interaction across the encoder hierarchy. Fusion tokens are reintegrated into the token sequence and refined by subsequent transformer layers, facilitating hierarchical propagation of complementary information while preserving view-specific structure. Experiments on VinDr-Mammo and CMMD datasets demonstrate consistent improvements over linear probing, prompt-only adaptation, and conventional fusion baselines. On the VinDr-Mammo BI-RADS classification task, the framework achieves 50.40% F1-score and 0.8090 AUC, including a 0.10 AUC improvement over a dual-view fusion baseline in the binary setting. Ablation studies further validate the effectiveness of token-based fusion and multi-depth interaction design.

---

## 论文详细总结（自动生成）

# 论文总结：Token-Based Dual-view Fusion and Adaptation of Large Vision Models for Breast Cancer Classification

## 1. 核心问题与整体含义（研究动机和背景）
- **任务**：乳腺X线摄影乳腺癌分类，需融合头尾位（CC）和内外斜位（MLO）两个视图的互补信息。
- **现有方法局限**：
  - 传统多视图方法（早期融合、中间融合、晚期融合）通常只在网络单层进行特征聚合，无法捕捉不同表示层次间的互补关系。
  - 基于Transformer的交叉注意力方法常将交叉注意力输出直接残差加到视图特征中，导致**视图特有信息**与**跨视图共享信息**纠缠，削弱了跨视图依赖的显式建模。
  - 大多数方法未充分利用现代视觉Transformer的迁移能力，且参数效率低。
- **研究动机**：设计一种统一框架，在冻结的视觉Transformer骨干内，通过**结构化令牌级通信**实现参数高效的自适应和跨视图融合，支持**多深度、渐进式**的交互，同时解耦视图特有与共享表示。

## 2. 方法论：核心思想、关键技术细节、流程
- **整体架构**：两阶段训练，基于冻结的MedSigLIP视觉编码器。

### 阶段1：深度共享提示学习（Deep Shared-View Prompt Adaptation）
- **思想**：借鉴VPT-Deep和MaPLe，将一组可学习提示令牌注入多个Transformer层，使CC和MLO视图共享同一套提示，促进视图间表示一致性。
- **关键技术**：
  - 在Transformer前 \(J\) 层每层插入一个提示令牌 \(\tilde{p}_i\)。
  - 提示令牌与当前层输入拼接：\(H_i = V_i([\tilde{p}_i, H_{i-1}])\)。
  - 超出提示深度 \(J\) 的层正常前向。
  - 仅更新提示令牌和线性分类头，骨干冻结。
- **输出**：各自视图经多注意力池化得到紧凑嵌入 \(z_{cc}, z_{mlo}\)，拼接后通过线性分类器。

### 阶段2：基于令牌的跨视图融合（Token-Based Cross-View Fusion）
- **核心思想**：引入专用**融合令牌**，通过双向交叉注意力将一视图信息摘要为紧凑令牌，插入到另一视图的令牌序列中，由后续Transformer层进一步细化。
- **关键技术**：
  - 在选定Transformer层插入**跨视图融合模块**。
  - 计算双向交叉注意力：
    \[
    A_{cc} = \text{MHA}(H_{cc}, H_{mlo}, H_{mlo}), \quad A_{mlo} = \text{MHA}(H_{mlo}, H_{cc}, H_{cc})
    \]
  - 对交叉注意力输出进行**均值池化**得到融合令牌：
    \[
    t_{cc} = \frac{1}{N}\sum_{i=1}^N A_{cc}^{(i)}, \quad t_{mlo} = \frac{1}{N}\sum_{i=1}^N A_{mlo}^{(i)}
    \]
  - 融合令牌插入原序列：\(H'_{cc} = [t_{cc}, H_{cc}],\; H'_{mlo} = [t_{mlo}, H_{mlo}]\)。
  - 可插入多个深度实现层次化交互。
- **训练**：阶段1学到的提示冻结，仅更新融合模块、注意力池化头和分类器。

## 3. 实验设计
- **数据集与场景**：
  - **VinDr-Mammo**：5000例四视图检查，BI-RADS标注。使用5类（BI-RADS 1-5）和二分类（Suspicious Benign vs Malignancy）。
  - **CMMD**：5202张图像，病理确诊。二分类（良性/恶性）。
- **预处理**：裁剪乳房区域 + CLAHE增强 + resize至448×448。
- **基线方法**：
  - Linear Probe（Avg Fusion / Max Fusion）：冻结编码器，仅训练线性分类器，后聚合两视图预测概率。
  - Stage 1 Only（仅深度共享提示学习）。
  - Stage 2 Only（仅交叉融合，无阶段1提示初始化）。
  - Full Framework（两阶段联合）。
  - **DIVF**[20]：现有双视图特征融合方法（仅在二分类上对比）。
- **评价指标**：宏平均F1-score、AUC-ROC。
- **公平性**：所有方法相同骨干、预处理、训练协议、评价指标。

## 4. 资源与算力
- **GPU**：单张NVIDIA RTX 4090。
- **训练配置**：
  - 阶段1：15 epoch，AdamW，lr=1e-3，weight decay=0.01，batch size=8，cosine学习率调度+100步线性warmup。
  - 阶段2：相同优化配置。
- **未明确说明**：总训练时间、参数量、推理速度等细节。

## 5. 实验数量与充分性
- **总体实验组数**：至少10组以上，涵盖：
  - 两个数据集（CMMD二分类、VinDr-Mammo五分类/二分类）。
  - 多个基线对比（LP-Avg、LP-Max、Stage1、Stage2、Full、DIVF）。
  - 多项消融实验：
    - 融合策略（残差融合 vs 融合令牌）
    - 融合令牌聚合方式（max/mean/attention pooling）
    - 编码器头微调（不同学习率）
    - 提示深度（5种配置）
    - 融合块数量（1/2/4个）
- **充分性与公平性**：
  - 消融实验覆盖核心设计选择，结果在多个设置下一致。
  - 所有对比使用相同骨干和预处理，公平。
  - 使用病人级划分防止信息泄露，采用不平衡采样处理类别不均衡。
  - **不足**：未与更前沿的AGN、MaMVT、TransReg等方法比较，仅对比DIVF（单基线）；未在更大规模外部数据集上验证。

## 6. 主要结论与发现
- **性能表现**：
  - VinDr-Mammo五分类：F1=50.40%，AUC=0.8090，显著优于LP（38.95% F1, 0.7548 AUC）。
  - CMMD二分类：F1=64.96%，AUC=0.7161，优于LP（57.57% F1, 0.7045 AUC）。
  - 与DIVF对比：二分类AUC从0.7486提升至0.8593（+0.11），F1从75.98%提升至79.57%。
- **组件贡献**：
  - 融合令牌策略优于残差融合（VinDr-5类F1提升5.22%）。
  - 共享提示学习和交叉融合互补，联合训练最佳。
  - 最佳提示深度为12层；2个融合块（位于12和23层）效果最好，过多融合块（4个）反而下降。
  - 编码器头微调（lr=1e-5）可进一步提升性能。
- **定性分析**：遮挡灵敏度图显示模型聚焦于关键解剖区域。

## 7. 优点
- **方法创新**：
  - 首次将提示学习和令牌融合统一于冻结ViT框架，参数高效（仅更新极少参数）。
  - 融合令牌作为独立载体显式保留跨视图信息，避免与视图特有信息纠缠。
  - 多深度交互设计实现渐进式层次化融合，而非单层操作。
- **实验扎实**：
  - 在多个数据集和任务上验证，消融覆盖关键设计选择。
  - 与线性探测、仅适应、仅融合等多种基线对比，公平且结论一致。
  - 代码开源，可复现性强。
- **实用性**：冻结骨干降低计算成本，适用于低资源场景；可扩展到其他多视图医学图像。

## 8. 不足与局限
- **实验覆盖不足**：
  - 仅使用MedSigLIP一个视觉基础模型，未验证在CLIP、BiomedCLIP等骨干上的迁移性。
  - 缺少与最先进的图方法（AGN）、多实例学习（MIL）、其他Transformer方法（MaMVT、TransCorNet）的对比。
  - 未在更大型数据集（如CBIS-DDSM）或外部测试集上验证泛化性。
- **偏差风险**：
  - 数据集类别不平衡（VinDr-Mammo中BI-RADS 1/2占多数），虽使用不平衡采样，但五分类上BI-RADS 4、5类准确率仍很低（混淆矩阵显示）。
  - 仅使用两个公开数据集，可能受地域、设备、标注标准等偏差影响。
- **应用限制**：
  - 两阶段训练流程稍复杂，需要先训练提示再训练融合模块。
  - 未讨论推理速度或参数量细节，实际部署效率未评估。
  - 仅处理CC和MLO两个视图，未扩展到侧位（MLO通常包含更多信息但未使用侧位比较）。
- **其他**：
  - 消融实验中“融合块数量”仅在二分类任务上调参，未在五分类上深入分析。
  - 未进行统计显著性检验（如p值或置信区间）。

（完）
