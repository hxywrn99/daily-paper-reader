---
title: "CRISP: Constrained Refinement via Iterative Squeezing Process for Robust Medical Image Segmentation under Domain Shift"
title_zh: CRISP：通过迭代挤压过程实现域偏移下鲁棒医学图像分割的约束细化
authors: "Yizhou Fang, Pujin Cheng, Yixiang Liu, Xiaoying Tang, Longxi Zhou"
date: 2026-07-16
pdf: "https://arxiv.org/pdf/2607.15231v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 通过迭代挤压过程实现域偏移下鲁棒的医学图像分割
tldr: "医学图像分割中分布偏移导致性能下降，现有域适应方法无法应对真实世界无限偏移。提出CRISP框架，基于正区域秩稳定性假设，通过概率排序稳定性推导鲁棒空间先验，利用潜在特征扰动定义高精度核心和高召回支持，并迭代训练逐步逼近分割。在多中心心脏MRI和肺血管CT上，HD95分别降低0.14(7.0%)、1.90(13.1%)和8.39(38.9%)像素，显著优于现有方法。无需测试时参数更新和目标域数据，即插即用，提升了医学AI的临床鲁棒性。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.15231v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1835, \"height\": 1048, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15231v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1830, \"height\": 637, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.15231v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 872, \"height\": 326, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.15231v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1833, \"height\": 640, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15231v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1827, \"height\": 477, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.15231v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 870, \"height\": 289, \"label\": \"Table\"}]"
motivation: 现有域适应方法受限于预定义模拟和伪标签，无法应对真实世界无限分布偏移，需无需目标数据且不更新参数的鲁棒分割方法。
method: 基于正区域秩稳定性假设，通过潜在特征扰动提取高精度核心和高召回支持先验，再迭代训练逐步压缩至最终分割。
result: "在多中心心脏MRI和肺血管CT分割中，HD95最多降低38.9%，远超SOTA方法。"
conclusion: CRISP作为即插即用框架，无需目标域数据，显著提升跨域分割鲁棒性，促进医学AI临床落地。
---

## 摘要
医学图像中的分布偏移仍然是医学AI临床转化的核心瓶颈。未能解决该问题会导致在未见环境中性能严重下降并加剧健康不平等。现有的域适应方法本质上受限于通过模拟偏移或伪监督来穷尽预定义可能性，这样的策略在开放且不可预测的真实世界中（分布偏移实际上无限）难以奏效。为应对这一挑战，我们采用“正区域的秩稳定性”作为分布偏移下的工作假设，并利用该假设为纯源域分割推导出鲁棒的空间线索。在此假设指导下，我们提出了CRISP，一个与模型无关的框架，与部署时自适应不同，它不需要测试时参数更新，也不需要目标域数据——一个无目标、即插即用的细化框架，以冻结权重进行分割。CRISP不是直接使用排序输出掩码，而是利用分布偏移下概率排序的稳定性来推导鲁棒的空间先验。通过潜在特征扰动，扰动不变的高等级区域定义了一个高精度（HP）核心，而在至少一次扰动下仍可能为前景的体素则定义了一个高召回率（HR）支撑集；然后在扰动下递归地细化这些双重先验。我们设计了一个迭代训练框架，逐步将HP和HR向最终分割挤压。在多中心心脏MRI和基于CT的肺血管分割上的广泛评估表明，CRISP具有卓越的鲁棒性，在多中心、人口统计和模态偏移上，分别显著优于最先进的方法，HD95像素降低高达0.14（改善7.0%）、1.90（改善13.1%）和8.39（改善38.9%）。

## Abstract
Distribution shift in medical imaging remains a central bottleneck for the clinical translation of medical AI. Failure to address it can lead to severe performance degradation in unseen environments and exacerbate health inequities. Existing methods for domain adaptation are inherently limited by exhausting predefined possibilities through simulated shifts or pseudo-supervision. Such strategies struggle in the open-ended and unpredictable real world, where distribution shifts are effectively infinite. To address this challenge, we adopt the "Rank Stability of Positive Regions" as a working assumption under distribution shift, and use it to derive robust spatial hints for source-only segmentation. Guided by this assumption, we propose CRISP, a model-agnostic framework that, unlike deployment-time adaptation, requires no test-time parameter updates and no target-domain data--a target-free, plug-in refinement framework that segments with frozen weights. Rather than using ranking to directly output masks, CRISP exploits the stability of probability rankings under distribution shift to derive robust spatial priors. Via latent feature perturbation, perturbation-invariant high-grade regions define a high-precision (HP) core, while voxels that remain potentially foreground under at least one perturbation define a high-recall (HR) support; these dual priors are then recursively refined under perturbation. We then design an iterative training framework that progressively squeezes HP and HR toward the final segmentation. Extensive evaluations on multi-center cardiac MRI and CT-based lung vessel segmentation demonstrate CRISP's superior robustness, significantly outperforming state-of-the-art methods with striking HD95 reductions of up to 0.14 (7.0% improvement), 1.90 (13.1% improvement), and 8.39 (38.9% improvement) pixels across multi-center, demographic, and modality shifts, respectively.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 核心问题与整体含义（研究动机与背景）

- **核心问题**：医学图像分割中的**分布偏移**（domain shift）导致模型在未见过的临床环境下性能严重下降，阻碍了医学AI的临床转化。现有方法分为两类：
  - **训练时泛化**（如FedDG、DDG-Med）：试图学习不变表示，但受限于预定义的模拟偏移空间。
  - **部署时适应**（如IPLC、TEGDA）：需要目标域数据动态更新参数，但面对开放无限的偏移仍显脆弱，且可能发生负适应。
- **整体含义**：本文旨在提出一种**无需目标域数据、无需测试时参数更新**的源域泛化分割框架，利用“概率排序在偏移下保持稳定”这一假设，从根本上绕过对偏移的显式建模，更鲁棒地应对真实世界中的无限分布偏移。

## 2. 方法论：核心思想、关键技术细节与算法流程

### 核心思想
- **“正区域秩稳定性”假设**：在分布偏移下，尽管绝对概率值可能大幅变化（如从0.9降至0.4），但正预测区域的**相对排序顺序**基本保持不变。因此，利用排序稳定性而非绝对概率值来获取鲁棒的空间线索。
- 不直接输出分割掩码，而是通过潜在特征扰动获得**高精度（HP）核心**和**高召回率（HR）支撑集**，并递归地将二者向真实分割挤压。

### 关键技术细节
1. **潜在特征扰动模拟域偏移**：
   - 在瓶颈特征上注入高斯噪声（N=10次，σ=0.35），模拟邻域域间的偏移。
   - 对每个扰动后的概率图进行log-odds变换并量化成L=5个等级，得到多个随机等级图。
   - **HP核心**：所有扰动下均达到最高等级的体素之交（交集）。
   - **HR支撑集**：任一次扰动下为非背景的体素之并（并集）。
   - 两者之差即为不确定区域Ω_u（即为需要细化的边界）。

2. **递归自进化训练框架**：
   - 初始模型f_θ1从平凡线索（HP=0，HR=全图）开始训练。
   - 冻结当前模型后，通过扰动和排序机制生成深度d的HP/HR线索（深度越大，线索越紧致）。
   - 下一代模型f_θk从上一代模型微调，并在混合深度（0～k-1）的平衡数据集上训练，学会处理从粗到细的不同线索。
   - 训练中引入**不确定性挤压损失**：仅在不确定区域Ω_u内惩罚预测与扰动预测的不一致性，迫使模型在该区域做出确定且稳定的预测。
   - 最终在测试时只部署最后一个模型f_θK，通过相同的扰动和递归流程（约3步）从平凡线索逐步收敛到最终分割。

### 算法流程文字描述
```
输入：源域图像切片X_sl 及初始线索（M_HP=0, M_HR=全图）
循环（每次迭代k）：
  编码器提取特征F
  施加N个高斯扰动，得到扰动特征F^(n)
  解码并计算概率图，变换为等级图
  计算M_HP（交集）和M_HR（并集）
  将当前线索与图像拼接，输入网络预测
  使用不确定性挤压损失（仅在Ω_u内）训练当前模型
  冻结模型，生成更深度的线索供下一代使用
直至HP与HR收敛（大约3代）
输出：最终分割掩码
```

## 3. 实验设计：数据集、场景与对比方法

### 数据集与场景
| 场景类型 | 数据集 | 源域 | 目标域 | 结构/类别 |
|---------|-------|------|--------|-----------|
| **多中心偏移** | M&Ms心脏MRI | 供应商A（Siemens） | 供应商B（Philips）、C（GE）、D（Canon） | LV、MYO、RV |
| **模态偏移** | CT肺血管（私有） | 增强CT（93例） | 非增强CT（101例） | 肺血管 |
| **人口统计偏移** | CT肺血管（私有） | 正常队列（98例） | COVID队列（74例） | 肺血管 |

### 对比方法
- **源域泛化方法（⋆）**：FedDG、DDG-Med
- **部署时适应方法（#）**：IPLC（测试时伪标签）、TEGDA（测试时动态适应）
- **基线**：直接在目标域测试的源模型（Baseline）
- **参考**：A+T Ref.（源域+一个标注目标域联合训练，非公平对比，仅作上界参考）

### 实现细节
- 骨干网络：DeepLabv3+ 搭配 MobileNetV2。
- 训练：Adam优化器，学习率0.001，迭代60,000次，批次大小10（M&Ms）/ 35（CT）。
- CRISP超参数：N=10，σ=0.35，L=5，α=0.5，K=3。

## 4. 资源与算力

- **GPU**：单块NVIDIA TITAN RTX（24GB显存）。
- **训练迭代**：每个模型训练60,000次迭代。
- **训练时长**：未明确给出具体小时数，但提到在单GPU上完成。
- **推理开销**：每次测试约需6次前向传播（约3步，每步1次预测+1次HP/HR更新），在可接受范围内换取鲁棒性提升。

## 5. 实验数量与充分性

- **完整实验分组**：
  - 主对比实验（Table 1）：M&Ms上的3个目标域，报告Dice和HD95，包含9个类域组合。
  - 极端偏移实验（Table 2）：模态偏移和人口统计偏移，对比基线、IPLC、TEGDA。
  - 消融实验（Table 3）：分离递归自进化（RSE）和不确定性挤压损失，在三个场景上的冰状结果。
  - 收敛分析（Figure 3）：显示Dice/HD95随生成世代变化。
  - 最坏情况分析：非增强CT上的全病例Dice分布，检验尾部性能。
  - 定性对比（Figure 2）：展示模态偏移和人口统计偏移下的分割效果。

- **充分性与公平性**：
  - 对比方法均为原文推荐超参数，报告均值±标准差（病例层面）。
  - 明确区分源域方法（⋆）和目标域方法（#），并强调两者不等价，但不影响结论（CRISP作为⋆仍超越#）。
  - 消融实验完整，验证两个组件互补。
  - 但私有CT数据集未公开，可能影响复现；所有实验仅一个随机种子（虽说明可复现）。

## 6. 主要结论与发现

- CRISP在所有三个分布偏移场景下**显著优于当前最先进方法**，且**无需目标域数据**：
  - 多中心偏移：HD95降低0.14（7.0%）、1.90（13.1%）、8.39（38.9%）像素分别为多中心、人口统计、模态偏移。
  - 极端偏移（非增强CT）：Dice从基线33.26提升至59.46，HD95从29.97降至13.19。
  - 人口统计偏移：Dice从65.15提升至70.61，HD95从17.12降至12.57。
- **超越目标自适应方法**：CRISP在多数指标上优于IPLC和TEGDA，且不会发生负适应（IPLC在COVID队列上反而下降）。
- **提升最坏情况**：非增强CT中，基线36.4%病例Dice<0.3，CRISP完全消除该比例；6.5%病例仍低于0.5，但大幅减少灾难性失败。
- **组件贡献**：RSE负责提升整体区域重叠，挤压损失负责收紧边界，两者叠加效果最佳。

## 7. 优点

- **创新性**：首次将“秩稳定性”假设系统化应用于医学图像分割的域泛化，替代常用的绝对概率依赖，理论新颖且直观合理。
- **实际性**：模型无关、即插即用、纯源域、无需测试时参数更新，非常适合临床部署。
- **鲁棒性**：在严重模态偏移下表现出色，且能消除长尾失败，这是临床风险的核心。
- **设计巧妙**：通过潜在特征扰动模拟偏移，用交集/并集提取稳定先验，再通过递归挤压和不确定性损失逐步收敛，流程清晰优雅。
- **消融完整**：验证了两个关键组件各自贡献，且展示收敛速度。

## 8. 不足与局限

- **推理开销**：需要多次前向传播（约6倍单次），在实时性要求极高的场景中可能受限。
- **扰动依赖**：仅使用高斯噪声模拟偏移，虽被实验证明有效，但可能无法覆盖所有真实偏移类型（如解剖结构变异）。
- **需要多代训练**：递归自进化需要训练多个模型（文中3代），增加了离线训练成本，但推理时只需最终一个模型。
- **泛化验证范围**：仅在心脏MRI（三个结构）和肺血管CT（二值分割）上验证，未在更多解剖部位、更多类别或MRI/CT混合任务上测试。
- **缺乏理论保证**：“秩稳定性”仅作为工作假设，缺乏严格理论推导其适用边界（例如当偏移极大时排序是否仍稳定）。文中只提供了部分经验证据。
- **数据集局限性**：私有CT数据集未公开，复现困难；公开M&Ms验证了多中心偏移，但源域仅一个，未做交叉验证。
- **未讨论失败案例**：虽总结了最坏情况提升，但未分析CRISP在哪些具体病例上仍有明显错误。

（完）
