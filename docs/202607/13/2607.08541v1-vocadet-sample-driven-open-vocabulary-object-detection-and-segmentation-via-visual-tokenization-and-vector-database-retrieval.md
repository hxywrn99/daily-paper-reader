---
title: "VocaDet: Sample-Driven Open-Vocabulary Object Detection and Segmentation via Visual Tokenization and Vector Database Retrieval"
title_zh: "VocaDet: 基于视觉标记与向量数据库检索的样本驱动开放词汇目标检测与分割"
authors: ZhiXin Sun
date: 2026-07-09
pdf: "https://arxiv.org/pdf/2607.08541v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 基于样本驱动的开放词汇分割，使用深度学习方法
tldr: 现有开放词汇目标检测与分割方法依赖文本提示或有限视觉样例，难以扩展至大规模持续增长的目标库。针对此问题，提出VocaDet框架，将连续视觉表示通过层次聚类离散化为多粒度视觉词汇，结合位置去偏和空间拓扑信息存储到向量数据库，无需模型重训练即可实现样本驱动的识别。在UA-DETRAC数据集上，VocaDet无需传统检测器训练即达到有效检测性能，并支持随正负样本积累而持续扩展的识别能力。该方法为开放词汇检测提供了可扩展的新范式。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.08541v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1451, \"height\": 433, \"label\": \"Figure\"}]"
motivation: 现有方法难以处理大规模持续扩展的目标库，且依赖文本或特征匹配，扩展性差。
method: 使用DINOv3提取特征，通过自适应层次聚类生成多粒度视觉词汇，结合位置去偏和空间拓扑存入向量数据库，推理时匹配检索实现检测分割。
result: 在UA-DETRAC数据集上，无需传统检测器训练即实现有效检测，且识别能力随样本积累持续扩展。
conclusion: VocaDet提供样本驱动、无需重训练的开放词汇检测框架，通过视觉词汇化与向量数据库实现高效可扩展识别。
---

## 摘要
开放词汇目标检测与分割旨在识别预定义类别之外的任意目标。尽管最近的视觉-语言和基于参考的方法显著推进了该领域，但它们通常依赖文本提示、有限的视觉示例或昂贵的特征匹配过程，使其难以扩展到大型且不断扩展的目标库。在本文中，我们提出VocaDet，一个样本驱动的开放词汇目标检测与分割框架，该框架直接从用户提供的正负样本集合中学习目标概念，无需模型重训练。关键思想是将连续的视觉表示转换为离散的视觉词汇，并通过可扩展的向量数据库执行高效的基于检索的识别。具体而言，我们采用DINOv3作为视觉特征提取器，并应用具有自适应聚类敏感性的凝聚聚类生成多粒度视觉标记。这些视觉标记与去偏的位置表示和空间拓扑信息一起作为可扩展的目标记忆存储在向量数据库中。在推理时，查询图像转换为视觉标记，并与存储的目标记忆高效匹配以进行目标定位和分割。此外，引入了背景过滤机制以移除频繁出现的背景模式，并在实际固定相机场景中减少冗余检索操作。在UA-DETRAC数据集上的实验表明，VocaDet无需传统检测器训练即可实现有效的开放词汇检测性能，同时随着正负样本的积累，支持持续可扩展的识别能力。

## Abstract
Open-vocabulary object detection and segmentation aim to recognize arbitrary objects beyond predefined categories. Although recent vision-language and reference-based approaches have significantly advanced this field, they often rely on text prompts, limited visual examples, or expensive feature matching procedures, making them difficult to scale to large and continuously expanding object repositories. In this work, we propose VocaDet, a sample-driven open-vocabulary object detection and segmentation framework that learns object concepts directly from user-provided positive and negative sample collections without model retraining. The key idea is to transform continuous visual representations into discrete visual vocabularies and perform efficient retrieval-based recognition through a scalable vector database. Specifically, we employ DINOv3 as the visual feature extractor and apply agglomerative clustering with adaptive clustering sensitivity to generate multi-granularity visual tokens. These visual tokens, together with position-debiased representations and spatial topology information, are stored as expandable object memories in a vector database. During inference, query images are converted into visual tokens and efficiently matched against the stored object memories for object localization and segmentation. Furthermore, a background filtering mechanism is introduced to remove frequently occurring background patterns and reduce redundant retrieval operations in practical fixed-camera scenarios. Experiments on the UA-DETRAC dataset demonstrate that VocaDet achieves effective open-vocabulary detection performance without conventional detector training, while supporting continuously expandable recognition capability as additional positive and negative samples are accumulated.

---

## 论文详细总结（自动生成）

# VocaDet 论文详细总结

## 1. 核心问题与整体含义（研究动机和背景）

- **研究动机**：现有开放词汇目标检测与分割方法（如基于文本提示的 Grounding DINO、基于视觉提示的 T-Rex2、训练无关的参考图像分割方法 Training-free 等）在扩展至大规模、持续增长的目标库时存在瓶颈：它们依赖文本提示、有限视觉样例或重复的特征匹配，难以高效处理用户提供的大规模正负样本集合。
- **核心问题**：如何从用户不断累积的正负样本中自动学习目标概念，并在任意图像中实现高效、准确的检测与分割，而无需模型重训练？
- **整体含义**：提出一种样本驱动的全新范式——通过视觉词汇化（Visual Tokenization）将连续特征离散化为多粒度视觉单词，并利用可扩展的向量数据库进行检索，从而将开放词汇检测转化为检索问题，实现识别能力的持续扩展。

## 2. 方法论

### 核心思想
将连续的视觉特征通过层次聚类离散化为“视觉单词”，结合位置去偏和空间拓扑信息，构建可扩展的目标记忆向量数据库；推理时，查询图像经同样流程转化为视觉单词，在向量数据库中检索匹配，输出检测结果。

### 关键技术细节
1. **特征提取**：使用 DINOv3（ViT 架构）提取密集语义特征。
2. **视觉词汇构建**：
   - 对 DINOv3 特征应用**凝聚聚类**（Agglomerative Clustering），并通过**聚类敏感性参数**控制词汇粒度，生成多级粒度视觉单词。
   - 利用空间连通性将相同视觉模式但空间不连通的区域拆分为独立实例。
3. **位置去偏**：采用 INSID3 中的策略，将原始 DINOv3 特征转换为位置去偏表示，减少空间位置对匹配的影响。
4. **向量数据库存储**：
   - 正样本（目标）和负样本（背景）的视觉单词特征存入 Milvus 等向量数据库。
   - 对于同一目标内多个空间相连的视觉单词，记录其拓扑邻接关系（indexList），匹配时要求同时满足特征相似性和拓扑一致性。
   - 为不同聚类敏感性分别建立独立的向量数据库。
5. **推理流程**：
   - 输入图像 → DINOv3 提取特征 → 凝聚聚类生成视觉单词 → **背景过滤层**移除背景单词 → 剩余单词与向量数据库匹配 → 匹配的视觉簇转换为候选框 → **非极大值抑制（NMS）**合并重叠框，输出最终检测结果。
6. **背景过滤机制**：
   - 维护一个背景特征内存，记录代表性背景单词。
   - 推理时先对比背景内存，相似度超阈值则视为背景并排除。
   - 内存在线更新：根据当前帧匹配结果，保留匹配的背景向量，移除未匹配的，并将新确认的背景单词加入。
7. **多粒度融合**：不同聚类敏感性下的检测结果通过 NMS 合并，得到最终一致框。

## 3. 实验设计

- **数据集**：UA-DETRAC（车辆检测基准，包含复杂交通场景）。
- **评测协议**：遵循原数据集的标准多目标检测评估指标（如 mAP）。
- **对比方法**：未明确列举对比基线，但指出相比传统固定类别检测器（如 YOLO 系列），VocaDet 无需训练即可工作，且性能随样本扩充而提升。
- **定量结果**：论文仅用文字描述“VocaDet 达到了具有竞争力的检测性能”，未提供具体数值表格或与其他方法的直接对比。

## 4. 资源与算力

- **文中未明确说明**使用的 GPU 型号、数量、训练时长等。仅提及使用 DINOv3 作为预训练特征提取器（冻结），无需传统检测器训练，因此计算开销主要在特征提取和聚类/检索。向量数据库（Milvus）用于高效检索。无模型训练过程，资源需求相对较低。

## 5. 实验数量与充分性

- **实验数量**：单数据集（UA-DETRAC）上的评估，未进行多数据集的跨域泛化验证或消融实验。
- **充分性**：实验覆盖不足：
  - 缺少定量指标（如 mAP、AR）和对比其他 SOTA 方法的数值。
  - 未进行消融研究（如背景过滤、拓扑约束、多粒度策略的贡献）。
  - 仅一个数据集，且为固定场景（交通），缺乏对通用开放词汇场景的验证。
  - 对于冷启动问题和多目标合并失败的定性观察未提供定量分析。
- **公平性**：未说明实验设置细节（如正负样本数量、聚类敏感性参数取值、阈值设定），难以判断实验的客观性和可重复性。

## 6. 主要结论与发现

1. **可行性**：VocaDet 通过样本驱动的视觉词汇化与向量数据库检索，在无传统检测器训练的情况下，实现了有效的开放词汇目标检测。
2. **可扩展性**：随着用户积累更多正负样本，向量数据库中的目标记忆更全面，检测性能持续提升。
3. **局限性**：当多个外观相似的同类目标空间相邻时，容易被错误合并为一个检测实例（缺乏实例级判别信息）。
4. **效率**：背景过滤机制在固定相机场景下有效减少冗余查询，提升推理速度。

## 7. 优点

- **创新性**：提出一种新颖的样本驱动范式，将开放词汇检测转化为视觉词汇检索，摆脱了对文本提示和模型重训练的依赖。
- **可扩展性**：利用向量数据库实现记忆的动态扩充，识别能力可随样本积累线性增长。
- **无训练成本**：所有 DINOv3 特征提取器冻结，无需梯度计算和参数更新，部署友好。
- **多粒度与拓扑感知**：自适应聚类生成多级视觉单词，并结合空间拓扑约束提升匹配准确性，有助于处理部分遮挡或变形。
- **背景过滤**：在线更新背景记忆，降低实际场景中的计算冗余。

## 8. 不足与局限

- **实验不充分**：仅一个数据集，无定量对比、无消融实验、无具体性能数值，论证力度弱。
- **冷启动问题**：初始阶段正负样本少时，目标概念表征不准确，检索易错。
- **实例区分困难**：对于相似外观的相邻目标，缺乏实例级别分割能力（如 SAM2 的掩码生成），易合并错误。
- **依赖聚类参数**：聚类敏感性参数需手动设定，不同场景可能需调优，泛化性未验证。
- **缺少与现有方法的直接对比**：文中提及的 T-Rex2、Grounding DINO、INSID3 等方法未在相同条件下比较，难以评估实际优势。
- **范围局限**：仅在交通场景（UA-DETRAC）测试，未涵盖室内、通用目标等更开放场景。
- **资源细节缺失**：未说明推理速度、数据库规模、检索耗时等实际效率指标。

（完）
