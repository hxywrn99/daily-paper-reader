---
title: "MIS-HCC: Hierarchical Channel Clustering for Efficient Medical Image Segmentation"
title_zh: MIS-HCC：面向高效医学图像分割的层次化通道聚类方法
authors: "Bo Zhao, Haoran Yu, Lifei Liu, Zongcheng Chu, Yining Liu, Chang Liu, Szu-Yu Chen, Zequn Xie"
date: 2026-07-19
pdf: "https://arxiv.org/pdf/2607.17329v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 医学图像分割，使用层次通道聚类压缩
tldr: 医学图像分割模型需兼顾高精度与轻量化，但现有剪枝方法忽略通道内在关联，导致压缩模型丢失基础特征。本文提出MIS-HCC，利用层次聚类和Wasserstein距离度量通道相似性，融合相似通道参数实现高效压缩。在三个医学数据集上，该方法在精度和压缩效率上均超越现有方法，为资源受限平台部署提供了有效方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.17329v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1811, \"height\": 815, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.17329v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1599, \"height\": 1004, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.17329v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1829, \"height\": 539, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17329v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 905, \"height\": 256, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17329v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 854, \"height\": 330, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.17329v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 728, \"height\": 192, \"label\": \"Table\"}]"
motivation: 现有剪枝方法忽略深度网络内部通道的相似性，导致压缩模型无法有效保留预训练网络的基础特征。
method: 通过Wasserstein距离构建通道相似矩阵，运用层次聚类划分并融合通道参数，生成压缩网络。
result: 在三个医学图像数据集上，MIS-HCC在精度和压缩效率方面均优于当前最优方法。
conclusion: MIS-HCC为资源受限医疗平台高效部署医学图像分割模型提供了可行且优越的解决方案。
---

## 摘要
医学图像分割模型需要同时具备高精度和轻量化设计，以适应实际医疗应用。由于这些模型的高计算量和参数需求，将其部署在资源有限的医疗平台上仍然是一项重大挑战。现有的模型压缩剪枝方法大多忽略了复杂深度神经网络内部结构之间的内在联系和相似性，导致压缩后的模型可能无法有效保留预训练网络的基本特征。为解决这一问题，我们提出了一种面向医学图像分割模型的层次化聚类压缩方法（MIS-HCC）。该方法采用层次化聚类对通道进行划分，并高效融合其参数。具体而言，它利用Wasserstein距离表示预训练网络层内通道间的相似性，形成指导聚类过程的相似性矩阵。随后，对每个聚类内的通道进行融合，生成压缩网络。在三个医学图像数据集上的实验结果表明，MIS-HCC在准确率和压缩效率上均优于现有最先进方法，为在资源有限的医疗平台上部署医学图像分割模型提供了一种有效解决方案。

## Abstract
Medical image segmentation models require both high accuracy and lightweight design to accommodate real-world medical applications. The deployment of these models on resource-limited medical platforms remains a significant challenge due to their high computational and parameter requirements. Existing pruning methods for model compression mostly overlook the intrinsic connections and similarity between the internal structures of complex deep neural networks. As a result, compressed models may not effectively retain the basic features of the pretrained network. To solve this problem, we propose a hierarchical clustering compression method for medical image segmentation models (MIS-HCC). This approach employs hierarchical clustering to partition channels and fuse their parameters efficiently. Specifically, it leverages the Wasserstein distance to represent similarity of channels within layers of pre-trained network, forming a similarity matrix that guides the clustering process. Channels within each cluster are then fused to produce a compressed network. Experimental results on three medical image datasets application demonstrate that MIS-HCC outperforms the state-of-the-art methods in both accuracy and compression efficiency, offering an effective solution for deploying medical image segmentation models on resource-limited medical platforms.

---

## 论文详细总结（自动生成）

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：医学图像分割模型（如U-Net、Transformer架构）在取得高精度同时，参数量和计算开销巨大，难以部署在资源受限的医疗平台（如移动设备、边缘计算节点）。现有结构化剪枝方法（如HRank、CHEX）通常独立评估每个通道的重要性，忽略了深度神经网络内部通道之间的**内在相似性与相关性**，导致压缩后模型丢失关键细粒度特征。
- **核心问题**：如何在保留预训练网络基本特征的前提下，高效压缩模型，实现精度与轻量化的平衡。
- **总体含义**：提出一种基于层次化通道聚类与参数融合的结构化压缩框架MIS-HCC，通过显式建模通道相似性并智能融合冗余通道，而非简单删除，从而在保持甚至提升分割精度的同时大幅降低模型规模。

### 2. 论文提出的方法论

- **核心思想**：利用**Wasserstein距离**衡量预训练网络每层中不同通道之间的分布相似性，构建相似矩阵；然后采用**凝聚层次聚类（Agglomerative Hierarchical Clustering）** 将相似通道划分为簇；最后对每个簇内的通道参数进行**算术平均融合**，生成压缩后的网络。该方法无需微调或重训练，适用于CNN和Transformer骨干网络。
- **关键技术细节**：
  1. **相似性度量**：对任意层 \( l_i \) 的权重张量 \( W_i \in \mathbb{R}^{N_{in} \times N_{out} \times \cdots} \)，将其除输入通道维度外的所有维度展平，得到特征矩阵 \( M_1 \in \mathbb{R}^{N_{in} \times D_i} \)。对任意两个通道 \( v_p, v_q \)，计算Wasserstein距离：
     \[
     W(v_p, v_q) = \inf_{\gamma \in \Gamma(\mu_p, \mu_q)} \int_{X \times X} d(x,y) \, d\gamma(x,y)
     \]
     归一化后得到相似矩阵 \( M_2 = 1 - c M_{dist} \)。
  2. **层次聚类**：基于相似矩阵 \( M_2 \)，采用**平均连接（Average Linkage）** 的凝聚层次聚类，逐步合并距离最小的簇，直至达到预设的压缩比例（由剪枝率或树状图截断阈值决定）。
  3. **参数融合**：对每个聚类索引集 \( I_c \)，沿输入通道维度求平均：
     \[
     W_{\text{fused}}[c, \cdot] = \frac{1}{|I_c|} \sum_{p \in I_c} W_i[p, \cdot]
     \]
     新层输入通道数变为 \( K \)，参数和FLOPs减少因子为 \( K / N_{in} \)。

### 3. 实验设计

- **数据集与场景**：
  - **2D分割**：BUSI（乳腺超声，437张）、DSB（细胞核分割，670张）、ISIC（皮肤病变，2594张）。所有图像统一裁剪至256×256，80%训练/20%测试。
  - **3D分割**：MICCAI FLARE 2021（腹部CT，511例扫描，标注肝脏、肾脏、脾脏、胰腺）。使用MedSAM2作为骨干。
- **基准方法**：随机剪枝（RP）、PFEC（基于幅值）、HRank（基于特征图秩）、CHEX（通道探索）、TPP（训练性保持剪枝）。所有方法在相同环境下复现。
- **评估指标**：Dice系数、IoU（交并比）。
- **主实验剪枝率**：统一87.5%（参数和FLOPs大幅降低）。额外在BUSI上测试了50%、75%两种剪枝率。

### 4. 资源与算力

- **2D分割**：单张NVIDIA RTX 4090 GPU，batch size=100，训练200 epochs，Adam优化器，初始学习率0.001，权重衰减0.0001，余弦退火学习率调度。
- **3D分割**：因显存限制，batch size=2，初始学习率1e-4，其余设置与2D相同。
- 未提及具体训练时长或使用的GPU数量，仅说明单卡环境。

### 5. 实验数量与充分性

- **实验组数**：主实验覆盖2D三个数据集×两种骨干（U-Net, U-Net++）共6组对比，3D一个数据集×MedSAM2一组对比；压缩效率对比（不同剪枝率）；消融实验（是否存在Wasserstein距离、层次聚类的组合对比）；视觉结果展示。总计约**10+组定量实验**。
- **充分性评估**：
  - **客观公平**：所有方法在相同环境复现，统一剪枝率、统一指标。
  - **覆盖度**：涵盖2D/3D、CNN/Transformer骨干、多种数据集，对比了6种主流剪枝方法，包含随机、基于重要性、基于秩、训练式等多种范式。
  - **消融完整**：分别验证了Wasserstein距离和层次聚类模块的单独效果及组合效果。
  - **不足**：未在更大规模模型（如SwinUNet）或其他模态（如MRI、X-ray）上验证，也未报告统计显著性检验（如置信区间）。

### 6. 论文的主要结论与发现

- **MIS-HCC在所有实验中优于或持平SOTA方法**：在87.5%高剪枝率下，2D任务中Dice/IoU甚至略高于未压缩的基线（如BUSI上U-Net Dice从0.7806提升至0.7841），而其他方法均有明显下降；3D任务中仅损失0.14% Dice（0.8483→0.8471），远优于TPP（下降1.61%）。
- **Wasserstein距离+层次聚类协同有效**：消融实验表明二者组合达到最佳性能，单独使用任一模块也有提升。
- **方法通用性强**：适用于CNN（U-Net, U-Net++）和Transformer（MedSAM2），2D及3D数据。

### 7. 优点

1. **无需微调或重训练**：参数融合为封闭式算术平均，压缩后直接部署，计算开销小。
2. **显式建模通道相关性**：利用Wasserstein距离捕捉分布差异，比余弦相似性更鲁棒，避免局部质量偏移影响。
3. **层次聚类保留结构化信息**：平均连接平衡簇内/簇间距离，生成稳定聚类，避免k-means的随机初始化和硬划分问题。
4. **兼容多种硬件**：保留原始网络拓扑（仅改变通道数），可直接利用通用加速库。
5. **在部分数据集上压缩后精度甚至超过原模型**，说明融合冗余通道可起到正则化或去噪作用。

### 8. 不足与局限

- **相似性矩阵计算开销**：对每层通道对计算Wasserstein距离可能在大规模模型（如具有数千通道的深层网络）中带来额外时间/内存成本，论文未讨论计算效率改进。
- **实验覆盖有限**：仅测试了3个2D数据集和1个3D数据集，未涉及更多模态（如MRI、视频）、更复杂任务（如多器官分割）或更大型骨干（如SwinUNet、nnU-Net）。
- **剪枝率单一主导**：主实验固定87.5%，虽然辅以50%和75%对比，但缺乏对极低剪枝率（如95%以上）或自适应剪枝率的探索。
- **缺乏部署实测**：未在真实移动设备或边缘节点上测试推理速度、内存占用等实际性能，仅报告参数和FLOPs减少率。
- **可能的风险**：聚类融合可能导致某些不相似的通道被迫合并，在噪声或异常数据上引入偏差；论文未提供对聚类数K的自动选择策略（需人为设定剪枝率或树状图阈值）。

（完）
