---
title: "UMSS: Towards Unsupervised Multi-modal Semantic Segmentation"
title_zh: "UMSS: 迈向无监督多模态语义分割"
authors: "Haitian Zhang, Thai Duy Nguyen, Xiangyuan Wang, Mohan Liu, Lin Wang"
date: 2026-07-14
pdf: "https://arxiv.org/pdf/2607.12372v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 无监督多模态语义分割
tldr: "多模态语义分割在复杂环境中至关重要，但像素级标注成本高昂。尽管单模态无监督语义分割已取得进展，直接扩展至多模态时因融合退化而难以发挥互补优势。本文首次探索无监督多模态语义分割（UMSS），提出UniM2框架，基于DINOv3构建统一潜在空间，通过跨模态对应协同（CMCS）提取共享语义，并设计跨模态协调器（CMH）以RGB为稳定参考，抑制不一致关系监督。在NYU Depth v2和MFNet上，UniM2分别提升mIoU 6.4%和9.8%，证明了无需标注即可有效融合多模态信息的可行性，为无监督多模态分割开辟了新方向。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.12372v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1274, \"height\": 377}, {\"url\": \"assets/figures/arxiv/2607.12372v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1262, \"height\": 829}, {\"url\": \"assets/figures/arxiv/2607.12372v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1268, \"height\": 362}, {\"url\": \"assets/figures/arxiv/2607.12372v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1263, \"height\": 702}, {\"url\": \"assets/figures/arxiv/2607.12372v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1262, \"height\": 411}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.12372v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1254, \"height\": 449}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1256, \"height\": 697}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1259, \"height\": 246}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1259, \"height\": 284}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 496, \"height\": 228}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 393, \"height\": 228}, {\"url\": \"assets/tables/arxiv/2607.12372v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 386, \"height\": 226}]"
motivation: 多模态语义分割依赖昂贵标注，现有无监督方法在多模态融合中存在退化问题。
method: 提出UniM2框架，利用跨模态对应协同学习统一潜在空间，并引入跨模态协调器以RGB为参考抑制冲突。
result: "在NYU Depth v2和MFNet上mIoU分别提升6.4%和9.8%，优于现有方法。"
conclusion: 首次实现无监督多模态语义分割，验证了无需标注有效融合互补信息的可行性。
---

## 摘要
多模态语义分割（MSS）对于复杂环境中的鲁棒感知至关重要，但由于人工标注的昂贵成本，其潜力仍未得到充分开发。尽管无监督语义分割（USS）在单一RGB模态上取得了显著成果，但其直接扩展到多模态数据时常常受到融合退化问题的阻碍。这是因为，在没有显式监督的情况下，现有框架难以协调不同传感器捕获的异质结构模式，因此无法有效利用它们的互补信息。在本文中，我们首次尝试解决无监督多模态语义分割（UMSS）这一新问题，旨在完全无标签的设置下有效利用互补的传感器信息。为此，我们提出了UniM2（统一多模态），一种基于DINOv3构建的新框架，将传统融合方法转化为一致的性能提升。我们的关键思想是学习一个由跨模态对应协同（CMCS）驱动的统一潜在空间，以提取内在的共享语义线索，从而绕过标签引导的自适应融合需求。为了缓解固有的模态间冲突，我们引入了跨模态协调器（CMH），将RGB指定为稳定参考，有效抑制不一致的关系监督，同时引导模型利用互补的结构特征。在NYU Depth v2和MFNet上的大量实验结果表明，UniM2分别将mIoU提高了6.4%和9.8%，显示出在UMSS中相对于现有框架的明显优势。

## Abstract
Multimodal semantic segmentation (MSS) is essential for robust perception in complex environments, yet its potential remains largely untapped because of the prohibitive cost of human annotations. While unsupervised semantic segmentation (USS) has achieved strong results on a single RGB modality, its naive extension to multimodal data is often hindered by fusion degradation. This occurs because, without explicit supervision, existing frameworks struggle to reconcile the heterogeneous structural patterns captured by different sensors and therefore fail to effectively exploit their complementary information. In this paper, we make the first attempt to address the novel problem of Unsupervised Multimodal Semantic Segmentation (UMSS), aiming to effectively exploit complementary sensor information in a fully label free setting. To this end, we propose UniM2 (Unified Multimodal), a novel framework built on DINOv3 that transforms conventional fusion methods into consistent performance gains. Our key idea is to learn a unified latent space driven by Cross Modal Correspondence Synergy (CMCS) to extract intrinsic shared semantic cues, bypassing the need for label guided adaptive fusion. To mitigate inherent intermodal conflicts, we introduce a Cross Modal Harmonizer (CMH) that designates RGB as a stable reference, effectively suppressing inconsistent relational supervision while guiding the model to exploit complementary structural features. Extensive experimental results on NYU Depth v2 and MFNet show that UniM2 improves mIoU by 6.4% and 9.8%, respectively, demonstrating clear advantages over existing frameworks for UMSS.

---

## 论文详细总结（自动生成）

# UMSS: 迈向无监督多模态语义分割 - 论文总结

## 1. 核心问题与整体含义（研究动机和背景）

- **背景**：多模态语义分割（MSS）在自动驾驶、机器人导航等安全关键任务中至关重要，通过融合RGB与深度/热红外等互补信号来应对复杂环境。但现有MSS严重依赖密集像素级人工标注，成本高昂且限制了可扩展性。
- **问题**：无监督语义分割（USS）在单一RGB模态上取得了成功，但将其直接扩展至多模态输入时，由于不同传感器捕获的异质结构模式难以在没有标签引导下协调，导致“融合退化”——性能反而下降，无法有效利用互补信息。
- **目标**：本文首次定义并探索无监督多模态语义分割（UMSS）任务，旨在完全无标签设置下有效利用互补传感器信息。

## 2. 方法论：核心思想、关键技术细节、公式或算法流程（文字说明）

- **核心思想**：提出UniM2框架，基于DINOv3构建，通过学习一个统一的潜在空间（unified latent space）来提取跨模态的共享语义线索，绕过传统标签引导的自适应融合；同时引入跨模态协调器来缓解模态冲突。
- **关键技术细节**：
    - **Cross-modal Correspondence Synergy (CMCS)**：作为内在监督信号，通过鼓励跨模态对应的一致性（即不同模态对同一对图像的语义相似度保持同步），促进发现共享语义流形，同时保留互补线索。损失函数为：
        - 对RGB和辅助模态分别计算特征对应 \(F_{rgb}\) 和 \(F_X\)，以及分割嵌入对应 \(S\)，然后按USS的对应蒸馏损失公式（\( -\sum (F - b) \odot \max(0, S) \)）计算 \(L_{rgb}\) 和 \(L_X\)，总损失 \(L_{cmcs} = L_{rgb} + \lambda L_X\)。
    - **Cross-modal Harmonizer (CMH)**：指定RGB为稳定语义参考，引入一个轻量级可学习变换（两层卷积）对分割嵌入 \(s\) 进行变换得到 \(s_X\)，用 \(s_X\) 而非原始 \(s\) 与辅助特征进行对应监督。这样能自适应吸收互补线索，同时隔离像素级模态矛盾，防止辅助信号扭曲主语义流形。
    - **框架流程**：RGB图像和辅助模态图像分别输入冻结的DINOv3骨干提取特征 \(f_{rgb}, f_X\)，经模态特定网络（MSN）精炼后通过Conv Fusion融合得到 \(f_{fus}\)，再由分割头S投影到低维嵌入 \(s\)。训练时利用CMCS和CMH优化整个框架。
- **可扩展性**：模块化设计允许CMH分支扩展至多个辅助模态（如MCubeS中的NIR、DoLP、AoLP），总损失为 \(L_{cmcs} = L_{rgb} + \sum \lambda_n L_{X_n}\)。

## 3. 实验设计：数据集、benchmark、对比方法

- **数据集**：
    - **NYU-Depth-v2**：室内RGB-深度基准，1449对齐图像对，13类评估协议。
    - **MFNet**：城市RGB-热红外数据集，1569对齐图像对，8类，包含白天和夜间场景。
    - **MCubeS**：四模态材料分割数据集，包括RGB、NIR、DoLP、AoLP，20类。
- **基准（benchmark）**：在三个数据集上评估平均交并比（mIoU）和像素准确率（Acc.）。
- **对比方法**：
    - 直接K-means聚类于DINO特征。
    - 代表性USS方法：STEGO、EAGLE及其多模态变体（RGB+X输入）。
    - 图像级融合方法（如SwinFusion、Mask-DiFuser）和RGB→X蒸馏方法（CORAL、MMD、Cosine）作为补充比较（结果在附录）。
    - 所有方法使用相同超参数搜索预算（200次贝叶斯优化）保证公平。

## 4. 资源与算力

- **文中提及**：训练过程高效，所有模型在**单块NVIDIA GeForce RTX 5090 GPU**上训练时间**少于两小时**（每个模型）。优化器为Adam，学习率5e-4，batch size 32。
- **注**：未具体说明多个实验的总GPU小时数，但单模型训练效率极高。

## 5. 实验数量与充分性

- **实验组数**：主实验在3个数据集上进行，每个数据集对比了多种基线（DINO、STEGO、EAGLE及其多模态版本，以及UniM2的多个骨干变体）。
- **消融实验**：
    - 组件消融：CMCS、MSN、CMH的贡献（表5）。
    - CMH放置位置消融：RGB仅锚定 vs. 深度仅锚定 vs. 两者均有 vs. 无锚定（表6）。
    - 融合策略消融：Max、Mean、Sum vs. Conv Fusion（表7）。
    - 附录中还包含：更多融合基线、超参数分析、每类分布和混淆矩阵等。
- **充分性评估**：实验设计较为充分，覆盖多个模态组合、骨干网络变体（ViT-S/16和ViT-B/16），并严格确保超参数搜索公平。消融实验验证了每个模块的有效性。但缺少对更多USS方法（如PiCIE、MaskContrast等）的直接对比，主要聚焦于STEGO和EAGLE这两个最新代表。

## 6. 主要结论与发现

- 现有监督融合策略在无标签设置下会导致性能退化（如STEGO+深度mIoU下降3.5%），而UniM2**始终将模态互补转化为一致的正向增益**：
    - 在NYU-Depth-v2上：相比STEGO RGB基线，UniM2提升mIoU **6.4%**（ViT-B/16），从31.7%到38.1%。
    - 在MFNet上：提升mIoU **9.8%**（ViT-B/16），从35.9%到45.7%。
    - 在MCubeS上：从单RGB的19.1% mIoU提升至IN组合的21.5%，IND组合的21.8%。
- CMH有效缓解模态冲突，RGB作为锚定优于深度作为锚定，验证了RGB提供更稳定的语义结构。
- Conv Fusion优于静态融合，但在CMH约束下才真正有效。
- 可扩展性验证：UniM2能够自然扩展到四模态输入，并在多数设置下带来持续改进。

## 7. 优点

- **问题新颖**：首次系统定义并解决UMSS任务，填补了无监督学习与多模态感知之间的空白。
- **方法设计巧妙**：CMCS通过对应蒸馏天然地利用跨模态协同，CMH以轻量可学习缓冲层实现软对齐，既保留互补信息又抑制冲突，设计简洁有效。
- **实验严谨**：使用一致超参数搜索、多数据集验证、公平比较，并提供了充分的消融和可视化分析。
- **实用高效**：基于冻结DINOv3骨干，训练快速（<2小时/模型），易于复现。
- **可扩展性**：模块化设计能够无缝扩展到任意数量的辅助模态。

## 8. 不足与局限

- **评估局限性**：仅与STEGO和EAGLE两种USS基线对比，未涵盖更早的USS方法（如IIC、PiCIE）或更近期的变体（如MaskContrast、SMOOSeg），比较范围可进一步扩大。
- **模态依赖**：实验主要考察RGB-深度/RGB-热红外两模态，以及四模态材料分割。对于更多样化的传感器（如事件相机、LiDAR）在无监督下的表现未验证。
- **超参数敏感性**：USS任务本身对超参数敏感，虽然分配了统一搜索预算，但最优配置可能因方法而异，图表中未显示超参数具体分布。
- **跨域泛化风险**：所有数据集均为室内/城市驾驶场景，未测试在更复杂开放环境（如野外、水下）的性能，泛化能力未知。
- **CMH设计简化**：CMH仅为两层卷积，可能无法处理高度复杂的模态冲突；更精细的冲突检测和自适应机制或许能带来进一步提升。
- **缺乏开放代码**：论文附有项目页面，但未提及代码开源计划，可能影响可复现性。

（完）
