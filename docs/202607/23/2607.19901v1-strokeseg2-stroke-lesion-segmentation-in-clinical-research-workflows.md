---
title: "StrokeSeg2: Stroke Lesion Segmentation in Clinical Research Workflows"
title_zh: StrokeSeg2：临床研究工作流中的卒中病变分割
authors: "Youwan Mahé, Axel Plessis, Stéphanie Leplaideur, Elise Bannier, Florent Leray, Francesca Galassi"
date: 2026-07-22
pdf: "https://arxiv.org/pdf/2607.19901v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 临床研究中的卒中病灶分割
tldr: "深度学习分割模型在临床环境中部署困难，StrokeSeg2通过知识蒸馏和ONNX Runtime Float16量化构建轻量级框架。架构蒸馏贡献最大，能耗降低超90%，推理时间平均减少84%，0.84M参数学生模型仅2.1MB。提供Windows/macOS/Linux独立安装程序，无需外部依赖，便于临床研究集成。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.19901v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1266, \"height\": 327, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.19901v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1200, \"height\": 1091, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.19901v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 594, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19901v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1023, \"height\": 323, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.19901v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1013, \"height\": 223, \"label\": \"Table\"}]"
motivation: 解决nnU-Net等深度学习模型因软件依赖和计算需求难以在临床工作站部署的问题。
method: 提出StrokeSeg2框架，结合架构知识蒸馏压缩模型，利用ONNX Runtime和Float16量化优化推理效率。
result: "能耗降低超90%，推理时间平均减少84%，0.84M参数模型保持鲁棒定位和竞争性分割性能，磁盘占用2.1MB。"
conclusion: 开发跨平台独立安装的StrokeSeg2，无需Docker或外部环境，实现高性能分割在常规临床研究流程中的便捷部署。
---

## 摘要
深度学习框架如nnU-Net在脑病变分割中达到了最先进的性能，但由于软件依赖和计算需求等原因，在临床研究环境中仍难以部署。我们介绍了StrokeSeg2，一个轻量级、模块化、跨平台的C++/Qt框架，旨在将资源密集型的3D卒中分割流程转化为可移植且可复现的应用。为提高与标准临床工作站的兼容性，我们研究了通过知识蒸馏进行架构压缩与使用ONNX Runtime结合Float16量化的推理优化的联合效果。在不同的硬件配置（CPU、集成GPU和专用GPU）下，架构蒸馏成为效率提升的主要贡献者，使能耗降低超过90%，推理时间平均减少84%。具体而言，我们确定了一个0.84M参数的学生模型作为最有利的权衡，将原始102.3M参数的教师架构缩小至2.1 MB的磁盘占用，同时保持稳健的病变定位和具有竞争力的分割性能。这种小占用支持为临床工作站目标开发自包含安装程序。最后，StrokeSeg2将这些优化打包成适用于Windows、macOS和Linux的独立安装程序。通过提供图形和命令行界面，且无需Docker或外部环境依赖，StrokeSeg2促进了高性能分割流程在常规临床研究管线中的部署。

## Abstract
Deep learning frameworks like nnU-Net achieve state-of-theart brain lesion segmentation performance but remain difficult to deploy in clinical research environments due to, among other reasons, software dependencies and computational requirements. We introduce StrokeSeg2, a lightweight, modular, cross-platform C++/Qt framework designed to adapt resource-intensive 3D stroke segmentation pipelines into portable and reproducible applications. To improve compatibility with standard clinical workstations, we investigate the combined effect of architectural compression through knowledge distillation and inference optimisation using ONNX Runtime with Float16 quantisation. Across heterogeneous hardware configurations (CPU, integrated GPU, and dedicated GPU) architectural distillation emerged as the primary contributor to efficiency gains, contributing to over 90% reduction in energy consumption and an average 84% reduction in inference time. Specifically, we identify a 0.84M-parameter student model as the most favourable trade-off, reducing the original 102.3M-parameter teacher architecture to a 2.1 MB disk footprint while preserving robust lesion localisation and competitive segmentation performance. This small footprint supports the development of a self-contained installer for clinical workstation targets. Finally, StrokeSeg2 packages these optimisations into standalone installers for Windows, macOS, and Linux. By providing both graphical and commandline interfaces without Docker or external environment dependencies, StrokeSeg2 facilitates deployment of high-performance segmentation workflows for routine clinical research pipelines.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 核心问题与整体含义

- **研究动机**：深度学习模型（如 nnU-Net）在脑病变分割上表现优异，但部署到临床研究环境中面临软件依赖复杂、计算资源需求高、难以与标准临床工作站兼容等问题。现有工具（如 TorchIO、pymia、MONAI Deploy、PACS-AI）侧重于研究预处理或系统集成，缺乏轻量级、自包含的独立应用。
- **整体含义**：开发一个轻量级、模块化、跨平台的 C++/Qt 框架 StrokeSeg2，将资源密集型的 3D 卒中分割流水线转化为可移植、易部署的独立应用，以适应标准临床工作站（CPU、集成 GPU、专用 GPU），从而推动高性能分割模型在常规临床研究中的实际应用。

## 2. 方法论：核心思想、关键技术细节

- **核心思想**：通过知识蒸馏进行架构压缩，结合 ONNX Runtime 和 Float16 量化优化推理效率，并将优化后的模型打包为自包含的跨平台安装程序。
- **关键技术细节**：
  - **模型蒸馏**：基于 ATLAS v2.0 数据集（655 名受试者）训练教师模型（nnU-Net，102.3M 参数）。设计 9 个学生架构，逐步减少每阶段特征通道数（特征数保持 16 的倍数以优化内存对齐）。训练 1000 个 epoch，batch size 为 6，损失函数为加权组合：\(\mathcal{L}_{total} = (1-\alpha)\mathcal{L}_{hard} + \alpha T^2 \cdot D_{KL}\left(\sigma(z_t/T) \parallel \sigma(z_s/T)\right)\)，其中 \(\alpha=0.5\)，温度 \(T=4.0\)。教师模型冻结，学生同时从真实标签和教师输出学习。
  - **推理优化**：将训练好的模型转换为 ONNX 格式，应用 Float16 量化以减小模型尺寸和内存占用。使用 ONNX Runtime（或 Windows ML）作为统一推理后端，自动选择最优设备（CPU、集成 GPU、专用 GPU）。
  - **软件架构**：三阶段流水线——① BIDS 兼容预处理（使用 Anima 工具进行空间归一化和脑提取）；② ONNX Runtime 推理（包含 patch-based Gaussian merging）；③后处理（softmax 转概率图、二值化掩码，可选空间变换恢复原始空间）。代码分为应用仓库和构建打包仓库，使用 CMake/CPack 生成平台特定安装程序（Windows NSIS、macOS DMG、Linux DEB/RPM），打包所有依赖，无需 Docker 或 Python 环境。
- **对比基线**：原始 Python 版 nnU-Net 流水线 [8]（约 2.8 GB 应用大小）及更早的 nnU-Net 系统。

## 3. 实验设计

- **数据集**：
  - 训练：ATLAS v2.0，655 名受试者（用于蒸馏训练）。
  - 测试：ATLAS v2.1，300 名独立受试者（慢性卒中，T1w 模态）。
- **基准与对比**：
  - 原始教师模型（102.3M 参数）作为性能上界。
  - 9 个学生网络（S1~S9，参数从 52.9M 递减至 0.05M）。
  - 对比不同推理后端：CPU（Intel Core Ultra 7 265HX）、集成 GPU（Intel Graphics）、专用 GPU（NVIDIA RTX Pro 1000 Blackwell）。
  - 对比 FP32 与 FP16 量化 ONNX 模型效果。
- **评价指标**：
  - 分割质量：全局 Dice、病灶级 Dice、平均表面距离（ASD）。
  - 检测性能：病灶级 F1 分数（真阳性定义为至少 10% 体积重叠）。
  - 按病灶大小分层（小、中、大，基于体积四分位数）。
  - 统计等价性检验：TOST 检验（\(\alpha=0.05, \delta=10^{-3}\)）比较 PyTorch 与量化 ONNX 的 Dice 分数。
  - 运行时与能耗：推理时间、能耗（扣除空闲基线功率）。

## 4. 资源与算力

- 训练：NVIDIA H100 GPU，共 154 小时（IDRIS HPC 资源分配 2026-AD011016064R1），估计排放 16 kgCO₂eq。
- 推理测试硬件：CPU（Intel Core Ultra 7 265HX）、集成 GPU（Intel Graphics）、专用 GPU（NVIDIA RTX Pro 1000 Blackwell）。还测试了 NPU（Intel AI Boost NPU），但因 SRAM 缓冲区无法容纳大 3D 全卷积网络且编译时间超 30 分钟而被排除。

## 5. 实验数量与充分性

- **实验组数**：
  - 蒸馏实验：9 个学生架构 + 1 个教师，共 10 种模型。
  - 推理后端测试：3 种硬件（CPU/iGPU/dGPU）× 2 种精度（FP32/FP16）分别测试教师和 S7 模型（最佳权衡架构）。其他学生模型仅报告了在 CPU FP32 下的完整指标曲线。
  - 量化验证：对每个架构进行了 PyTorch FP32 vs ONNX FP16 的统计等价性检验（TOST）。
  - 安装包大小对比：Windows/macOS/Debian 三平台。
- **充分性与公平性**：
  - 测试集独立（ATLAS v2.1，不同于训练集 v2.0），避免过拟合。
  - 指标按病灶大小分层，揭示了大小依赖的偏差。
  - 统计检验确保了量化不引入显著性差异。
  - 能耗计算扣除空闲基线，公平比较。
  - 但仅测试了单模态（T1w），未测试 T1w+FLAIR 双模态或其他数据集，外部泛化性有限。

## 6. 主要结论与发现

1. **架构蒸馏是效率提升的主要贡献者**：相比教师模型，S7 学生模型在 CPU 上提速 15.2 倍，iGPU 上 5.5 倍，dGPU 上 4.7 倍；能耗降低超 90%（相对于 CPU FP32 教师基线）。
2. **最佳权衡点**：S7 模型（0.84M 参数，磁盘占用 2.1 MB ONNX FP16）在保持良好分割性能（全局 Dice ~0.6，病灶级 Dice ~0.7，F1 ~0.8）的同时实现大幅压缩。
3. **量化效果有限**：FP16 量化主要进一步减小模型尺寸和磁盘占用（比 FP32 减小约 70%），但对推理时间影响很小，且统计等价性成立。
4. **部署可行**：自包含安装程序显著减小体积（Windows 应用 269 MB vs 原 Python 基线 2815 MB），无需 Docker 或 Python 环境。
5. **病灶大小依赖性**：小病灶始终得分较低且置信区间较宽，与以往研究一致。

## 7. 优点

- **实用性导向**：从临床部署痛点出发，最终产出可直接安装使用的独立跨平台应用，具有实际应用价值。
- **方法组合有效**：系统性地结合知识蒸馏与 ONNX Runtime 量化，并分别评估各自贡献，消融分析清晰。
- **轻量化效果显著**：将 102M 参数模型压缩至 0.84M，磁盘占用从 820 MB 降至 2.1 MB，能耗与推理时间大幅下降。
- **实验设计严谨**：包含统计等价性检验、按病灶大小分层、能耗扣除基线等，增强了结论可信度。
- **软件工程优秀**：模块化架构、CMake/CPack 打包、多平台支持、命令行/GUI 双界面，便于不同用户群体使用。
- **数据与代码开源**：提供了软件文档链接和实验复现代码，可复现性良好。

## 8. 不足与局限

- **仅评估单模态**：实验仅用 T1w 单模态数据，未验证 T1w+FLAIR 双模态或多模态场景下的性能。
- **外部泛化性有限**：只基于 ATLAS 数据集（慢性卒中），对急性/亚急性病灶、不同 MRI 采集参数或不同人群的效果未知。
- **仅报告了 S7 的硬件全面测试**：其他学生架构（S1~S6、S8、S9）仅展示了 CPU FP32 下的分割指标，缺乏跨硬件效率对比。
- **NPU 验证不充分**：因技术限制排除 NPU，但未深入分析原因，未来可能随硬件更新而改变。
- **知识蒸馏未与剪枝、量化感知训练等方法对比**：缺乏与其他压缩技术（如结构化剪枝、QAT）的比较。
- **临床部署后实际用户反馈缺失**：论文未报告在真实医院信息系统中的集成测试结果或用户研究。
- **能耗计算未标准化**：虽扣除空闲功率，但未使用标准能耗度量工具（如功率计），仅依赖系统估算值，可能存在误差。
- **缺乏不确定性量化**：模型未提供预测置信度，对临床决策的可靠性有影响。

（完）
