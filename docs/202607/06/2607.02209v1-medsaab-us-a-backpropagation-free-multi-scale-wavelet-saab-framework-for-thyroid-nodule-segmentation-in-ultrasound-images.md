---
title: "MedSaab-US: A Backpropagation-Free Multi-Scale Wavelet-Saab Framework for Thyroid Nodule Segmentation in Ultrasound Images"
title_zh: MedSaab-US：一种无反向传播的多尺度小波-Saab框架用于超声图像中甲状腺结节分割
authors: Mohammad Amanour Rahman
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02209v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 无反向传播的医学图像分割，结合小波变换和Saab变换
tldr: 针对深度学习分割甲状腺结节时参数多、依赖GPU、可解释性差的问题，提出无反向传播的MedSaab-US框架。该框架结合多级离散小波变换和多尺度通道级Saab变换提取空频特征，经标签辅助贪婪特征选择后输入XGBoost分类器。在TN3K数据集上，平均Dice系数0.4784，参数量小于500K，CPU推理约0.3秒每张。作为非DL基线，揭示等回声结节带来的分割挑战。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02209v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1845, \"height\": 1005}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02209v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 933, \"height\": 379}, {\"url\": \"assets/tables/arxiv/2607.02209v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 930, \"height\": 277}]"
motivation: 现有深度学习分割方法参数量大、依赖GPU反向传播训练，且缺乏数学可解释性，难以部署于资源受限环境。
method: 采用多级小波变换与多尺度（5x5/11x11/21x21）通道级Saab变换提取特征，经标签辅助贪婪选择后由XGBoost分类。
result: 在TN3K数据集上达到0.4784 Dice、0.5768精确率、0.5604召回率，模型小于500K参数，CPU推理0.3秒每张。
conclusion: MedSaab-US作为非深度学习基线，证明了无反向传播分割的可行性，并暴露了等回声结节对分割的挑战。
---

## 摘要
深度学习方法主导了超声图像中甲状腺结节的分割，取得了高Dice分数，但代价是数百万参数、通过反向传播的GPU依赖训练以及有限的数学可解释性。这些限制阻碍了在资源受限环境中的部署。在本文中，我们提出了MedSaab-US，一种基于绿色学习范式的无反向传播分割框架。MedSaab-US通过将多级离散小波变换与多尺度通道级Saab（带调整偏置的子空间近似）变换相结合，在5×5、11×11和21×21像素的块尺寸下提取多尺度空间-频率特征。标签辅助贪婪特征选择保留最具判别力的特征，并输入XGBoost分类器进行像素级预测。Saab变换参数根据数据统计解析确定，而XGBoost采用迭代贪婪树构建，无需反向传播。在TN3K数据集（2879张训练图像和614张测试图像）上评估，MedSaab-US的平均Dice系数为0.4784±0.2190，精确率为0.5768，召回率为0.5604，模型占用低于500K参数，仅使用CPU推理每张图像约0.3秒。我们将此结果作为甲状腺超声分割的探索性非深度学习基线，并分析了等回声结节带来的具体挑战。消融研究进一步量化了每个管道组件的贡献，包括对标签辅助贪婪特征选择和训练集大小的单独评估。

## Abstract
Deep learning (DL) methods dominate thyroid nodule segmentation in ultrasound (US) images, achieving high Dice scores but at the cost of millions of parameters, GPU-dependent training via backpropagation, and limited mathematical tractability. These limitations impede deployment in resource-constrained environments. In this paper, we propose MedSaab-US, a backpropagation-free segmentation framework grounded in the Green Learning paradigm. MedSaab-US extracts multi-scale spatial-frequency features by combining multi-level Discrete Wavelet Transform (DWT) with multi-scale channel-wise Saab (Subspace Approximation with Adjusted Bias) transforms at patch sizes of 5 x 5, 11 x 11, and 21 x 21 pixels. Label-Assisted Greedy (LAG) feature selection retains the most discriminative features, which are fed to an XGBoost classifier for pixel-wise prediction. The Saab transform parameters are determined analytically from data statistics, while XGBoost employs iterative greedy tree construction without requiring backpropagation. Evaluated on the TN3K dataset (2,879 training and 614 test images), MedSaab-US achieves a mean Dice coefficient of 0.4784 +/- 0.2190, precision of 0.5768, and recall of 0.5604, with a model footprint under 500K parameters and CPU-only inference in approximately 0.3 seconds per image. We present this result as an exploratory non-DL baseline for thyroid ultrasound segmentation and analyze the specific challenges posed by isoechoic nodules. An ablation study further quantifies the contribution of each pipeline component, including separate evaluations of LAG feature selection and training-set size.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 核心问题与整体含义（研究动机和背景）
- **问题**：深度学习在甲状腺超声结节分割中虽然取得高Dice（>0.80），但存在三大局限：① **参数多、依赖GPU反向传播训练**，难以部署于资源受限环境（如 CPU-only 边缘设备）；② **数学可解释性差**，形成“黑箱”映射，临床医生难以审计；③ **数据饥饿**，小样本时性能严重下降。
- **背景**：绿色学习（Green Learning, GL）范式通过顺序闭式子空间学习（如 Saab 变换）替代梯度优化，已在图像分类（PixelHop、VoxelHop）中展现可解释性和轻量优势，但**尚未应用于像素级医学图像分割**。
- **整体含义**：本文探索性提出 **MedSaab-US**，作为首个 GL 驱动的无反向传播分割框架，旨在建立非DL基线，揭示该路径的可行性及当前性能上限，并分析等回声结节带来的特殊挑战。

### 2. 方法论：核心思想、关键技术细节与流程
- **核心思想**：利用多级离散小波变换（DWT）提取多尺度空频特征，结合多尺度通道级 Saab 变换（解析主成分分析）产生低维表示，经标签辅助贪婪（LAG）特征选择后，使用 XGBoost 进行像素级二分类，全过程**无需反向传播**。
- **关键技术细节**：
  - **Stage 1: 多级DWT分解**：对灰度图采用 2 级 Daubechies-4（db4）小波，得到 7 个子带（LL2、LH2、HL2、HH2、LH1、HL1、HH1），各子带上采样至原图大小并归一化。
  - **Stage 2: 多尺度通道级 Saab 变换**：对每个子带分别提取三种 patch 尺寸（5×5、11×11、21×21）的重叠块，每块展平后执行 PCA，保留前 8 个主成分作为特征。参数通过约 40,000 块（来自 400 张训练图像）的样本均值计算，解析确定。共得到 7×3×8 = 168 维特征/像素。
  - **Stage 3: 位置编码 + LAG 特征选择**：连接 3 个归一化空间坐标（行/列归一化、距图像中心欧氏距离），形成 171 维向量。LAG 按与标签的互信息排序，保留前 60 维。
  - **Stage 4: XGBoost 像素分类**：使用 600 棵树、最大深度 7、学习率 0.05，训练时平衡采样（每张图 400 前景 + 400 背景像素）。超参数通过验证集优化 Dice。注意：XGBoost 为迭代贪心树构建，并非闭式解析，但**不涉及梯度反向传播**。

### 3. 实验设计：数据集、基准与对比方法
- **数据集**：**TN3K**（甲状腺结节三视图知识数据集），含 3,493 张超声图像（2,879 张训练 / 614 张测试），图像调整为 256×256，提供像素级结节掩膜。
- **基准与对比方法**：
  - **深度学习方法**：U-Net（0.719 Dice）、TRFE+（0.763）、SwinE-Net（0.831）、MADGNet（0.842）、作者先前 DeepLabv3+ 变体（0.847）。结果引用自原论文。
  - **传统非DL方法**：Otsu阈值+形态学后处理（Otsu+Morph）、基于Haralick纹理的随机森林（RF+Haralick）。均在相同CPU环境下运行。
- **消融实验**：表 2 包含 6 组配置（单尺度→多尺度→加位置特征→加LAG→增加训练数据量）。

### 4. 资源与算力
- **算力详情**：
  - **仅使用 CPU**：Intel Xeon @ 2.0 GHz，2 核心，Kaggle CPU-only 会话（无GPU）。
  - **模型参数量**：< 500K（Saab 变换+LAG+XGBoost，无神经网络权重）。
  - **推理速度**：约 0.3 秒/张（CPU）。
  - **训练资源**：未提及具体训练时间，但 Saab 参数解析计算、LAG 统计选择、XGBoost 训练均为 CPU 可完成，无需 GPU。

### 5. 实验数量与充分性
- **实验组数**：
  - 主对比实验（表1）：对比 5 个 DL 方法 + 2 个传统非DL方法。
  - 消融实验（表2）：6 组配置（逐步添加组件）。
  - 额外分析：XGBoost 特征重要性排序、Dice 分数分布（双峰模式）、等回声结节诊断。
- **充分性评价**：
  - **优点**：消融设计清晰，分离了多尺度、位置特征、LAG 选择、数据量的独立贡献；对性能差距原因（等回声结节、局部感受野）做了诚实分析。
  - **不足**：仅在一个数据集（TN3K）上验证，缺乏跨机构、不同超声设备或不同解剖部位的外部验证；DL 方法的结果引自原文而非复现，对比公平性受限于不同训练/测试设置。

### 6. 主要结论与发现
- **核心结果**：MedSaab-US 在 TN3K 测试集上达到 **Dice 0.4784**，**IoU 0.3415**，**Precision 0.5768**，**Recall 0.5604**。显著优于传统非DL基线（RF+Haralick 0.3518 Dice，Otsu+Morph 0.2341），但与最佳 DL 方法（0.847 Dice）差距 0.37。
- **关键发现**：
  1. **多尺度是最大贡献**：从单尺度（5×5，Dice 0.26）到三尺度（5/11/21，Dice 0.45），Dice 提升 +0.172。
  2. **LAG 选择和数据量贡献微小但独立**：各增约 +0.006~0.008 Dice。
  3. **性能瓶颈**：等回声结节导致高频小波特征弱（XGBoost 特征重要性中 LL2 占据主导，HH 子带排名靠后）；21×21 的最大感受野无法获取全局上下文（如甲状腺边界、气管位置）。
  4. **Dice 分布呈双峰**：约 0.0~0.1 峰对应等回声难例，0.5~0.6 峰对应低回声易例；最高 Dice 可达 0.9184（强对比度案例）。

### 7. 优点
- **方法创新**：首次将绿色学习（Saab 变换）用于超声图像像素级分割，全程无反向传播，具有数学透明性（PCA子空间可审计）。
- **资源友好**：模型 < 500K 参数，CPU 推理 0.3秒/张，适合边缘部署（便携超声设备、低算力环境）。
- **诚实分析**：明确承认非DL方法的性能上限和特定原因（等回声、局部感受野），并指出空间先验的数据集依赖性。
- **实验设计**：消融实验精细，逐组件量化贡献；特征重要性分析支持病理生理解释（等回声导致边界弱响应）。

### 8. 不足与局限
- **性能差距大**：Dice 0.4784 远低于 DL 主流方法（>0.80），当前无法满足临床高精度需求，仅供探索性基线参考。
- **数据集单一**：仅 TN3K，空间先验（图像中心距离）在本数据集中有效，但不同采集协议（不同探头位置、视野裁剪）下可能失效，外部验证缺失。
- **局部感受野限制**：最大 21×21 无法捕捉全局解剖结构，XGBoost 虽强但本质是局部分类器，无法替代 CNN 的上下文编码。
- **XGBoost 仍需调参**：虽然无反向传播，但树的数量、深度、学习率等超参数需要手动调整（论文中在验证集上优化），非完全闭式。
- **等回声结节表现差**：约 83% 案例 Dice < 0.70，模型对弱边界场景缺乏鲁棒性。
- **对比公平性**：DL 方法数据来自原论文，并非在相同硬件/预处理下复现，可能存在差异（如图像尺寸、后处理等）。

（完）
