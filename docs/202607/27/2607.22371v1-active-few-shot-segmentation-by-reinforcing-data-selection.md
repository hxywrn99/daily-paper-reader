---
title: Active few-shot segmentation by reinforcing data selection
title_zh: 通过强化数据选择的主动式小样本分割
authors: "Chenlan Zhao, Benny Wong, Timothy F. Lundberg, Ahmed M. Elsayed, Abdallah Aljarkas, Hamad A. Aljamaan, Lynn Karam, Qianye Yang, Yipeng Hu, Claire C. Villette, Shaheer U. Saeed"
date: 2026-07-24
pdf: "https://arxiv.org/pdf/2607.22371v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 主动少样本医学图像分割
tldr: 小样本学习使医疗图像分割模型能快速适应新任务，但性能高度依赖支持集的选择。现有方法独立评估样本，忽视样本间互补性。本文提出强化学习框架，直接联合优化支持集，最大化下游分割性能。在跨机构骨盆MRI数据集上，该方法显著优于随机选择和现有最优方法，验证了支持集互补性的关键作用。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22371v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1201, \"height\": 339, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22371v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1013, \"height\": 548, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.22371v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1256, \"height\": 244, \"label\": \"Table\"}]"
motivation: 现有主动数据选择方法独立评估样本，忽视支持集内样本间的互补性，导致小样本分割适应性能受限。
method: 提出强化学习框架，智能体从未标记候选池中直接预测能使下游分割性能最大化的支持集，实现支持集的联合优化。
result: 在跨机构骨盆MRI数据集上，该方法在分割性能上优于随机选择和当前最优主动选择方法。
conclusion: 支持集互补性对小样本适应至关重要，强化学习可有效优化适应集，为医疗影像分割提供了新思路。
---

## 摘要
小样本学习使得医学图像分割模型能够仅使用少量标注示例适应新任务。然而，适应性能在很大程度上取决于为支持集选择哪些示例。有效的支持集应捕捉目标域内的相关变异，并能为适应提供信息，其组成样本应提供互补信息。尽管如此，现有的主动数据选择方法大多单独对样本进行优先级排序，并未明确考虑样本间的交互作用。在本工作中，我们提出了一种用于小样本医学图像分割中支持集选择的强化学习框架，使得支持集能够联合优化，而非通过独立的样本评分。给定一组未标注的候选图像，一个智能体直接预测一个能够最大化下游分割性能的支持集。在跨机构盆腔MRI数据集上的实验表明，该方法优于随机选择和当前最先进的方法。我们的研究结果强调了支持集互补性对于有效适应的重要性，并展示了强化学习在优化适应集方面的潜力。

## Abstract
Few-shot learning enables medical image segmentation models to adapt to new tasks using only a small number of labelled examples. However, adaptation performance depends strongly on which examples are selected for the support set. Effective support sets should capture relevant variation within the target domain and be informative for adaptation, with constituent samples providing complementary information. Despite this, existing active data selection approaches largely prioritise samples individually and do not explicitly account for interactions between examples. In this work, we propose a reinforcement learning framework for support-set selection in few-shot medical image segmentation, enabling support sets to be optimised jointly rather than through independent sample scoring. Given a pool of unlabelled candidate images, an agent directly predicts a support set that maximises downstream segmentation performance. Experiments on a cross-institutional pelvic MRI dataset demonstrate improvements over random selection and current state-of-the-art methods. Our findings highlight the importance of support-set complementarity for effective adaptation and demonstrate the potential of reinforcement learning for optimising adaptation sets.

---

## 论文详细总结（自动生成）

# 论文总结：Active few-shot segmentation by reinforcing data selection

## 1. 核心问题与整体含义（研究动机与背景）
- **研究动机**：小样本学习（Few-shot learning）使医学图像分割模型能够仅用少量标注样本适应新任务，但适应性能高度依赖于**支持集（support set）** 的选择。有效的支持集应能捕捉目标域的多样性、包含信息量且样本间互补。然而，现有主动数据选择方法（如主动学习、数据价值评估）通常**独立评估单个样本**，未明确建模样本间的交互作用，导致所选支持集可能冗余、缺乏互补性。
- **整体含义**：本文首次将支持集选择视为一个**集合级优化问题**，提出强化学习（RL）框架，使智能体直接最大化下游分割性能，从而学习到兼顾覆盖性、信息性和互补性的支持集策略，显著提升小样本分割效果。

## 2. 方法论：核心思想、关键技术细节与算法流程
- **核心思想**：将支持集选择建模为强化学习问题。给定一个未标注的候选图像池（状态），RL智能体输出一个二元选择掩码（动作，约束选中数量为K），然后将所选图像标注后用于适应一个**元学习分割模型**（使用Reptile算法预训练），最后在验证集上计算分割Dice系数作为奖励信号，通过PPO算法优化策略，使智能体学会选择能最大化验证性能的支持集。
- **关键技术细节**：
  1. **元学习**：使用Reptile（一阶元学习）在多个源域（8种骨盆结构中的6种，排除NB和OB；6个机构，排除机构3和4）上预训练模型初始化参数θ*，使得模型能通过少量梯度更新快速适应新任务。
  2. **RL设置**：
     - 状态：候选池P张未标注图像的特征或原始图像（本文使用图像本身作为状态输入，具体由策略网络处理）。
     - 动作：长度为P的二元向量，和为K。
     - 奖励：将所选支持集标注后，用K张图像进行少量梯度更新得到θ'，然后计算在验证集V张图像上的平均Dice系数。
  3. **策略优化**：使用PPO算法训练策略网络，参数为φ，最大化期望奖励。
- **算法流程**（文字描述）：
  1. 元训练阶段（离线）：用Reptile训练分割模型。
  2. RL训练阶段（离线）：对每个目标域，随机采样候选池，策略网络输出选择动作；支持集标注后适应模型；在验证集上计算奖励；用PPO更新策略。
  3. 推理阶段：用训练好的策略直接从未标注候选池中选择K个图像，标注后适应模型，再对剩余图像进行分割。

## 3. 实验设计
- **数据集**：跨机构男性盆腔3D T2-weighted MRI数据集（Li et al., 2023），共589例，来自8家机构，标注8个解剖结构。实验保留**神经血管束（NB）和闭孔内肌（OI）**作为小样本目标结构（因为临床标注稀缺），并保留**机构3和4**作为未见目标域（共156例）。元训练使用剩余6个结构和其余机构数据。
- **Benchmark**：在NB和OI上评估，支持集大小K=4,8,16，候选池大小P=32。
- **对比方法**：
  1. **随机选择**（baseline）
  2. **任务优先主动学习（TBP）**：基于任务可适应性的主动学习
  3. **数据价值强化学习（DVRL）**：用RL为每个样本独立分配效用分数后选最高分
- **消融实验**：
  1. 候选池大小P=8,16,32（K固定为4）
  2. 替换元学习Backbone为MAML（与Reptile比较）

## 4. 资源与算力
- 元训练：约24小时，使用**2块Nvidia A5000 GPU**，超过10000步。
- RL训练：约72小时，同样硬件。
- 文中未提及具体显存需求，但A5000（24GB）可支持3D U-Net（256×256×32输入，8通道基础特征）。

## 5. 实验数量与充分性
- **实验数量**：主实验（K=4,8,16，NB和OI分开和联合Dice），共3×2×2=12组比较（每种方法、每个K、每个结构/联合）。另外有消融实验（3种P，2种Backbone）和定性分析。
- **充分性**：实验设计较全面：
  - 支持集大小从4到16，覆盖低资源到较高资源。
  - 对比了两种主流的主动选择方法（TBP、DVRL）和随机基线。
  - 消融研究了候选池大小和元学习算法的影响。
  - 统计显著性检验（配对t检验，p<0.01）确保了结论可靠性。
- **客观性与公平性**：对比方法均在同一元学习模型（Reptile）和相同实验设置下进行；同时验证了在MAML上的泛化性。但缺少与更多基于集合多样性方法（如BatchBALD）的对比。

## 6. 主要结论与发现
- 提出的RL支持集选择方法在所有设置下均显著优于随机、TBP和DVRL（p<0.01），在K=4时提升尤其显著（对比随机+8.1百分点，对比DVRL+4.5百分点）。
- 支持集**互补性**至关重要：对比方法因独立评分导致所选样本视觉相似，而RL策略能选到包含不同图像外观（如存在失真伪影的样本）的支持集，从而提升对域内变异（如特定扫描仪伪影）的分割准确性。
- 更大候选池（P从8增至32）带来更高性能，提供更多选择机会。
- RL策略的收益不依赖于特定元学习算法（在MAML上同样有效，虽未达统计学显著差异p=0.06，但趋势一致）。

## 7. 优点
- **方法论创新**：将支持集选择从独立样本评分转变为集合级优化，直接优化下游任务性能，是解决小样本学习中数据选择问题的自然方式。
- **考虑样本间互补性**：RL奖励函数基于整个支持集的适应结果，隐式鼓励多样性、覆盖性和互补性。
- **实验设计严谨**：使用真实跨机构医学数据集，保留稀缺结构和小样本场景；统计显著性检验；消融实验充分。
- **定性分析有洞察**：展示所选支持集包含失真图像，解释性能提升来源，验证了互补性假设。

## 8. 不足与局限
- **计算成本高**：RL训练需要72小时，且每次动作需完成完整模型适应和验证，若候选池增大或图像尺寸增大，成本可能难以承受。
- **依赖验证集**：奖励需要已标注的验证集，在现实中对新任务可能不可用（需额外标注）。
- **候选池规模有限**：实验中P最大为32，未探索更大池（>32）的性能，且RL动作空间随P指数增长，策略网络可能难以扩展。
- **仅在一个数据集上评估**：骨盆MRI数据集，结构有限（仅NB和OI），未验证在其他解剖结构或模态（如CT、超声）上的泛化性。
- **未与其他集合式主动学习方法对比**：如BatchBALD（基于贝叶斯不确定性加多样性）、CoreSet等。
- **未消融RL组件**：如策略网络架构、奖励函数设计、PPO超参数的影响。
- **元学习假设**：需要预训练和源域数据，在零源域场景下不适用；且文中仅用Reptile，未验证其他元学习方法（如MAML结果未达显著差异，需更多分析）。
- **标注成本**：支持集选择后仍需人工标注，方法与标注成本联合优化未探讨。

（完）
