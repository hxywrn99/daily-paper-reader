---
title: "DAMamba-UNet3D: A Parameter-Efficient Mamba State Space U-Net with Dynamic Adaptive Scan for 3D Medical Image Segmentation"
title_zh: DAMamba-UNet3D：一种用于3D医学图像分割的参数高效Mamba状态空间U-Net与动态自适应扫描
authors: "Mohammad Arafat Hussain, Ellen Grant, Yangming Ou"
date: 2026-07-21
pdf: "https://arxiv.org/pdf/2607.22718v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 深度学习用于3D医学图像分割
tldr: 3D医学图像分割中，U-Net缺乏全局上下文，Transformer计算开销大。本文提出DAMamba-UNet3D，一种参数高效的混合编码器-解码器，在编码器阶段引入三平面动态自适应扫描（3D-DAS）学习数据依赖重排序，其余保持卷积，仅5.3M参数。在BraTS2020五折交叉验证中，全卷评估平均Dice达0.815，参数比SegMamba少13倍；大规模变体DAMamba-L（70M）达0.829，超越SegMamba。首次将学习式DAS用于3D医学分割，证明混合架构高效。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.22718v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1263, \"height\": 494, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.22718v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1042, \"height\": 1011, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.22718v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1270, \"height\": 266, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22718v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1275, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22718v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1221, \"height\": 365, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.22718v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1049, \"height\": 385, \"label\": \"Table\"}]"
motivation: 现有SSM分割器依赖固定扫描模式和大量参数，缺乏数据自适应能力，且未涉及3D体积学习式扫描。
method: 提出三平面动态自适应扫描（3D-DAS）学习数据依赖重排序，集成到混合U-Net编码器，瓶颈和解码器保留卷积以控制参数量。
result: BraTS2020上，5.3M参数的DAMamba-UNet3D达Dice 0.815±0.013，70M的DAMamba-L达0.829±0.012，参数量为SegMamba的1/13。
conclusion: 学习式三平面DAS在混合U-Net中与固定扫描竞争力相当，大规模设计可进一步提升性能。
---

## 摘要
我们提出了用于3D医学图像分割的参数高效SSM基U-Net架构。卷积U-Net每层提供O(n)的局部混合，但缺乏显式全局上下文；Transformer以序列长度n的O(n^2)代价提供全局推理。状态空间模型（SSM），如Mamba，每块提供O(n)全局传播。然而，现有的医学SSM分割器依赖于固定扫描模式和大参数预算。动态自适应扫描（DAS）在选择性扫描前学习数据依赖的重排序，尚未应用于医学成像或扩展到3D体积。我们提出了DAMamba-UNet3D，一种混合编码器-解码器，它在编码器阶段E2-E4集成了三平面3D-DAS块，同时在其它位置保留卷积（约5.3M参数）。在BraTS 2020五折交叉验证中，DAMamba-UNet3D以比SegMamba（0.824±0.014，约70M）低约13倍的参数成本，实现了平均Dice 0.815±0.013（全卷每例评估）。在相似规模下，DAMamba-L（约70M）是一种宽DAS原生变体，具有仅编码器的DAMamba和卷积瓶颈，达到0.829±0.012，比重新训练的SegMamba高出0.5个百分点。组件消融表明，仅编码器DAS放置至关重要，因为瓶颈和解码器SSM块会降低Dice。综上，结果表明，在BraTS 2020上，混合U-Net中的学习三平面DAS与SegMamba的固定三方向Mamba（ToM）扫描具有竞争力，并且在我们的大规模设计下可能优于后者。代码：https://github.com/marafathussain/DAMamba-UNet3D。

## Abstract
We propose parameter-efficient SSM-based U-Net architectures for 3D medical image segmentation. Convolutional U-Nets afford O(n) local mixing per layer but lack explicit global context; transformers provide global reasoning at O(n^2) cost in sequence length $n$. State-space models (SSMs), such as Mamba, offer $O(n)$ global propagation per block. Yet, existing medical SSM segmenters rely on fixed scan patterns and large parameter budgets. Dynamic Adaptive Scan (DAS), which learns data-dependent reordering before selective scan, has not been applied to medical imaging or extended to 3D volumes. We propose DAMamba-UNet3D, a hybrid encoder-decoder that integrates tri-plane 3D-DAS blocks at encoder stages E2-E4 while retaining convolutions elsewhere (~5.3M parameters). On BraTS 2020 five-fold cross-validation, DAMamba-UNet3D achieves mean Dice 0.815+/-0.013 (full-volume per-case evaluation) at ~13x lower parameter cost than SegMamba (0.824+\-0.014, ~70M). At comparable scale, DAMamba-L (~70M), a wide DAS-native variant with encoder-only DAMamba and a convolutional bottleneck, reaches 0.829+\-0.012, surpassing retrained SegMamba by 0.5pt. Component ablations show that encoder-only DAS placement is critical as bottleneck and decoder SSM blocks lower Dice. Together, the results suggest that learned tri-plane DAS in a hybrid U-Net is competitive with, and under our large-scale design may improve upon, SegMamba's fixed Tri-orientated Mamba (ToM) scanning on BraTS 2020. Code: https://github.com/marafathussain/DAMamba-UNet3D.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：三维医学图像分割需要在局部边界精细刻画与全局空间上下文理解之间取得平衡，但现有方法存在矛盾：
  - 卷积 U-Net 每层计算复杂度 O(n)，但局部感受野无法捕捉全局依赖。
  - Transformer 能提供全局推理，但自注意力复杂度 O(n²)，对高分辨率 3D 体积（如 128³ 补丁含约 2.1M 体素）计算开销巨大。
  - 状态空间模型（SSM，如 Mamba）能以 O(n) 复杂度实现全局信息传播，但现有医学 SSM 分割器（如 SegMamba）依赖固定扫描模式（如 Tri-orientated Mamba, ToM），缺乏数据自适应能力，且参数量大（约 70M）。
- **整体含义**：本文旨在探索**学习式动态自适应扫描（DAS）** 在 3D 医学分割中的应用，在保持参数高效的同时，实现与固定扫描模式相当或更优的性能。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 将 **Dynamic Adaptive Scan (DAS)** 从 2D 自然图像分类扩展至 3D 医学图像分割，提出 **三平面 3D-DAS** 模块，学习数据依赖的扫描顺序，取代 SegMamba 的固定 ToM 扫描。
- 在混合 U-Net 架构中，仅在编码器较高层级（E2–E4）插入 DAS–SSM 块，瓶颈和解码器保留纯卷积，以控制参数量并保持局部细节。

### 关键技术细节
1. **动态自适应扫描（DAS）**：
   - 输入特征图经过偏移预测网络（OPN）生成每个位置的偏移量 Δp，得到采样位置 p' = p + Δp。
   - 通过双线性插值从原始特征图上采样特征，形成自适应重排的序列。
   - 该序列随后送入选择性扫描（Mamba SSM）处理，实现 O(N) 的全局混合。
2. **三平面 3D-DAS 扩展**：
   - 对 3D 特征图 X ∈ ℝ^{C×D×H×W}，分别在三个正交平面（轴向 H×W、冠状 D×W、矢状 D×H）上应用 2D DAS，然后取平均融合，得到三平面自适应重排结果。
   - 公式：DAS_3D(X) = (1/3)[DAS_ax(X) + DAS_cor(X) + DAS_sag(X)]
3. **整体架构 DAMamba-UNet3D**：
   - 4 级 U-Net，基础通道数 C=16（E1:16，E2:32，E3:64，E4:128，瓶颈:256）。
   - 编码器 E1 为卷积块；E2–E4 每个阶段包含一个卷积块（两个 3³ 卷积）后接一个三平面 DAMamba 块（conv → DAS → selective scan）。
   - 瓶颈和解码器均为纯卷积（不含 SSM）。
   - 损失函数：调制组合损失 L = (L_CE + δ) · (L_modDice + δ)，δ=0.1，γ=2，聚焦困难体素。

## 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：BraTS 2020 训练集（369 名受试者，4 模态：T1、T1ce、T2、FLAIR，标签：ET/TC/WT）。
- **评估协议**：五折交叉验证（受试者级别划分），全体积评估（滑窗推理 128³ 补丁，重叠 0.5，高斯混合），按 BraTS 官方惯例计算每例 Dice 和 HD95（空区域约定）。
- **Benchmark 与对比方法**：
  - 主要对比：SegMamba（约 70M 参数，固定 ToM 扫描）——按相同协议重新训练。
  - 基线：纯 3D U-Net（无 DAMamba）。
  - 消融变体：不同 DAMamba 放置位置（瓶颈、解码器）、不同损失函数、不同固定扫描顺序（raster, snake, cross, tri-orientated）。
  - 大规模变体：DAMamba-L（约 70M），宽度与 SegMamba 对齐，编码器堆叠两个 DAMamba 块，卷积瓶颈。

## 4. 资源与算力

- **文中未明确说明使用的 GPU 型号、数量及训练时长**。
- 仅提及训练细节：AdamW 优化器，学习率 5×10⁻⁵，余弦衰减，权重衰减 10⁻⁴，梯度裁剪 1.0，50 个 epoch。使用数据增强（随机翻转、强度偏移缩放、高斯噪声）。
- **缺陷**：缺乏算力使用的具体报告，不利于可重复性和效率评估。

## 5. 实验数量与充分性

- **实验组数**：
  - 主实验：DAMamba-UNet3D（5.3M）五折 CV（表1）。
  - 对比实验：与 SegMamba 和 DAMamba-L 对比（表2）。
  - 组件消融：4 种配置（基线 U-Net、E2–E4+bottleneck DAS、全 DAMamba、E2–E4+调制损失）（表3）。
  - 损失函数消融：2 种对比（交叉熵、CE+Dice 无调制）。
  - 扫描模式消融：5 种固定顺序 vs 学习 DAS（表4）。
- **充分性分析**：
  - **充分**：覆盖了主要的架构决策（DAS 放置位置）、损失函数、扫描顺序，且全部在相同协议下进行，使用了全体积评估而非 Patch 级指标，保证公平。
  - **不足**：仅使用 BraTS 2020 单一数据集，未在多数据集（如其他肿瘤或器官分割）上验证泛化能力；未进行统计显著性检验（如配对 t 检验）；未与更多最新方法（如 nnU-Net、Swin-UNETR）对比。
  - **客观性**：对 SegMamba 进行了重新训练（而非直接引用原文数据），保证了公平比较；但未开放本文代码（声称接收后发布）。

## 6. 论文的主要结论与发现

1. **参数高效性**：DAMamba-UNet3D（5.3M）以 SegMamba（70M）约 1/13 的参数，达到近似的平均 Dice（0.815 vs 0.824），证明了学习式 DAS 在低容量下可接近固定扫描的效果。
2. **大规模优势**：DAMamba-L（70M）达到 0.829 ± 0.012，超过重训练的 SegMamba（0.824），且在 TC 子类上提升最大（+1.5 pt），表明在更宽网络上 DAS 可能优于 ToM。
3. **编码器仅放置关键**：组件消融显示，将 DAMamba 放在瓶颈或解码器会降低 Dice（0.807 → 0.805），仅编码器放置（E2–E4）最佳（0.815），说明全局上下文应集中在编码器深层。
4. **扫描顺序重要性**：学习式 DAS（0.815）显著优于所有固定顺序（最高 0.808），验证了数据依赖扫描的有效性。
5. **损失函数影响**：调制组合损失明显优于交叉熵或未调制 Dice 损失（0.815 vs 0.798）。

## 7. 优点：方法与实验设计亮点

- **首次将 DAS 引入 3D 医学分割**，提供了学习式扫描替代固定扫描的新范式，概念创新性强。
- **三平面扩展**巧妙且简单：在三个正交平面分别应用 2D DAS 再平均，避免了直接设计 3D 偏移预测的复杂度。
- **参数效率设计合理**：仅编码器高层使用 SSM，瓶颈和解码器保持卷积，既获得全局信息又避免参数激增。
- **实验规范**：采用五折交叉验证、全体积评估、损失函数和训练协议与 SegMamba 对齐，确保公平对比。
- **提供代码仓库**（目前为占位，但承诺开放）。

## 8. 不足与局限

- **数据集单一**：仅在 BraTS 2020 上评估，未验证在其他肿瘤分割（如肝脏、肾脏）或器官分割任务上的泛化性，结论推广性受限。
- **与 SOTA 对比不足**：未与 nnU-Net、Swin-UNETR、UTNet 等主流方法比较，也未报告 HD95 在 SegMamba 和 DAMamba-L 上的对应数值（仅有 DAMamba-UNet3D 的 HD95 数据）。
- **统计显著性缺乏**：未进行假设检验（如配对检验或置信区间），报告仅给出均值±标准差，无法判断差异是否统计显著。
- **未报告训练/推理时间**：无法评估计算效率的实际增益（虽然理论上 O(N) vs O(N²)）。
- **DAMamba-L 与 SegMamba 差异多维**：DAMamba-L 不仅扫描方式不同，U-Net 深度、块堆叠、瓶颈设计也不同，因此改进不能完全归因于 DAS。
- **代码尚未公开**：论文声明代码将“在接受后”发布，当前无法复现结果。

（完）
