---
title: Parameter-Efficient Adaptation of SAM3 for Prompt-Driven Surgical Concept Segmentation
title_zh: 用于提示驱动的手术概念分割的SAM3参数高效适配
authors: "Changjing Liu, Yiming Huang, Beilei Cui, Liangjing Shao, Long Bai, Yanheng Li, Haoxuan Che, Hongliang Ren"
date: 2026-07-26
pdf: "https://arxiv.org/pdf/2607.23694v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 手术概念分割与LoRA适配
tldr: "SAM3在自然图像上表现优异，但手术数据存在域差距导致分割精度下降，且现有医疗SAM方法全参数微调计算开销大。本文提出基于LoRA的参数高效适配，仅优化0.98%的模型参数，支持单消费级GPU训练。在手术概念分割任务中，该方法一致优于零样本SAM3和主流基线。分割结果可直接用于下游机器人手术场景重建和物理仿真，实现了轻量高效的手术分割。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.23694v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1241, \"height\": 249, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23694v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1254, \"height\": 388, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23694v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1264, \"height\": 830, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23694v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1112, \"height\": 635, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.23694v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1250, \"height\": 426, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.23694v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1273, \"height\": 448, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.23694v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1255, \"height\": 460, \"label\": \"Table\"}]"
motivation: 解决SAM3在手术数据上的域差距以及全参数微调带来的高计算开销与低效率问题。
method: "向SAM3的提示编码器、检测器和跟踪器注入低秩适配器，冻结视觉骨干，仅优化0.98%参数。"
result: 在手术概念分割任务上，性能一致超越零样本SAM3和其他主流基线。
conclusion: 提出的参数高效适配方法支持单GPU训练，分割结果可直接用于下游机器人手术场景重建和物理仿真。
---

## 摘要
高效的手术分割有助于临床诊断、术中监测以及下游的机器人重建与模拟流程。尽管像Segment Anything Model 3（SAM3）这样的提示驱动基础模型在自然图像上取得了强大的分割性能，但手术数据与其预训练数据存在领域差距，导致分割精度下降。此外，现有的医学SAM方法需要全参数微调，导致计算消耗大且效率低。为解决这些局限，本文提出了一种参数高效的低秩适配（LoRA）方法，对SAM3进行适配，用于手术概念分割。我们将低秩适配器注入提示编码器、检测器和跟踪器，同时完全冻结视觉主干，仅优化模型总参数的0.98%，并支持在单个消费级GPU上进行训练。综合实验表明，我们的方法始终优于零样本SAM3和其他主流基线，生成的分割结果可直接部署，以支持下游的机器人手术场景重建和物理模拟流程。

## Abstract
Efficient surgical segmentation empowers clinical diagnosis, intraoperative monitoring, and downstream robotic pipelines for reconstruction and simulation. Although prompt-driven foundation models like Segment Anything Model 3 (SAM3) achieve strong segmentation performance on natural images, surgical data exhibits domain gaps against its pre-training data, resulting in degraded segmentation accuracy. Furthermore, existing medical SAM methods require full-parameter fine-tuning, incurring heavy computational consumption and low efficiency. To address these limitations, this work proposes a parameter-efficient Low-Rank Adaptation (LoRA) adaptation of SAM3 for surgical concept segmentation. We inject low-rank adapters into the prompt encoder, detector and tracker while fully freezing the vision backbone, which only optimizes 0.98% of the total model parameters and supports training on a single consumer GPU. Comprehensive experiments demonstrate that our method consistently outperforms zero-shot SAM3 and other mainstream baselines, and the generated segmentation results can be directly deployed to support downstream robotic surgical scene reconstruction and physical simulation pipelines.

---

## 论文详细总结（自动生成）

# 论文详细总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：
  - 手术概念分割是临床诊断、术中监测及下游机器人重建/仿真的基础，但现有方法泛化性不足。
  - 大模型（如SAM3）在自然图像上表现优异，但手术数据与预训练数据存在**域差距**，导致分割精度下降。
  - 现有的医学SAM方法（如MedSAM、Medical SAM3）采用**全参数微调**，需要大量GPU内存（约80 GB），难以在消费者级GPU上部署，且可能破坏预训练表征。

- **研究动机**：
  - 探索**参数高效微调（PEFT）** 方法，特别是LoRA，仅更新极少参数以实现手术场景的跨域迁移。
  - 在保持SAM3视觉编码能力的同时，高效适应手术概念，降低计算开销。

- **整体含义**：
  - 首次将LoRA应用于SAM3的手术概念分割，在仅更新0.98%参数、单GPU（9GB显存）条件下，性能显著优于零样本SAM3和全参数微调的Medical SAM3。
  - 分割输出可直接用于下游的**高斯泼溅场景重建**和**物理仿真**，形成从分割到仿真的完整闭环。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 使用**低秩适配（LoRA）** 冻结SAM3的视觉骨干（图像编码器），仅对**提示编码器、DETR检测器、跟踪器**注入低秩矩阵，实现参数高效适配。

### 关键技术细节
1. **统一输入表示**：
   - 将CholecSeg8k、EndoVis18、CaDISv2三个数据集的类别标签映射到**共享概念词汇表V**（如“抓钳”、“电凝钩”），支持跨数据集共享权重，无需数据集特异性调优。

2. **LoRA注入位置**：
   - **提示编码器**：对齐手术术语（如l-hook electrocautery）与预训练文本嵌入。
   - **DETR检测器**：在注意力投影（q, k, v, out_proj）中添加LoRA，处理罕见手术器械。
   - **跟踪器**：在跟踪注意力层添加LoRA，稳定跨帧掩码传播。
   - **冻结部分**：视觉编码器（其低层特征对手术数据泛化良好）及其他所有组件。

3. **LoRA公式**：
   - 对于冻结权重 \(W_0 \in \mathbb{R}^{d \times k}\)，更新权重为 \(W = W_0 + BA\)，其中 \(B \in \mathbb{R}^{d \times r}\)，\(A \in \mathbb{R}^{r \times k}\)，秩 \(r \ll \min(d,k)\)。仅优化A和B。

4. **训练配置**：
   - 秩 \(r=16\)，缩放因子 \(\alpha=32\)，dropout 0.1。
   - bf16混合精度，batch size=1，梯度累积=8。
   - 优化器：AdamW（\(\beta_1=0.9,\beta_2=0.999,\epsilon=10^{-8}\)），权重衰减0.01。
   - 学习率：余弦退火，初始 \(10^{-4}\)，200步warmup，梯度裁剪1.0。
   - 训练10个epoch，选择验证集最高mIoU的checkpoint。

## 3. 实验设计

### 数据集与场景
- **CholecSeg8k**：胆囊切除术（腹腔镜），含肝脏、抓钳、电凝钩等类别。
- **EndoVis18**：肾切除（机器人），含器械腕部、器械轴、肾实质等。
- **CaDISv2**：白内障手术，含多种器械和组织。
- 测试集：每个数据集保留若干视频序列（如CholecSeg8k: 01_00080, 01_00240, 01_15019, 12_15750；EndoVis18: Seq_5, Seq_9；CaDISv2: videos 2,12,22）。

### 对比方法
- **零样本基线**：CLIP、SurgVLP、CAT-Seg、SAM3（原始版本）。
- **全参数微调基线**：Medical SAM3（2D和3D版本，在33个多模态医学数据集上训练）。
- 所有方法在相同测试集上评估。

### 评估指标
- 每个类别的**平均交并比（mIoU）** 和**Dice系数**，最终平均。

### 实验数量与充分性
- **定量实验**：在三个不同手术数据集上报告了mIoU（主要结果，表1、2）和Dice（图4汇总），覆盖多个测试序列和类别。
- **定性实验**：图3展示了分割可视化对比（SAM3、Medical SAM3 vs 本文方法）。
- **下游应用验证**：图5展示了分割结果用于高斯泼溅重建和MPM物理仿真的示例。
- **消融分析**：**未进行**不同LoRA配置（如秩、位置、组件）的消融。仅通过对比全参数微调（Medical SAM3）和零样本SAM3间接证明LoRA有效性。
- **公平性评价**：在所有对比方法中，采用相同训练/测试分割，但Medical SAM3为全参数微调，本文方法仅更新0.98%参数，比较尺度不完全对等，但凸显了参数高效的优越性。

## 4. 资源与算力

- **GPU型号**：单张 **NVIDIA RTX 3090**（消费级GPU，24 GB显存）。
- **峰值显存**：仅 **9 GB**（相比全参数微调约80 GB，降低>80%）。
- **训练参数**：总参数849M，可训练参数8.32M（0.98%）。
- **训练时长**：未明确给出具体时间，但10个epoch、batch size=1、梯度累积=8，可推断在单卡上数小时内完成。

## 5. 实验数量与充分性

- **覆盖范围**：三个不同手术类型（胆囊、肾脏、白内障），共9个测试视频序列，约20个类别，实验场景较为全面。
- **对比方法**：包含6种主流/最新基线（CLIP、SurgVLP、CAT-Seg、SAM3、Medical SAM3 2D/3D），数量合理。
- **缺失消融**：
  - 未探索不同LoRA秩（r=8、32等）的影响。
  - 未单独消去检测器或跟踪器中的LoRA组件。
  - 未对比其他PEFT方法（如Adapter、Prefix Tuning）。
- **统计显著性**：未报告标准差或多轮重复实验，结果可能受单次训练影响。
- **下游量化**：仅定性展示分割结果用于重建和仿真，未定量评估分割误差对下游性能的影响。

## 6. 论文的主要结论与发现

- **结论1**：LoRA适配SAM3在手术概念分割上显著优于零样本SAM3和全参数微调的Medical SAM3，特别是在细粒度手术器械（如抓钳、电凝钩）上。
- **结论2**：仅更新0.98%参数，训练内存仅9 GB，单消费者GPU即可完成，适合资源受限的临床部署。
- **结论3**：统一输入表示支持跨数据集共享权重，无需为每个数据集单独训练。
- **结论4**：生成的分割结果可直接驱动下游的**高斯泼溅重建**和**物理仿真**，验证了实用价值。

## 7. 优点（方法与实验设计亮点）

- **极致的参数效率**：仅微调0.98%参数，大幅降低内存和计算需求，为大规模基础模型在医疗手术领域的落地提供了可行路径。
- **统一输入表示**：将不同手术数据集的类别映射到共享词汇，实现了跨数据集的单权重训练，减少了重复调优成本。
- **实际下游验证**：不仅停留在分割精度，还展示了从分割到重建、再到物理仿真的完整流程，增强了方法的实用性。
- **对比充分**：在三个数据集、多种基线上测试，结果具有说服力。特别是揭示了全参数微调的Medical SAM3在手术数据上反而退化，凸显了参数高效微调的优势。

## 8. 不足与局限

- **缺乏消融实验**：未对LoRA的秩、注入位置、组件选择进行系统消融，无法证明当前配置是最优的。
- **实验统计性不足**：未报告多轮实验的平均值和方差，结果可能存在随机性。
- **仅静态图像**：当前方法基于单帧图像，未扩展到视频序列（尽管SAM3支持视频，但本文只做了图像级微调）。作者提及将是未来工作。
- **下游量化缺失**：分割精度对重建和仿真的影响未量化，仅展示定性结果，难以评估实际应用中的可靠性。
- **数据集局限性**：仅使用三个公开手术数据集，且均为腹腔镜/内窥镜场景，未覆盖开放性手术或更多复杂器械。
- **可复现性**：代码已开源，但未提供预训练权重或完整的训练日志；图5中的物理仿真参数设置未详细说明，可能存在可复现挑战。

（完）
