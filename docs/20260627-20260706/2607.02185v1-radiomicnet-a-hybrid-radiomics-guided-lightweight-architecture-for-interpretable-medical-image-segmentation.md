---
title: "RadiomicNet: A Hybrid Radiomics-Guided Lightweight Architecture for Interpretable Medical Image Segmentation"
title_zh: RadiomicNet：一种用于可解释医学图像分割的混合放射组学引导轻量级架构
authors: Mohammad Amanour Rahman
date: 2026-07-02
pdf: "https://arxiv.org/pdf/2607.02185v1"
tags: ["query:medical-seg"]
score: 9.0
evidence: 混合放射组学引导的深度学习医学图像分割
tldr: 针对深度学习在医学图像分割中参数多、可解释性差的问题，提出RadiomicNet双流轻量架构。通过Radiomics Attention Gate将GLCM和LBP特征融入跳跃连接注意力，实现事前可解释性。Radiomics Consistency Loss降低校准误差。在BUSI和Kvasir-SEG数据集上DSC达0.763和0.854，参数仅3.27M，显著优于U-KAN。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.02185v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 910, \"height\": 717, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02185v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 920, \"height\": 305, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.02185v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 911, \"height\": 727, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.02185v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 933, \"height\": 261, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02185v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 933, \"height\": 260, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02185v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 933, \"height\": 345, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02185v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 612, \"height\": 165, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.02185v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 748, \"height\": 262, \"label\": \"Table\"}]"
motivation: 现有深度学习医学图像分割模型参数庞大、数学不透明且缺乏临床可解释性，需在轻量化与可解释性间取得平衡。
method: 基于MobileNetV2编码器-解码器，设计Radiomics Attention Gate融合GLCM和LBP特征调制注意力，并引入Radiomics Consistency Loss对齐纹理复杂度与预测不确定性。
result: BUSI上DSC 0.763±0.231，Kvasir-SEG上0.854±0.112，参数3.27M，比U-Net减少9.5倍，ECE从0.142降至0.118。
conclusion: 结合领域知识的紧凑可解释模型能以更低计算开销实现先进分割性能，为临床提供有意义的解释依据。
---

## 摘要
深度学习在医学图像分割中取得了显著性能，但仍存在关键局限性：数学难解性、大量参数需求以及缺乏临床可解释性。我们提出RadiomicNet，一种新颖的双流混合架构，通过将手工制作的放射组学特征直接集成到分割学习过程中，增强了标准深度学习。关键贡献是放射组学注意力门（RAG），它利用灰度共生矩阵（GLCM）和局部二值模式（LBP）特征来调节基于轻量级MobileNetV2的编码器-解码器中的跳跃连接注意力，提供事前可解释性，无需事后近似。一种新颖的放射组学一致性损失进一步强化了纹理复杂度与预测不确定性之间的对齐，将期望校准误差（ECE）从0.142降低到0.118。RadiomicNet在乳腺超声图像（BUSI）数据集上达到0.763±0.231的Dice相似系数（DSC），在Kvasir-SEG上达到0.854±0.112，分别比U-KAN高出1.2%和1.8%（p<0.05，Wilcoxon符号秩检验），且仅需3.27M参数，比标准U-Net少9.5倍，比U-KAN少4.3倍。基于梯度的特征重要性分析显示，GLCM不相似性（15.24%）、GLCM能量（14.56%）和LBP熵（11.49%）是主要的放射组学线索，为分割决策提供了临床上有意义的解释。所提出的方法表明，基于领域知识的紧凑、可解释模型能够在显著降低计算开销的同时提供最先进的分割性能。

## Abstract
Deep learning has achieved remarkable performance in medical image segmentation, yet it suffers from critical limitations: mathematical intractability, substantial parameter requirements, and lack of clinical interpretability. We propose RadiomicNet, a novel two-stream hybrid architecture that enhances standard deep learning by integrating handcrafted radiomics features directly into the segmentation learning process. The key contribution is the Radiomics Attention Gate (RAG), which leverages Gray-Level Co-occurrence Matrix (GLCM) and Local Binary Pattern (LBP) features to modulate skip-connection attention in a lightweight MobileNetV2-based encoder-decoder, providing ante-hoc interpretability without post-hoc approximations. A novel Radiomics Consistency Loss further enforces alignment between texture complexity and prediction uncertainty, reducing Expected Calibration Error (ECE) from 0.142 to 0.118. RadiomicNet achieves a Dice Similarity Coefficient (DSC) of 0.763 +/- 0.231 on the Breast Ultrasound Images (BUSI) dataset and 0.854 +/- 0.112 on Kvasir-SEG, outperforming U-KAN by 1.2% and 1.8%, respectively (p < 0.05, Wilcoxon signed-rank test), with only 3.27M parameters, 9.5x fewer than standard U-Net and 4.3x fewer than U-KAN. Gradient-based feature importance analysis reveals that GLCM dissimilarity (15.24%), GLCM energy (14.56%), and LBP entropy (11.49%) are the dominant radiomics cues, providing clinically meaningful explanations for segmentation decisions. The proposed approach demonstrates that compact, interpretable models grounded in domain knowledge can deliver state-of-the-art segmentation performance with substantially reduced computational overhead.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：当前深度学习医学图像分割模型（如U-Net及其变体）虽然性能优异，但存在三个关键局限：(1) **数学难解性**——预测无法追溯到人类可理解的特征；(2) **参数效率低**——标准U-Net需31M参数，不利于边缘部署；(3) **数据饥渴**——在标注有限时性能显著下降。此外，模型缺乏临床可解释性，阻碍了临床落地。
- **整体含义**：论文旨在解决“轻量化”与“可解释性”之间的平衡问题，提出一种融合手工放射组学特征的混合架构，实现**事前可解释性**（ante-hoc interpretability）和**参数高效**的分割网络，满足LBDL（学习超越深度学习）研讨会的号召——优先考虑可解释性、小模型、低计算复杂度和高性能。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 核心思想
- 采用**双流架构**：流1为轻量级MobileNetV2编码器-解码器处理RGB图像；流2为轻量级MLP处理13个手工放射组学特征。两流通过**放射组学注意力门（RAG）**在每个解码器层级融合，实现事前可解释性。
- 引入**放射组学一致性损失（Radiomics Consistency Loss, LRC）**，强制对齐纹理复杂度与预测不确定性，提升模型校准。

### 关键技术细节
1. **放射组学特征提取**：
   - 提取13个特征，分三类：
     - **GLCM纹理统计**（5个）：对比度、不相似性、同质性、能量、相关性（d={1,3}, θ={0°,45°,90°,135°}, 64量化级）。
     - **LBP结构描述符**（4个）：均值、标准差、熵、均匀性（半径=3，P=24点，均匀方法）。
     - **全局强度统计**（4个）：均值、标准差、偏度、峰度。
   - 特征向量 r∈ℝ¹³ 通过放射组学流MLP映射为32维嵌入 ˆr：
     \[
     \hat{\mathbf{r}} = \text{LayerNorm}(\text{ReLU}(\mathbf{W}_2\cdot \text{ReLU}(\mathbf{W}_1\mathbf{r}+\mathbf{b}_1)+\mathbf{b}_2))
     \]

2. **放射组学注意力门（RAG）**：
   - 在每个解码器层级ℓ，接收跳跃连接特征Fℓ和ˆr，生成调制后的特征：
     - **通道注意力**来自放射组学：αc = σ(Wc2·ReLU(Wc1ˆr))
     - **空间注意力**来自特征统计：αs = σ(Conv7×7(Fℓ))
     - **门控融合**：F'ℓ = Fℓ ⊙ [γ·αc·αs + (1-γ)·αs]，其中γ为可学习标量（∈[0,1]），表示放射组学注意力与特征驱动注意力的相对贡献。
   - γ本身即为一种可解释量。

3. **复合损失函数**：
   \[
   \mathcal{L} = \lambda_1 \mathcal{L}_{BCE} + \lambda_2 \mathcal{L}_{Dice} + \lambda_3 \mathcal{L}_{bnd} + \lambda_4 \mathcal{L}_{RC}
   \]
   - λ1=0.5, λ2=0.5, λ3=0.1, λ4=0.05。
   - **边界加权BCE**（Lbnd）：膨胀因子α=3，加重边界附近错误的惩罚。
   - **放射组学一致性损失（LRC）**：
     \[
     \mathcal{L}_{RC} = \text{MSE}\left(\bar{H}(p), e^{f_1}\right)
     \]
     其中¯H(p)为预测平均熵，e^{f1}为归一化GLCM对比度。强制高纹理复杂度区域对应高预测不确定性，嵌入临床先验（乳腺超声中纹理复杂度与诊断不确定性相关）。

4. **网络架构与参数**：总参数3.27M：MobileNetV2骨干（去除原始分类头）约2.20M；轻量级解码器、RAG模块（4个层级）和放射组学流MLP共约1.07M。

## 3. 实验设计：数据集、基准对比与方法

### 数据集
- **BUSI**（乳腺超声图像）：647幅图像（437良性，210恶性），像素级病变掩码。按类别分层划分为452/97/98（训练/验证/测试）。
- **Kvasir-SEG**（结肠镜图像）：1000幅息肉分割图像。使用官方训练/验证划分，保留测试集。

### Benchmark与对比方法
- 对比方法：U-Net、UNet++、TransUNet、U-KAN，涵盖CNN、Transformer、KAN三大范式。所有基线使用原始架构，采用相同数据划分和增强协议。
- 评估指标：DSC、IoU、Precision、Recall、Specificity（均值±标准差），统计显著性采用配对Wilcoxon符号秩检验（基于每张图像DSC vs U-KAN）。

### 实验结果
- **BUSI**：RadiomicNet DSC=0.763±0.231，IoU=0.662，比U-KAN高1.2% DSC、1.7% IoU（p<0.05）。
- **Kvasir-SEG**：DSC=0.854±0.112，IoU=0.783，比U-KAN高1.8% DSC、3.8% IoU（p<0.05）。
- 参数仅3.27M（U-Net的1/9.5，U-KAN的1/4.3），推理速度8.85ms/图像（113 images/sec）。

## 4. 资源与算力

- 论文仅提及“所有实验在一个NVIDIA GPU上运行”，**未明确说明GPU型号、数量及具体训练时长**。仅报推理时间8.85ms/幅（单GPU）。
- 训练配置：编码器初始学习率1×10⁻⁵，解码器1×10⁻⁴，AdamW优化器（权重衰减10⁻⁴），线性预热5个epoch后余弦退火，批大小8，梯度裁剪1.0，早停耐心20。具体训练轮数未给出。

## 5. 实验数量与充分性

### 实验组数
- **主要对比**：在BUSI和Kvasir-SEG两个数据集上，与4种基线（U-Net、UNet++、TransUNet、U-KAN）对比，共2×4=8组对比实验。
- **消融实验**（表III）：
  - 组件级消融：移除RAG（MobileNetV2基线）、移除LRC、移除Lbnd共3组。
  - RAG流分解：仅空间注意力、仅通道注意力、完整RAG共3组。
  - 共6组消融。
- **校准分析**（表IV）：比较有无LRC的ECE（2组）。
- **计算效率**（表V）：参数、模型大小、推理时间对比（但只列出RadiomicNet的推理时间，其他未提供）。
- 未进行跨模态（如CT、MRI）或更多数据集的实验。

### 充分性评价
- **优点**：消融实验设计合理，拆解了RAG各组件和损失项的贡献；使用了统计显著性检验（Wilcoxon）保证结果可靠性；在两种不同模态（超声与内镜）上验证了泛化性。
- **不足**：
  - 仅覆盖2个数据集，且数据集规模较小（BUSI 647张，Kvasir-SEG 1000张），**缺乏大规模或3D医学图像（如CT/MRI）的验证**。
  - 未提供与其他轻量级方法（如EfficientNet-based U-Net、Attention U-Net等）的对比。
  - 无超参数敏感性分析。
  - 无跨中心或噪声鲁棒性实验。
  - 可解释性分析仅以梯度重要性图展示，缺少与医生判读的对照或用户研究。

## 6. 论文的主要结论与发现

- **主要结论**：将放射组学特征作为结构性先验嵌入深度学习，能同时实现参数高效、可解释且竞争性的分割性能。RadiomicNet在乳腺超声和息肉分割任务上以极少的参数（3.27M）超越U-KAN等更大型模型（p<0.05），且通过LRC损失降低校准误差（ECE从0.142降至0.118）。
- **关键发现**：
  - 梯度分析表明GLCM不相似性（15.24%）、GLCM能量（14.56%）、LBP熵（11.49%）是主导的放射组学线索，与临床认知一致（恶性病变有更高的纹理异质性）。
  - 可学习门控标量γ提供可解释的放射组学/特征注意力比例。
  - 前置可解释性（ante-hoc）优于事后近似（post-hoc）方法，因为放射组学特征是数学定义、临床验证且固定不变的。

## 7. 优点

1. **创新性融合范式**：将手工放射组学特征（GLCM/LBP）直接用于调制跳跃连接注意力，而非传统的后处理或分类任务，实现了真正的结构级可解释性。
2. **轻量化设计**：仅3.27M参数，推理速度达113 images/sec，适合边缘部署。
3. **校准损失新颖**：Radiomics Consistency Loss将临床先验（纹理复杂度与不确定性相关）编码为目标函数，显著降低ECE，提升模型可靠性。
4. **跨域验证**：在乳腺超声和结肠镜两个不同模态上均取得显著改进，且统计检验严格（p<0.05）。
5. **消融实验完整**：逐层拆解了RAG各子模块和每个损失项的贡献，证明核心组件不可或缺。
6. **可解释性双重保障**：架构本身是ante-hoc（可追溯至命名特征），同时提供梯度诊断用于验证。

## 8. 不足与局限

1. **2D局限性**：模型仅处理2D切片，未扩展到3D（如CT/MRI体积），限制了在三维医学图像中的应用。
2. **放射组学特征固定**：13个特征为手工预定义，未设计可微特征选择机制，可能忽略更优特征子集。
3. **数据集规模与多样性有限**：仅使用两个中等规模数据集（最大值1000张），未在大型公开基准（如BraTS、LiTS）或CT/MRI上验证，泛化性证据不足。
4. **缺乏与同量级轻量模型的对比**：未与EfficientNet、MobileNetV3-based分割网络、nnU-Net（轻量版）等公平对比。
5. **可解释性验证偏弱**：仅以梯度归因图示意，未进行量化评估（如与放射科医师标注一致性、用户研究），也未与LIME/SHAP等事后方法比较。
6. **计算资源报告不充分**：未提供GPU型号、训练时长、能耗等，难以复现和评估实际成本。
7. **校准分析维度单一**：仅报告ECE，未提供可靠性图、Brier Score等，也未分析不同置信区间的校准细节。
8. **代码未提及开源**：论文未提供代码仓库链接，影响可复现性。

（完）
