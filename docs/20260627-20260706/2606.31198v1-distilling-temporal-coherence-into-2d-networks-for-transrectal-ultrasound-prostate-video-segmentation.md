---
title: Distilling Temporal Coherence into 2D Networks for Transrectal Ultrasound Prostate Video Segmentation
title_zh: 将时间一致性蒸馏到二维网络用于经直肠超声前列腺视频分割
authors: "Dong Yeong Kim, JunGyu Lee, Jaewon Choi, June Young Seo, Myeongseop Kim, Jinwook Choi, Taek Min Kim, Young-Gon Kim"
date: 2026-06-30
pdf: "https://arxiv.org/pdf/2606.31198v1"
tags: ["query:medical-seg"]
score: 9.0
evidence: 深度学习用于经直肠超声前列腺视频分割，并融合时间一致性
tldr: 针对TRUS前列腺视频分割中2D网络忽略时域导致帧间抖动、3D网络延迟高的问题，提出时间连贯蒸馏框架，在训练时将时域一致性蒸馏至2D网络，推理保持单帧高效。利用前列腺几何稳定而周围声学环境波动的临床观察，设计基于光流残差的置信加权时间一致性损失，选择性抑制不可靠区域梯度；并引入双尺度原型对齐模块对比优化局部边界与全局语义。实验在SUN-SEG和TRUS-V基准上实现实时速度下的最优精度与时间一致性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.31198v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1232, \"height\": 383, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31198v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1222, \"height\": 573, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31198v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1239, \"height\": 515, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.31198v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1266, \"height\": 641, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.31198v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1266, \"height\": 486, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31198v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 631, \"height\": 379, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.31198v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 629, \"height\": 257, \"label\": \"Table\"}]"
motivation: 现有2D分割忽略时域产生帧间不一致，而3D架构延迟无法满足实时干预需求。
method: 置信加权时间一致性损失（基于光流残差）结合双尺度原型对齐模块，辅以几何等变伪标签知识蒸馏。
result: 在SUN-SEG和TRUS-V（2679帧）上达到实时推理下的分割精度与时间一致性SOTA。
conclusion: 提出高效时间一致分割框架，为TRUS引导介入提供实时稳定视频分割方案。
---

## 摘要
经直肠超声(TRUS)中前列腺的实时视频分割对于图像引导干预至关重要。传统二维方法忽略时间上下文导致帧间不一致，而三维架构则带来难以承受的延迟。为解决这一困境，我们提出一种时间一致性学习框架，在训练时将时间一致性蒸馏到二维网络中，同时保持单帧推理效率。我们的设计基于一个关键临床观察：前列腺表现出几何稳定性，而周围的声学环境因生理运动和探头压力而波动。由于传统时间约束会从这些不稳定区域传播错误梯度，我们引入基于光流扭曲残差的置信加权时间一致性目标，选择性地衰减不可靠区域的贡献。为补充这一像素级约束，双尺度原型对齐模块通过对比优化局部边界和全局语义特征来强制执行语义一致性。此外，为消除对密集逐帧视频标注的需求，我们采用基于几何等变性的伪标注，并结合预训练教师模型的知识蒸馏。在SUN-SEG和我们新引入的TRUS-V基准(2679帧)上的大量实验表明，该方法在实时速度下达到了最先进的准确性和时间一致性。代码和数据集可在https://github.com/DYDevelop/DTC-TRUS获取。

## Abstract
Real-time video segmentation of the prostate in Transrectal Ultrasound (TRUS) is essential for image-guided interventions. While conventional 2D methods suffer from inter-frame inconsistencies by disregarding temporal context, 3D architectures incur prohibitive latency. To resolve this dilemma, we present a Temporally Consistent Learning Framework that distills temporal coherence into a 2D network during training, preserving single-frame inference efficiency. Our design is driven by a key clinical observation: the prostate exhibits geometric stability, whereas the surrounding acoustic environment fluctuates due to physiological motion and transducer pressure. Because conventional temporal constraints propagate erroneous gradients from these unstable regions, we introduce a Confidence-Weighted Temporal Consistency objective derived from optical flow warping residuals, selectively attenuating contributions from unreliable regions. Complementing this pixel-wise constraint, a Dual-scale Prototype Alignment Module enforces semantic coherence through contrastive optimization of local boundary and global semantic features. Furthermore, to eliminate the need for dense per-frame video annotations, we employ geometric equivariance-based pseudo-labeling with knowledge distillation from a pretrained teacher. Extensive experiments on SUN-SEG and our newly introduced TRUS-V benchmark (2,679 frames) demonstrate state-of-the-art accuracy and temporal consistency at real-time speed. Code and dataset are available at https://github.com/DYDevelop/DTC-TRUS.

---

## 论文详细总结（自动生成）

### 论文详细中文总结

#### 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究动机**：经直肠超声（TRUS）前列腺视频分割是活检、近距离放疗等图像引导介入中的关键环节。现有深度学习方法主要分为两类：
  - **2D 静态模型**（如 U-Net、ACSNet）计算效率高，但将视频帧视为独立图像，忽略时域上下文，导致帧间预测不一致（“闪烁”伪影），影响临床跟踪可靠性。
  - **3D / 时序模型**（如 3D CNN、RNN 或带时序模块的 2D 网络）虽能捕获时空特征，但计算延迟大，无法满足术中实时性要求（低延迟反馈是硬约束）。
- **核心问题**：如何在保持 2D 网络推理效率的前提下，有效引入时域一致性，消除闪烁，同时避免对密集视频标注的依赖。
- **整体含义**：论文针对这一“效率-一致性”矛盾，提出一种训练时蒸馏时域知识到 2D 网络的新框架，使推理时仍为高效单帧 2D 模型，且达到或超越专用视频模型的性能。

#### 2. 论文提出的方法论：核心思想、关键技术细节

- **核心思想**：基于临床观察——前列腺几何结构相对稳定，而周围声学环境因生理运动或探头压力剧烈波动。因此，传统时域约束（如简单光流一致性）会从不可靠背景区域传播错误梯度。论文据此设计：
  1. **置信加权时域一致性损失**：选择性衰减不稳定区域的梯度贡献。
  2. **双尺度原型对齐模块**：在特征空间强制局部边界和全局语义一致性。
  3. **自监督伪标签 + 知识蒸馏**：无需逐帧视频标注，仅利用预训练静态教师模型和几何等变伪标签驱动学生模型训练。

- **关键技术细节**：
  - **置信加权时域一致性 (Confidence-Weighted Temporal Consistency)**  
    使用 Farneback 密集光流估计相邻帧运动，将前帧分割掩模 `P_{t-1}` 扭曲到当前帧坐标系得到 `~P_{t-1→t}`。计算非遮挡置信图 `M_noc = exp(-|I_t - W(I_{t-1}, F_{t-1→t})|)`。损失函数定义为：
    ```
    L_con = 1 - (1/N) ∑_p M_noc(p)·[1 - |P_t(p) - ~P_{t-1→t}(p)|]
    ```
    当 `M_noc≈1`（可靠区域）且预测一致时损失小；当 `M_noc≈0`（遮挡/不稳定区域）时贡献被抑制。
  - **双尺度原型对齐模块 (Dual-scale Prototype Alignment)**  
    从学生网络的瓶颈层（全局特征）和最后一层隐藏层（局部边界特征）提取前景/背景原型。对局部特征使用前景原型进行扭曲后对齐（保持边界细节）；对全局特征使用背景原型直接对比（稳定场景语义）。损失函数：
    ```
    L_proto = w_cb·[1 - cos(~v_loc_cf,t-1, v_loc_cf,t)] + w_cf·[1 - cos(v_glob_cb,t-1, v_glob_cb,t)]
    ```
    其中 `w_cb, w_cf` 为背景/前景面积比率，可动态平衡不同尺度。
  - **自监督等变伪标签与知识蒸馏**  
    学生模型对视频帧进行几何变换（如翻转），预测结果经逆变换后生成伪标签，采用停梯度技巧训练。同时，将预训练于静态图像的教师网络冻结，用 ℓ2 损失蒸馏其 logits 到学生，防止遗忘空间先验。
  - **总损失**：
    ```
    L_total = λ_seg L_seg + λ_KD L_KD + λ_con L_con + λ_proto L_proto
    ```
    超参数：λ_seg=λ_KD=λ_proto=1.0, λ_con=0.25。
  - **推理时**：仅使用学生 2D 网络，无需光流和教师，保持单帧高效。

#### 3. 实验设计：数据集、Benchmark、对比方法

- **数据集**：
  - **SUN-SEG**（公共基准）：大规模视频息肉分割数据集（158,690 帧），含“Easy”和“Hard”子集，用于验证通用视频分割性能。
  - **TRUS-V**（新引入的内部基准）：2,679 帧前列腺 TRUS 视频，来自 10 位患者，包含轴位和矢状位双视图，共 20 个连续序列。训练/测试按患者分离（2,400 训练 / 279 测试）。注释采用半自动策略（教师 U-Net 生成候选 + 放射科医生逐帧修正），保证了高可靠性。
- **对比方法**：
  - **2D 图像模型**：U-Net、U-Net++、ACSNet、PraNet、ColonSegNet、MedNeXt、MSRF-Net、TransNetR。
  - **视频模型**：SSTAN、FLA-Net、DALA（均含时域交互模块）。
- **评估指标**：Structure (Sα)、Enhanced-alignment (E_mn^φ)、加权 F-measure (F_w^β, F_mn^β)、Dice、Sensitivity。

#### 4. 资源与算力

- **明确说明**：单张 NVIDIA A100 40GB GPU 训练。
- **训练细节**：教师模型预训练于静态图像（2,140 轴位 + 2,260 矢状位）；学生模型使用 AdamW 优化器（lr=1×10⁻⁶, batch size=8），仅训练 1 个 epoch（适应视频）。报告了推理速度：TRUS-V 上 U-Net++ 骨干达 127.97 FPS，SUN-SEG 上 ACSNet 骨干达 89.95 FPS。
- **未说明**：具体训练总时长、GPU 数量（场景仅提及单卡）、教师预训练算力消耗。

#### 5. 实验数量与充分性

- **实验组数**：共 3 组核心实验：
  1. **SUN-SEG 定量对比**（表 1）：与 8 种 2D 模型 + 3 种视频模型对比。
  2. **TRUS-V 定量对比**（表 2）：同样对比上述模型。
  3. **消融实验**（表 3）：在 SUN-SEG-Easy 上逐步加入 L_KD、L_proto、L_con 及不同原型尺度变体（单尺度 vs. 双尺度）。
- **充分性**：数据集覆盖了非超声（息肉）和超声两种模态；消融验证了各组件贡献；对比方法广泛，包括近期 SOTA。实验设计较严谨，公平性方面：所有 2D 基线均按逐帧推理对比，视频模型按原方法评估，指标保持一致。
- **不足**：未报告跨域泛化实验（如 TRUS-V 上训练后直接在 SUN-SEG 测试）；未对比完全监督视频方法（因本文无需视频标注，但可对比弱监督方法）；消融仅在 SUN-SEG 上做，未在 TRUS-V 上重复验证鲁棒性。

#### 6. 论文的主要结论与发现

- **主要结论**：所提出的时域一致性蒸馏框架有效解决了 2D 网络忽略时域导致的闪烁问题，在保持单帧推理速度的同时，在 SUN-SEG 和 TRUS-V 上均达到 SOTA 性能（Dice 分别提升至 0.746 和 0.829，优于所有 2D 和视频模型）。
- **发现**：
  - 传统视频模型（SSTAN、FLA-Net、DALA）在 TRUS 超声域表现不如强 2D 基线，证明了超声特殊噪声（散斑、声影）对现有时域注意力的破坏性，而本方法因针对性设计（置信加权 + 原型对齐）更具鲁棒性。
  - 单独使用像素级一致性或单尺度原型对齐提升有限，双尺度（局部边界 + 全局语义）组合效果最佳。
  - 知识蒸馏有效防止了灾难性遗忘，稳定了初始训练。

#### 7. 优点

- **方法亮点**：
  - **临床观察驱动的设计**：利用前列腺几何稳定性与声学环境波动的差异，创新性地用置信加权抑制不稳定区域梯度，避免了传统时域约束的副作用。
  - **双尺度原型对齐**：兼顾边界细节和场景语义，对散斑噪声有更强的特征级鲁棒性。
  - **训练-推理解耦**：训练时引入光流、教师等复杂模块，推理时退化为纯 2D 网络，实现了实时性（>120 FPS）与高精度的兼得。
- **实验亮点**：
  - 贡献了大型多视图 TRUS 视频基准（TRUS-V），含高质人工精修标注，弥补了该领域视频数据缺失。
  - 在通用息肉分割基准上验证了泛化能力，方法不限于超声。
- **其他**：代码和数据集开源，有利于复现和后续研究。

#### 8. 不足与局限

- **实验覆盖**：
  - 消融实验仅在 SUN-SEG 上进行，未在 TRUS-V 上验证各组件对超声域特定问题的贡献（如置信加权对声影的敏感性）。
  - 未与弱监督或半监督视频分割方法对比（如 CPD、基于时序伪标签的迭代训练）。
  - 仅测试了单一骨干（U-Net++ 和 ACSNet），未验证在其他强大 2D 骨干（如 SegFormer、Swin UNETR）上的通用性。
- **偏差风险**：
  - TRUS-V 数据集规模有限（10 患者），可能引入患者特定偏差；测试集仅 279 帧，统计稳定性较弱。
  - 伪标签依赖教师模型质量，若静态图像训练分布与视频域差异大，可能引入噪声。
- **应用限制**：
  - 光流估计（Farneback）本身有噪声，尤其在散斑严重区域；虽用置信权重缓解，但极端阴影下可能失效。
  - 当前仅用于 2D 视频分割，未讨论扩展到 3D 容积重建或实时活检引导的闭环系统。
  - 对密集视频标注的需求虽减小，但仍需少量高质量静态图像预训练教师（本文使用了 4,400 张），严格上并非零标注。
- **计算资源**：文中仅提及单卡 A100 训练 1 个 epoch，但未报告实际训练时间；预训练阶段（5 折 U-Net 集成训练）的算力消耗未提及。

（完）
