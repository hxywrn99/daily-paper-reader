---
title: Flow Matching Meets 3D Curvilinear Structure Segmentation in Medical Imaging
title_zh: 流匹配遇上医学影像中的3D曲线结构分割
authors: "Sidi Mohamed Sid'El Moctar, Nicolas Vitry, Hélène Bouvrais"
date: 2026-08-20
pdf: "https://arxiv.org/pdf/2608.19965v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 面向3D医学图像分割的流匹配深度学习方法
tldr: 3D医学图像中弯曲结构分割面临拓扑复杂、类别不平衡、对比度低等挑战，现有方法多针对特定解剖结构，泛化性有限。扩散模型虽能迭代精化分割，但采样开销大，难以用于高分辨率三维数据。本文提出基于流匹配的3D-CurvSegFlow，学习从简单分布到目标血管表示的连续变换，实现高效推理的渐进精化。在门静脉、脑血管和冠状动脉三个公开数据集上，以统一架构和训练策略超越通用及专用方法，良好保持细小分支与血管连续性。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.19965v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1235, \"height\": 469, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.19965v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1241, \"height\": 960, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.19965v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1207, \"height\": 829, \"label\": \"Table\"}]"
motivation: 现有3D弯曲结构分割方法缺乏跨解剖和模态的泛化能力，扩散模型虽有效但采样成本过高，限制高分辨率应用。
method: 提出3D-CurvSegFlow，利用流匹配学习源分布到目标血管表示的连续变换，实现渐进式结构精化与高效推理。
result: 在门静脉、脑血管和冠状动脉三个公开数据集上，统一架构下性能优于通用和血管专用方法，薄分支保留更完整。
conclusion: 该方法推动了3D弯曲结构分割的精度与泛化，为医学图像分析提供了高效且临床可用的生成式分割新路径。
---

## 摘要
由于复杂的拓扑结构、严重的类别不平衡、弱对比度以及结构形态的巨大差异，3D医学图像中曲线解剖结构的分割仍然具有挑战性。尽管已经提出了针对3D曲线分割的深度学习方法，但它们通常针对特定解剖结构或模态进行定制，限制了跨临床场景的泛化能力，并留有改进空间。最近的生成模型已显示出迭代预测在结构化分割任务中的优势，然而基于扩散的方法面临计算成本高的采样问题，阻碍了其在高质量3D体数据上的应用。我们提出了3D-CurvSegFlow，一种基于流匹配的3D曲线结构分割模型。该模型学习从简单源分布到目标血管表示的连续变换，从而以高效推理逐步细化复杂曲线几何。我们在三个涵盖不同解剖结构和模态的公开挑战性数据集上评估了我们的方法：门静脉、脑血管和冠状动脉。在所有任务中使用统一的架构和训练策略，我们的方法优于通用方法和专门针对血管的方法，并具有对细薄分支和血管连续性的强保持能力。这项工作不仅推动了3D曲线分割的最新技术水平，还为医学图像分析中高效、可泛化且临床可行的方法开辟了新途径。

## Abstract
Segmentation of curvilinear anatomical structures in 3D medical images remains challenging due to complex topology, severe class imbalance, weak contrast, and large variations in structure morphology. While deep learning approaches for 3D curvilinear segmentation have been proposed, they are often tailored to specific anatomies or modalities, limiting generalization across clinical settings and leaving room for improvement. Recent generative models have shown the benefits of iterative prediction for structured segmentation tasks, yet diffusion-based methods suffer from computationally expensive sampling, hindering their use on high-resolution 3D volumes. We present 3D-CurvSegFlow, a flow matching-based model for 3D curvilinear structure segmentation. The model learns a continuous transformation from a simple source distribution to the target vascular representation, enabling progressive refinement of complex curvilinear geometries with efficient inference. We evaluate our method on Three public challenging datasets covering distinct anatomies and modalities: portal vein, cerebral vessel, and coronary arteries. Using a common architecture and training strategy across all tasks, our method outperforms general-purpose and vessel-specific approaches, with strong preservation of thin branches and vascular continuity. This work not only advances the state-of-the-art in 3D curvilinear segmentation but also opens new avenues for efficient, generalizable, and clinically applicable methods in medical image analysis.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：3D医学图像中的曲线解剖结构（血管等）分割面临多重挑战，包括复杂的拓扑结构、严重的类别不平衡（血管像素占比极低）、弱对比度、部分体积效应、边界模糊，以及不同患者之间血管形态的显著差异。
- **现有方法的不足**：
  - 已有的3D深度学习方法（如3D U-Net、Transformer、Mamba类架构）大多针对特定解剖结构或成像模态定制，需要大量重训练，泛化能力受限。
  - 扩散模型虽然通过迭代精化能改善分割质量，但采样步数多（数百步）、计算成本高，难以应用于高分辨率3D体数据。
  - 医学图像分析领域缺乏一个统一的、跨不同血管区域的通用分割框架。
- **研究意义**：本文提出基于流匹配（Flow Matching）的生成式分割方法，旨在以高效推理实现跨解剖区域、跨模态的稳健分割，为临床可用的3D曲线结构分割开辟新路径。

## 2. 论文提出的方法论

- **核心思想**：将分割问题建模为一个连续的动力系统过程——学习一个由输入图像条件化的、依赖时间的速度场（velocity field），逐步将噪声初始化（noisy initialization）传输到目标血管掩膜（ground-truth mask）。
- **关键公式**：
  - 线性插值轨迹：\( x_t = (1-t)x_0 + tx_1 \)，其中 \( x_0 \sim N(0, I) \) 为初始噪声，\( x_1 \) 为真实标签，\( t \in [0, 1] \)。
  - 目标速度：\( u_t = x_1 - x_0 \)。
  - 模型学习速度场 \( \nu_\theta(x_t, I, t) \) 来预测掩膜的瞬时变化。
  - 推理时使用显式Euler法以 N=3 步积分，\( x_{n+1} = x_n + \Delta t \cdot \nu_\theta(x_n, I, t_n) \)，远少于扩散模型所需的数百步。
- **网络架构**：
  - 3D时间条件U-Net：4层编码器（通道：32、64、128、256），瓶颈层512通道，对称解码器。
  - 卷积块采用 3×3×3 卷积 + Group Normalization + SiLU 激活。
  - 所有跳跃连接上应用**注意力门控（attention gates）**，聚焦血管区域并抑制背景。
  - 总参数量：**23.49M**。
  - 时间嵌入：正弦位置编码 → 256维 → 两层MLP → 逐元素加到各块特征图上。
- **损失函数**：复合损失
  - 流匹配损失：\( L_{FM} = \frac{1}{2}\|\nu_\theta(x_t, I, t) - u_t\|^2 \)
  - 加权二元交叉熵（类别权重 \( w_1=1.0, w_0=0.15 \)）缓解类别不平衡
  - Soft Dice Loss
  - 总损失：\( L = \lambda_{FM} L_{FM} + \lambda_{WBCE} L_{WBCE} + \lambda_{Dice} L_{Dice} \)
- **优化策略**：AdamW优化器，学习率 5×10⁻⁵，权重衰减 1×10⁻⁵，余弦退火调度至 10⁻⁶；数据增强包括随机翻转、仿射变换、伽马强度调整和高斯噪声。

## 3. 实验设计

- **数据集（3个公开数据集，覆盖不同解剖区域和模态）**：
  - **3Dircadb（门静脉）**：19例增强CT，15例训练/4例测试，血管占肝脏体积<2%。
  - **SMILE-UHURA（脑血管）**：14例7T TOF-MRA（300μm各向同性分辨率），12例训练/2例测试。
  - **ImageCAS（冠状动脉）**：1000例CTA，随机选800训练/200测试。
- **对比方法**：
  - 通用模型：3D U-Net、nnU-Net、CS²-Net
  - 血管专用模型：D2-RD-UNet（肝血管）、FFCM-MRF和vesselFM（脑血管）、MSFP-Net（冠状动脉）
  - 其他基准：VNet、Attention U-Net、SwinUNETR、UNETR、SAM-Med3D、GCS、GMM-MRF、CAS-Net、SegFormer等。
- **评估指标**：Dice、IoU、Precision、Recall、clDice（中心线Dice，衡量拓扑连续性）、HD95（95分位Hausdorff距离，衡量边界精度）。

## 4. 资源与算力

- **文中未明确说明GPU型号、数量或具体训练时长**。仅提及训练epoch数：3Dircadb和SMILE-UHURA最多700轮，ImageCAS最多200轮，并使用早停策略（EMA验证损失，α=0.1）。
- 推理速度方面：在SMILE-UHURA上，3步Euler积分推理每卷耗时**22.3秒**；5步需36.4秒，10步需71.9秒——步数增加反而降低精度，证明3步已足够。

## 5. 实验数量与充分性

- **实验组数**：
  - 3个数据集上的主实验（各对比多种SOTA方法）。
  - 推理步数消融（3步 vs 5步 vs 10步），但未包含其他消融实验（如损失函数各分量贡献、注意力门控有效性、网络深度等）。
- **充分性与客观性**：
  - **优点**：三个数据集涵盖CT/MRA两种模态、不同解剖区域、不同血管尺度，且采用统一架构和训练策略，验证了泛化性；使用了丰富的指标（体素重叠+拓扑保真度）。
  - **不足**：SMILE-UHURA仅2例测试，统计显著性有限；ImageCAS缺少clDice和HD95的某些对比数据；**无正式的消融研究**，难以量化各设计组件的独立贡献；结论未提供方差或置信区间。

## 6. 论文的主要结论与发现

- 3D-CurvSegFlow在**所有三个数据集上均取得最优的Dice、clDice和Recall**，在IoU、Precision、HD95上于3个数据集中的2个取得最优。
- 与nnU-Net等强基线相比，3Dircadb上Dice提升约4个百分点（0.70→0.74），SMILE-UHURA上提升约0.5个百分点（0.7981→0.8032），ImageCAS上提升约1.8个百分点（0.8059→0.824）。
- 高Recall表明能检测到更多细小分支，高Precision说明不会引入过多假阳性；clDice优势表明血管树连续性保持更好。
- **定性分析发现**：在弱对比度和伪影区域，3D-CurvSegFlow生成的血管树更连续、断支更少；甚至在某些情况下，其预测比SMILE-UHURA的人工标注更符合真实血管宽度。
- 3步推理即可达到最佳性能，比扩散模型所需的数百步高效数个数量级。

## 7. 优点

- **统一架构、强泛化性**：单一模型+统一训练策略跨3个数据集（不同解剖区域、不同模态、不同成像协议）均取得最优或次优性能，无需任务特定修改。
- **迭代精化机制**：流匹配的连续传输过程使网络能利用图像外观与当前分割状态逐步修正不完整的血管轨迹，恢复细小的模糊分支，优于单步预测。
- **推理计算效率高**：仅需3步Euler积分，远低于扩散模型，适合高分辨率3D体数据。
- **注意力门控跳跃连接**：有效聚焦血管区域、抑制背景干扰，类别不平衡损失进一步改善小目标分割。
- **全面的评估指标**：结合重叠指标（Dice/IoU）和拓扑指标（clDice/HD95），能捕捉血管分割特有的连续性需求，评估体系合理。
- **在SMILE-UHURA上超越人工标注质量**，说明模型有潜力辅助标注校正。

## 8. 不足与局限

- **模态覆盖有限**：仅评估了增强CT和TOF-MRA，未涉及非增强CT、MRI（非MRA）或超声等模态，模型的低/无对比度场景行为未知。
- **全局上下文受限**：训练依赖固定大小的体积patch，对非常庞大的血管树可能缺乏全局解剖上下文信息。
- **远端过度分割**：在冠状动脉CTA上，远端过度分割仍影响HD95边界指标，存在一定假阳性倾向。
- **缺乏消融实验**：未对损失函数各分量、注意力门控、时间嵌入维度、网络深度等设计选择进行系统的消融分析，各组件贡献无法量化。
- **测试集规模偏小**：SMILE-UHURA仅2个测试卷，结果的统计置信度有限。
- **GPU资源信息缺失**：未报告训练硬件配置、训练时长和显存消耗，可复现性和资源需求评估受限。
- **未与其他流匹配/扩散分割方法直接对比在ImageCAS上的性能**，部分对比结果来自原文献（带标注），实验中未使用完全一致的训练设置，对比的公平性存在一定局限。
- **未来方向待探索**：替代ODE求解器、自适应积分策略、更多临床模态的扩展均尚未完成验证。

（完）
