---
title: "Beyond Boundary Noise: Aggregated Aleatoric Uncertainty Fails to Capture Presence Ambiguity in 3D Lung Nodule Segmentation"
title_zh: 超越边界噪声：聚合偶然不确定性无法捕捉三维肺结节分割中的存在性模糊
authors: "Simon Baur, Arne Schernich, Ekin Böke, Wojciech Samek, Jackie Ma"
date: 2026-08-14
pdf: "https://arxiv.org/pdf/2608.14766v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 3D肺结节分割中的深度学习不确定性估计
tldr: 医学图像分割中，熵不确定性理论上应捕捉不可约数据歧义，但实际是否反映病理存在与否的临床歧义尚不清楚。在3D肺结节分割任务中，跨四种架构评估发现，熵不确定性主要对齐边界噪声，对存在歧义判别力不足。相比之下，基于冻结分割特征的轻量监督歧义头显著优于所有熵聚合基线，甚至匹配或超越显式建模歧义的方法。表明熵不确定性并非临床歧义的可靠代理。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.14766v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1263, \"height\": 827, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14766v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1250, \"height\": 478, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14766v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1255, \"height\": 891, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14766v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1185, \"height\": 864, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.14766v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1248, \"height\": 458, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.14766v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1268, \"height\": 673, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.14766v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1273, \"height\": 576, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.14766v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 850, \"height\": 275, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.14766v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 777, \"height\": 384, \"label\": \"Table\"}]"
motivation: 熵不确定性理论上应反映数据歧义，但尚未验证其能否捕捉临床相关的存在歧义，而非仅边界噪声。
method: 在LIDC-IDRI和外部LNDb上，用四种架构结合MC Dropout和深度集成评估熵不确定性，并设计基于冻结特征的监督歧义头。
result: 熵不确定性仅对边界噪声敏感，监督歧义头在各类指标上显著优于熵聚合基线，比肩甚至超越Probabilistic U-Net等显式方法。
conclusion: 熵不确定性无法代理临床存在歧义，应避免在安全关键应用中依赖其作为不确定性估计。
---

## 摘要
不确定性估计对于深度学习在医学图像分割中的安全临床应用至关重要，其中偶然不确定性在理论上旨在捕捉不可约的数据模糊性。然而，基于熵的度量是否能够反映临床上有意义的模糊性，即关于病理是否存在的病例级分歧，仍鲜有研究。与大多数先前专注于像素级边界分歧的工作不同，我们系统地评估了偶然不确定性捕捉存在性模糊的能力。我们的评估涵盖四种架构（使用蒙特卡洛丢弃法和深度集成）在LIDC-IDRI和外部验证队列（LNDb）上的三维肺结节分割。我们发现，基于熵的不确定性图与边界噪声和轻微勾画变异对齐，但对于存在性模糊缺乏足够的判别信号。相比之下，一个在冻结分割特征上训练的轻量级监督模糊头在所有架构、指标和两个队列中均显著优于所有基于熵聚合的基线，并且与在分歧监督下显式建模模糊的方法（概率U-Net、标注者混淆3D-UNet）相当或更优。定性特征空间分析表明，存在性模糊已经被编码在逐像素训练网络的冻结编码器特征中，只是在分割输出及其熵聚合中被丢弃。我们的发现揭示了偶然不确定性的理论承诺与其实际行为之间的根本不匹配，并建议从业者不应在安全关键应用中依赖基于熵的不确定性作为临床模糊性的代理。

## Abstract
Uncertainty estimation is critical for the safe clinical deployment of deep learning in medical image segmentation, with aleatoric uncertainty theoretically designed to capture irreducible data ambiguity. However, whether entropy-based measures reflect clinically meaningful ambiguity, i.e. case-level disagreement about whether a pathology is present at all, remains poorly understood. Contrary to most prior work, which focused on pixel-wise boundary disagreement, we systematically evaluate how well aleatoric uncertainty captures presence ambiguity. Our evaluation spans 3D lung nodule segmentation across four architectures with Monte Carlo dropout and deep ensembles, on LIDC-IDRI and an external validation cohort (LNDb). We find that entropy-based uncertainty maps align with boundary noise and minor drawing variation but carry insufficient discriminative signal for presence ambiguity. In contrast, a lightweight supervised ambiguity head trained on frozen segmentation features substantially outperforms all entropy-aggregation-based baselines across architectures, metrics, and both cohorts, and matches or exceeds methods that explicitly model ambiguity under disagreement supervision (Probabilistic U-Net, Annotator-Confusion 3D-UNet). A qualitative feature-space analysis shows that presence ambiguity is already encoded in the frozen encoder features of pixel-wise-trained networks, only to be discarded by the segmentation output and its entropy aggregation. Our findings expose a fundamental mismatch between the theoretical promise of aleatoric uncertainty and its practical behavior, and suggest that practitioners should not rely on entropy-based uncertainty as a proxy for clinical ambiguity in safety-critical applications.

---

## 论文详细总结（自动生成）

## 论文详细中文总结

### 1. 论文的核心问题与整体含义

- **研究动机**：深度学习在医学图像分割中的安全部署离不开可靠的不确定性估计。理论上，偶然不确定性（Aleatoric Uncertainty, AU）被设计用于捕捉"不可约"的数据歧义，例如解剖边界模糊、标注噪声等。然而，模型输出的不确定性是否真正反映了临床上**有意义的歧义**——即放射科医生在"该病理结构是否真的存在"这一病例层面上的分歧（即存在性模糊，Presence Ambiguity）——这一问题此前几乎没有被系统研究过。

- **核心问题**：本文针对 3D 肺结节分割任务，提出一个关键质疑：**基于熵（Entropy）的偶然不确定性能否捕捉病例级的存在性模糊？** 作者指出，熵不确定性图在视觉上看起来合理（主要高亮物体边界），但在专家对病灶存在性本身存在分歧的病例上，其不确定性信号可能反而低于无争议的病例，从而给临床决策带来"虚假的安全感"（false reassurance）。

- **整体含义**：论文用系统实验证明了**熵聚合的偶然不确定性无法作为临床存在性模糊的可靠代理**，揭示了不确定性量化理论承诺与实际行为之间的根本性错位，对安全关键医疗应用中 UQ 方法的选择具有警示意义。

### 2. 论文提出的方法论

论文方法部分包含三个核心环节，均为形式化定义与后验训练设计：

- **病例级存在性模糊的形式化**：
  - 给定 K 位独立标注者的分割掩码 $\{m_k\}_{k=1}^K$，定义每位标注者的存在指示符 $1_k(x) = \mathbb{1}[|m_k| > 0]$；
  - 定义经验存在一致性 $A(x) = \frac{1}{K} \sum_{k=1}^K 1_k(x) \in [0,1]$；
  - 将存在性模糊检测构造成二元分类问题：
    - $y_{\text{amb}}(x) = 1$ 当 $0 < A(x) < 1$（标注者间对病灶是否存在存在分歧）；
    - $y_{\text{amb}}(x) = 0$ 当 $A(x) \in \{0,1\}$（全体一致认为存在或不存在）。

- **病例级不确定性聚合（基线方法）**：
  - 先通过 MC-Dropout 或深度集成采样获得每个体素 $v_i$ 的偶然不确定性 $\text{AU}(v_i)$；
  - 在**真实标注掩码的并集** $M(x)$ 上取平均：$U_{\text{AU}}(x) = \frac{1}{|M(x)|} \sum_{v_i \in M(x)} \text{AU}(v_i)$；
  - 也评估了在预测掩码 $\hat{M}(x)$ 上聚合的变体（约 5% 预测为空的病例被剔除）。

- **后验歧义头（Ambiguity Head）**：
  - 核心思想：在**冻结的分割骨干网络瓶颈特征** $f_\theta(x)$（最压缩的潜在表征，位于解码器上采样之前）之上，附加一个轻量级可训练头 $g_\phi$；
  - 结构：注意力池化（attention pooling）→ 3 层 MLP（ReLU 激活）→ sigmoid 输出；
  - 优化目标：$\phi^* = \arg\min_\phi \mathcal{L}_{\text{BCE}}(g_\phi(f_\theta(x)), y_{\text{amb}}(x))$；
  - 输出标量 $U_{\text{amb}}(x)$ 即可作为存在性模糊的估计分数，与基线 $U_{\text{AU}}(x)$ 对比评估判别力。

- **关键洞察**：论文明确指出，歧义头不是要作为"竞争对手"提出，而是作为一个**具有监督优势的上界**（supervision-advantaged upper bound），用以量化标准熵聚合方法的能力上限。

### 3. 实验设计

- **数据集**：
  - **LIDC-IDRI（内部数据集，in-distribution）**：大规模胸部 CT 数据集，采用两阶段标注协议（独立标注→审阅修订），剩余分歧反映真实的临床歧义。按 70/15/15 划分训练/验证/测试。测试集包含 88（1vs3）、47（2vs2）、47（3vs1）个模糊病例和 80 个非模糊病例。
  - **LNDb（外部验证集，out-of-distribution）**：完全未见过的外部队列，无重训练或监督，用于检验泛化能力。评估时对各类别下采样到少数类规模，保证类别平衡。

- **分割架构**（4 种）：
  - 3D-UNet、3D-ResUNet、Attention-3D-UNet、SegFormer3D（transformer 骨干）。

- **不确定性建模方法**（6 种）：
  - **熵聚合基线**：MC-Dropout（S=100 样本，dropout=0.1）、深度集成（N=3 成员）；
  - **监督分歧建模基线**：Probabilistic 3D-UNet（条件潜在空间采样）、Annotator-Confusion 3D-UNet（学习标注者混淆矩阵，对 K 个标注者预测取熵平均）；
  - **本文方法**：后验歧义头。

- **评估指标**：
  - 二元存在性模糊检测：AUROC、AUPRC、F1（最优 F1 阈值在验证集上选取，报告 5 次不同随机初始化的均值）；
  - 序数歧义严重性排序：Spearman 相关系数 $\rho$（检验不确定性分数是否能区分 1vs3、2vs2、3vs1 等不同分歧等级）；
  - 分割质量本身也做了报告（Dice：UNet 变体 0.80–0.84，SegFormer3D 0.78，Ann-Conf 0.70）。

- **实验变量**：
  - 聚合区域（真实掩码 vs. 预测掩码）；
  - 不同分歧等级（1vs3、2vs2、3vs1；1vs2、2vs1）；
  - 同一实验跨 4 种架构 × 5 次初始化。

### 4. 资源与算力

- 论文正文和附录中**未明确说明 GPU 型号、数量、总训练时长或算力消耗**。
- 文中仅提供了训练配置信息：所有模型训练 100 epochs，早停 patience 为 15（基于验证集 Dice）；后验歧义头训练 10 epochs；MC-Dropout / Probabilistic 3D-UNet 推理采样 S=100 次，集成成员 N=3。
- 从实验规模（4 种 3D 架构 × 2 个数据集 × 5 次初始化 × 多种不确定性基线）推测算力需求不小，但论文未予以披露。

### 5. 实验数量与充分性

- **实验规模较大且系统**：
  - 4 种分割架构 × 2 种熵聚合 UQ 方法（MC-Dropout 和 Ensemble）× 2 个聚合区域（GT mask / Pred mask）；
  - 2 种显式分歧监督基线（Prob-3DUnet、Ann-Conf-3DUnet）；
  - 2 个数据集（其中 1 个为外部验证）；
  - 每个实验 5 次随机初始化，报告均值±标准差；
  - 二进制判别（AUROC/AUPRC/F1）和序数排序（Spearman ρ）两个层面的评估；
  - 附录中按分歧等级、按架构、按聚合区域分别给出了完整细分指标（表 1–4）。

- **公平性与客观性评估**：
  - 论文在主流基线上给出了公平对比，并明确区分了"熵聚合基线"与"监督上界"的角色定位，说明方法对比边界是清晰的；
  - 预测掩码聚合一致地提升了所有基线（包括有监督的显式建模方法），这一结果的呈现较为客观，未选择性隐瞒；
  - 但需要注意：歧义头使用了存在性标注进行训练（即利用了测试集同分布的监督信息），与完全无监督的熵基线相比不构成同一起跑线。作者对此有充分声明，将其定位为可量化的"上界"而非实用部署方案。

- **总体评价**：实验数量充足，维度覆盖广，消融和对比逻辑清晰，对评估歧义头在不同架构、不同分歧强度、不同分布下的稳健性提供了充分的数据支撑。但从"实际可用方法"的角度看，缺少对歧义头跨数据集直接迁移的研究（如只在 LIDC 上训练、直接在 LNDb 上测试）。

### 6. 论文的主要结论与发现

- **核心结论一（负向发现）**：熵基础的偶然不确定性聚合（无论基于 MC-Dropout 还是深度集成，也无论是真实掩码还是预测掩码聚合）在存在性模糊检测上接近随机表现。LIDC 上基于真实掩码聚合时 AUROC 最高仅 0.48；改用预测掩码聚合后提升至最高 0.58，但仍远低于实用水平。

- **核心结论二（正向发现）**：轻量级后验歧义头在冻结的编码器特征上训练，即可达到 LIDC AUROC 0.74 / AUPRC 0.67 / F1 0.63，LNDb AUROC 0.67 / AUPRC 0.66 / F1 0.66，**全面超越所有熵聚合基线**，并匹配或优于显式在分歧监督下训练的 Probabilistic 3D-UNet 和 Annotator-Confusion 3D-UNet（预测掩码聚合下 Ann-Conf 3D-UNet LIDC AUROC 接近 0.73，两者在 F1 上持平）。

- **核心结论三（机制解释）**：UMAP 投影显示，**存在性模糊信息已经编码在逐像素训练网络的冻结编码器特征中**，只是在下游分割输出及其熵聚合过程中被丢弃了。歧义头并非"从零学习"这一信号，而是"恢复"了标准流程无法传递的信息。

- **附加发现**：序数相关性分析表明，歧义头不仅能区分二元存在性模糊，还能按分歧严重程度（1vs3 > 2vs2 > 3vs1）对病例进行排序（LIDC Spearman ρ 最高 0.63），而熵聚合基线的序数相关接近零或很弱。

- **实践建议**：在有监督标签可用的情况下，**不应将熵不确定性作为临床歧义的代理**；需要进一步研究将存在性模糊纳入训练目标的形式化方法。

### 7. 优点

- **问题选得好、定义清晰**：提出并形式化了"存在性模糊"这一此前被忽视但对临床决策至关重要的不确定类型，与通常研究较充分的"边界噪声"做出了明确区分。
- **实验设计系统严谨**：覆盖多种架构（CNN 与 Transformer）、多种 UQ 方法（采样型与监督型）、内外两个数据集、多种分歧等级、二元和序数两个层面的评估，结论的稳健性强。
- **方法极简有效**：后验歧义头仅由注意力池化 + 3 层 MLP 构成，在冻结特征上训练 10 epochs 即达到最先进表现，是一个极具实用潜力的轻量级方案。
- **机制分析有说服力**：UMAP 特征空间分析为"信号存在但被标准输出丢弃"提供了直观的可视化证据，解释了为什么熵聚合会失效。
- **对基线公平**：明确披露预测掩码聚合会系统性提升所有基线（包括监督方法），并如实报告了歧义头相对于最佳监督基线优势收窄甚至持平的情况，未夸大结论。
- **外部验证**：LNDb 作为完全未见的分布外数据集，使跨数据集泛化结论更有说服力。

### 8. 不足与局限

- **方法定位的局限**：后验歧义头需要存在性标注（即多位放射科医生的独立标记）作为训练监督，这在真实临床场景中是一种稀缺资源；作者承认这一点，并将其定位为"上界"而非直接可部署方案，但这也意味着它不能解决无监督场景下的存在性模糊检测问题。
- **任务与模态单一**：仅覆盖肺结节分割（CT 模态、单一解剖结构），结论是否能推广到其他器官、其他模态（如 MRI、病理图像）或其他类型的存在性模糊（如病灶类型差异）尚未验证。
- **依赖特征提取位置**：歧义头性能随骨干架构不同而差异明显（SegFormer3D 上显著弱于 UNet 变体），说明"最优特征提取点"是架构相关的；当前只在瓶颈层取特征，可能有更优的特征层级或组合方式未被探索。
- **跨数据集直接迁移未测**：论文只在各数据集内部训练并评估歧义头（LNDb 上使用时未说明是否使用 LNDb 标注训练），缺少"只在 LIDC 训练、直接在 LNDb 部署"的跨域迁移实验，这限制了其实际部署意义的判断。
- **预测掩码聚合的天花板**：用预测掩码聚合时排除了约 5% 的预测为空病例，这些病例可能恰恰是模型认为"无病灶"的模糊病例，剔除它们可能带来选择性偏差。
- **序数相关性在外部数据集上偏弱**：LNDb 上歧义头的 Spearman ρ 仅有 0.22，虽然优于基线，但绝对值有限，外部泛化情形下的排序性能仍有较大提升空间。
- **算力信息披露不足**：未报告 GPU 型号、数量、训练总耗时等资源信息，复现成本不明。

（完）
