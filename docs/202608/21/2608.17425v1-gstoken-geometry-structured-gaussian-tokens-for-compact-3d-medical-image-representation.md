---
title: "GSToken: Geometry-Structured Gaussian Tokens for Compact 3D Medical Image Representation"
title_zh: GSToken：面向紧凑三维医学图像表示的几何结构化高斯令牌
authors: "Xiaoduo Li, Quan Gu"
date: 2026-08-18
pdf: "https://arxiv.org/pdf/2608.17425v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 提出了用于多模态MRI脑肿瘤分割的紧凑令牌表示。
tldr: 现有多模态MRI分割中，token化压缩常丢弃病变形态与空间范围信息，且端到端评估混杂下游解码器，难以归因tokenizer本身的信息保留。本文提出GSToken，每个token附加可学习的三维中心、各向异性尺度与方向，以极低参数成本显式建模几何结构。同时设计冻结token评估协议，在固定容量与共享探针下公平比较。结果表明，GSToken在所有肿瘤子区域、表面和距离指标上持续优于容量匹配基线，验证了显式几何编码对紧凑三维医学图像表示的价值。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.17425v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1141, \"height\": 861, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17425v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1443, \"height\": 294, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17425v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1412, \"height\": 501, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.17425v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1433, \"height\": 874, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.17425v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 932, \"height\": 320, \"label\": \"Table\"}]"
motivation: 现有token化压缩常丢弃病变形态与空间范围，且端到端评估混淆了tokenizer与解码器，难以归因信息保留。
method: 提出GSToken，为每个token附加可学习的三维中心、各向异性尺度和方向，并设计冻结token评估协议以公平测量信息保留。
result: 冻结探测下，GSToken在所有肿瘤子区域及表面、距离指标上一致且显著优于容量匹配的自适应基线。
conclusion: 在token中显式编码空间几何可显著提升三维体积表示的信息密度，为紧凑医学图像表示提供了新设计原则。
---

## 摘要
多模态MRI的有效分割对于提高脑肿瘤识别中神经网络的准确性至关重要。现有方法通常通过固定块编码或学习型注意力池化（如TokenLearner）将三维体积压缩为令牌序列。然而，这些压缩方案丢弃了显式的空间形状信息；生成的令牌不传达任何关于病灶形态或空间范围的概念。与此同时，端到端评估将分词器的信息保留与下游解码器的重建能力纠缠在一起，且方法间缺乏统一的能力契约，使得性能差异难以归因。在本文中，我们首次将高斯令牌引入多模态脑肿瘤分割：每个令牌不仅携带语义特征，还携带学习到的三维中心、各向异性尺度和方向，以可忽略的参数开销为表示提供显式的几何支撑。我们进一步提出一种冻结令牌实用性评估协议：训练好的分词器被冻结，其输出被转换为固定容量的序列化契约，共享的轻量级Transformer探针在严格匹配的条件下独立衡量每个分词器保留的信息。多样本配对统计检验表明，在冻结探针下，GSToken始终且显著优于容量匹配的自适应基线，在所有肿瘤亚区域、表面和距离指标上具有一致的优势。这些结果表明，在令牌内显式编码空间几何信息显著提高了体积表示的信息密度，为紧凑的三维医学图像表示和下游解读提供了新的设计原则。

## Abstract
Effective segmentation of multi-modal MRI is central to improving neural network accuracy in brain tumor recognition. Existing methods typically compress 3D volumes into token sequences via fixed patch encoding or learned attention pooling (e.g., TokenLearner). However, these compression schemes discard explicit spatial shape information; the resulting tokens convey no notion of lesion morphology or spatial extent. Meanwhile, end-to-end evaluation entangles a tokenizer's information retention with the reconstruction capacity of the downstream decoder, and the lack of a unified capacity contract across methods makes performance differences difficult to attribute. In this paper, we introduce Gaussian tokens to multi-modal brain tumor segmentation for the first time: each token carries not only a semantic feature but also a learned 3D center, anisotropic scale, and orientation, endowing the representation with explicit geometric support at negligible parameter cost. We further propose a frozen-token utility evaluation protocol: the trained tokenizer is frozen, its output is cast into a fixed-capacity serialized contract, and a shared lightweight Transformer probe independently measures each tokenizer's retained information under strictly matched conditions. Multi-seed paired statistical testing shows that GSToken consistently and substantially outperforms capacity-matched adaptive baselines under frozen probing, with uniform advantages across all tumor sub-regions, surface, and distance metrics. These results demonstrate that explicitly encoding spatial geometry within tokens significantly improves the information density of volumetric representations, offering a new design principle for compact 3D medical image representation and downstream reading.

---

## 论文详细总结（自动生成）

# GSToken：面向紧凑三维医学图像表示的几何结构化高斯令牌 — 中文总结

## 1. 核心问题与整体含义（研究动机与背景）

- **问题背景**：三维 MRI 脑肿瘤分割需要将高分辨率体积压缩为令牌序列供 Transformer 使用，但现有令牌化方案存在两大缺陷：
  - **固定网格补丁令牌**（如 ViT、TransUNet、UNETR）简单均匀，但几何形状与病灶形态脱耦，小或各向异性结构（如增强肿瘤边界）容易被稀释；
  - **学习型自适应令牌**（如 TokenLearner、Perceiver）更加灵活，但产生的令牌不携带显式的 3D 支撑信息（位置、范围、方向），无法表达病灶的空间延展。
- **评估混淆**：端到端评估把令牌化器的信息保留能力与下游解码器的重建能力纠缠在一起，且方法间缺乏统一“容量契约”，使性能差异难以归因。
- **核心问题**：在固定令牌预算下，如何保留最多的分割相关信息？显式地把空间几何编码进令牌能否提高信息密度？
- **核心思想**：首次将 3D 高斯原语（Gaussian primitives）用作分割导向的通用令牌，每个令牌同时包含语义特征和可学习的三维中心、各向异性尺度、方向，为紧凑表示提供显式几何支撑。
- **整体意义**：验证了“显式空间几何编码可显著提高体积表示信息密度”这一设计原则，为紧凑三维医学图像表示提供了新方向。

## 2. 方法论：核心思想、关键技术与算法流程

### 2.1 高斯令牌表示

- 每个令牌用五元组表示：  
  `g_i = (μ_i, s_i, R_i, α_i, t_i)`  
  其中 `μ_i` 为中心坐标，`s_i` 为各向异性尺度，`R_i` 为旋转矩阵（由四元数参数化），`α_i` 为重要性权重，`t_i` 为内容特征。
- 中心 `μ_i` 由锚点 `a_i` 加上有界可学习偏移得到；粗锚点基于规则网格，细锚点由自适应提议机制生成。
- 空间支持由各向异性高斯对数权重定义：  
  `ℓ_iv = -1/2 || R_i^T (x_v - μ_i) ⊘ s_i ||_2^2`

### 2.2 特征聚合与反投影（Gather 与 Splat）

- **聚合（Gather）**：对每个令牌，在空间位置上做 softmax 归一化，从密集特征场中加权求和得到令牌特征 `z_i`。
- **反投影（Splat）**：对每个空间位置，在令牌上做 softmax 归一化，将令牌特征加权散射回密集特征场。
- 这种方法让令牌从连续、各向异性、方向感知的支撑区域聚合信息，而非固定点或方形块。

### 2.3 整体网络架构

- 3D 卷积编码器 `E_θ` 将输入 `X` 映射为密集特征场 `F`。
- 提议头（proposal head）和规则粗网格共同生成候选锚点；每个锚点通过交叉注意力获得上下文特征，并预测高斯几何参数。
- 预测的几何参数定义高斯聚合权重，产生粗、细候选令牌集合。
- L 层令牌 Transformer 对令牌进行空间交互，输出令牌内容特征。
- 区域感知核心集选择器（region-aware coreset selector）保留全部粗令牌，并选择 `Kf` 个互补细令牌，固定总预算 `K = Kc + Kf`。
- 最终令牌集通过高斯散射重建密集特征场，再由 3D 解码器 `D_ψ` 输出分割预测。

### 2.4 自适应令牌构建与优化

- **粗到细候选分配**：粗令牌保证全局覆盖，细令牌由提议头基于重要性图生成，提高局部病灶表示能力。
- **区域感知核心集选择**：利用“区域证据”和“场证据”进行冗余消除，优化联合覆盖而非独立得分。
- **可选局部预测细化**：基于预测不确定性，仅对困难位置进行有界残差修正，不改变令牌定义和预算。

### 2.5 冻结令牌评估协议

- 训练完成后冻结令牌化器，导出统一序列化令牌包（含特征、中心、尺度、旋转矩阵、重要性、激活掩码）。
- 共享同一轻量级 Transformer 探针（2 层、6 头、特征维度 96）独立测量各令牌化器的信息保留能力。
- 所有方法使用相同令牌数量、特征宽度、序列化载荷、读者架构和训练预算，确保容量严格匹配。

## 3. 实验设计：数据集、基准与对比方法

- **数据集**：BraTS 2021 训练集，共 1,251 例标注数据；保留 20 例作为审计集（不访问），剩余 1,231 例按固定种子分为 985 训练 / 246 验证。
- **预处理**：各模态 z-score 归一化，共用脑边界框裁剪，重采样至 128³，按 BraTS 约定定义 WT/TC/ET。
- **评价协议**：冻结令牌化器，训练 feature-only Transformer 读者，比较分割准确率、边界质量、病灶检测和“可读性”。
- **对比方法**：
  - `GSToken256`：本文方法，256 个令牌，载荷 119,040 字节；
  - `TokenLearner256`：容量匹配的自适应注意力池化基线；
  - `Patch512`：更高容量参考（512 令牌，238,080 字节）。
- **训练设置**：令牌化器和读者各训练 1,000 步，AdamW，学习率 3e-4；重复 3 个随机种子（17, 23, 41）。
- **统计方法**：同种子内基于相同病例计算配对差异，使用 20,000 次 bootstrap 重采样估计配对 95% 置信区间。

## 4. 资源与算力

- **论文未明确报告 GPU 型号、数量或训练时长**。
- 仅提供训练步数（1,000 步）、优化器（AdamW）和学习率（3e-4）等超参数，未讨论显存、计算量或耗时。
- 由于输入体积为 128³、4 模态，推测计算成本适中，但文中没有给出可复现的算力基准。

## 5. 实验数量与充分性

- **主要实验**：在 BraTS 2021 单一数据集上，对比 3 种令牌化方法，每种 3 个随机种子。
- **指标覆盖**：Macro Dice、WT/TC/ET 子区域 Dice、Surface Dice、HD95，并展示令牌到图像的可视化结果。
- **评估公平性**：采用严格容量匹配（相同令牌数、特征宽度、序列化载荷、读者架构、训练预算），并冻结令牌化器，单独测量信息保留，设计较严谨。
- **统计可靠性**：使用配对差异、bootstrap 置信区间和多样本结果，增强了结论稳健性。
- **充分性局限**：
  - 仅在单一数据集（BraTS 2021）上验证，缺少泛化性测试；
  - 未报告组件消融实验（如粗-细分配、核心集选择、几何参数的影响）；
  - 未与完整分割系统（如 nnU-Net）进行端到端公平对比，作者也承认准确性仍落后于 nnU-Net；
  - 训练步数仅 1,000 步，可能未充分收敛。

## 6. 主要结论与发现

- GSToken256 的 Macro Dice 为 `0.6786 ± 0.0073`，显著高于 TokenLearner256（`0.4576 ± 0.0301`）和更高容量的 Patch512（`0.6005 ± 0.0096`）。
- 相比 TokenLearner，GSToken 平均提升 Macro Dice `+0.2210`、Surface Dice `+0.1645`，HD95 降低 6.93。
- 优势在全部 3 个随机种子、所有肿瘤子区域（WT/TC/ET）、表面与距离指标上一致，表明显式空间支撑和动态分配能在固定容量下保留更多三维肿瘤信息。
- 可视化显示：Patch 令牌呈刚性矩形覆盖，TokenLearner 响应集中但空间碎片化，GSToken 产生平滑的病灶中心响应并保留更广的解剖覆盖。
- 作者强调，GSToken 的价值在于“信息保留能力”，而非追求端到端分割系统的绝对最优性能。

## 7. 优点：方法与实验设计的亮点

- **创新性**：首次将 3D 高斯原语作为通用分割令牌，将显式几何（中心、尺度、旋转）融入紧凑表示，参数开销极小。
- **问题定义清晰**：明确区分“令牌化器的信息保留”与“下游重建能力”，提出冻结令牌 + 统一序列化契约 + 共享探针的评估协议，归因清晰。
- **公平比较**：严格匹配容量、序列化载荷、读者架构和训练预算，消除了方法间的隐性优势。
- **统计严谨**：配对差异分析、bootstrap 置信区间、多随机种子重复，增强了结果可信度。
- **实用设计**：粗到细候选分配与区域感知核心集选择兼顾全局覆盖和局部细节，具有可迁移性。

## 8. 不足与局限

- **实验覆盖狭窄**：仅 BraTS 2021 单数据集，未验证其他模态或器官；未见消融实验，难以归因各组件贡献。
- **与 SOTA 分割模型差距**：作者承认 GSToken 整体分割精度仍落后于 nnU-Net，且未与近年强分割器进行端到端对比，实际临床应用价值尚未证明。
- **评估协议偏抽象**：frozen-token 探针使用轻量读者（2 层 Transformer），可能与真实下游任务性能不完全一致；序列化契约排除了语义 logits，可能低估某些方法。
- **训练预算偏低**：1000 步可能不足以证明方法在大规模数据上的潜力。
- **缺少算力信息**：未报告 GPU 型号、数量、显存和训练耗时，复现难度增加。
- **可推广性风险**：高斯几何假设仅在脑肿瘤这类结构相对紧凑的病灶上验证，对多尺度、弥散性或拓扑复杂结构是否有效尚不明确。

（完）
