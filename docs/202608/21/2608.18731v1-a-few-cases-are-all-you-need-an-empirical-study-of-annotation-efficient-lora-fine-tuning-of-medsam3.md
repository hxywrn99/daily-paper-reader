---
title: "A Few Cases Are All You Need: An Empirical Study of Annotation-Efficient LoRA Fine-Tuning of MedSAM3"
title_zh: 几个案例就够：MedSAM3低秩适应微调的标注效率实证研究
authors: "Sachin Dudda Nagaraju, Bendik Skarre Abrahamsen, Ashkan Moradi, Mattijs Elschot"
date: 2026-08-19
pdf: "https://arxiv.org/pdf/2608.18731v1"
tags: ["query:image-seg"]
score: 9.0
evidence: 直接对应基于深度学习的医学图像分割；研究 MedSAM3 的 LoRA 高效微调及所需标注量。
tldr: 医学图像分割依赖大量标注，且现有专用模型在胆囊等器官上表现极差。本研究使用LoRA微调MedSAM3，仅利用1、2、5、10例标注数据在CT和MRI上分割五个腹部器官。结果表明10例即可达到与使用海量数据训练的专用系统相当的性能，胆囊分割显著优于现有工具，并成功扩展到心脏分割。这大幅降低了标注成本与训练时间，为少样本医学分割提供了实用方案。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2608.18731v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1253, \"height\": 359, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2608.18731v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1264, \"height\": 558, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2608.18731v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1272, \"height\": 563, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2608.18731v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1034, \"height\": 301, \"label\": \"Table\"}]"
motivation: 探索医学分割基础模型在极少标注下能否达到临床可用性能，以缓解数据瓶颈。
method: 用LoRA微调MedSAM3，在5个腹部器官上仅用1至10例标注数据进行参数高效适配。
result: 10例即可与专用系统匹敌，胆囊分割远超现有工具，并泛化至心脏分割，训练更快。
conclusion: 十例标注足以实现临床有用的分割，极大降低标注与训练开销，具有广泛实用价值。
---

## 摘要
医学图像分割对于治疗计划和疾病评估等临床工作流程至关重要。虽然TotalSegmentator和MRSegmentator等专用工具表现出色，但它们需要大量标注数据集进行训练。医学基础模型通过大规模预训练提供了一种有前景的替代方案，可减少新任务的标注负担，但其零样本性能仍然有限。通过低秩适应（LoRA）进行参数高效微调，可以用少量可训练参数实现高效特化，但一个关键问题仍然存在：需要多少专家标注案例才能达到临床有用的分割性能？我们通过将MedSAM3与LoRA相结合，仅使用1、2、5和10个标注案例，在CT和MRI中对五个腹部器官（肝脏、肾脏、脾脏、胆囊和胰腺）进行适配，并在AMOS22数据集上进行评估来解决这一问题。仅用10个案例，模型就达到了与在数量级更大的数据上训练的专用系统相当的性能。值得注意的是，这包括可靠的胆囊分割（CT Dice 0.68，MRI Dice 0.59），而现有工具几乎完全失败（Dice 0.0004），同时在肝脏、肾脏和脾脏方面与MRSegmentator保持5%-10%以内的差距，而使用的标注数量减少了100倍以上。此外，在Whole Heart Segmentation数据集上的外部验证表明，该方法可扩展到心脏分割，这是超出TotalSegmentator（MRI）和MRSegmentator范围的使用场景，仅用10个标注案例即可达到具有竞争力的左心室（LV）性能。训练每个器官只需在单个GPU上耗时3-5小时，比nnU-Net快约2-3倍。这些发现表明，十个标注案例足以实现临床有用的分割，有效减少了图像标注和训练时间方面的瓶颈。

## Abstract
Medical image segmentation is essential for clinical workflows such as treatment planning and disease assessment. While specialist tools like TotalSegmentator and MRSegmentator achieve strong performance, they require large annotated datasets for training. Medical foundation models offer a promising alternative through large-scale pretraining that reduces the annotation burden for new tasks, but zero-shot performance remains limited. Parameter-efficient adaptation via Low-Rank Adaptation (LoRA) enables efficient specialization with few trainable parameters, but a key question remains: how many expert-annotated cases are needed to achieve clinically useful segmentation performance? We address this by adapting MedSAM3 with LoRA for five abdominal organs (liver, kidneys, spleen, gallbladder, and pancreas) in CT and MRI using only 1, 2, 5, and 10 annotated cases, evaluating on AMOS22 dataset. With just 10 cases, models achieve performance competitive with specialist systems trained on orders of magnitude more data. Notably, this includes reliable gallbladder segmentation (Dice 0.68 CT, 0.59 MRI) where existing tools fail almost completely (Dice 0.0004), while remaining within 5--10% of MRSegmentator for liver, kidneys, and spleen using over 100 times fewer annotations. Furthermore, external validation on the Whole Heart Segmentation dataset shows that the approach extends to cardiac segmentation, a use case beyond the scope of TotalSegmentator (MRI) and MRSegmentator, achieving competitive left ventricle (LV) performance with only 10 annotated cases. Training requires only3--5,hours per organ on a single GPU, approximately 2--3 times faster than nnU-Net. These findings suggest that ten annotated cases are sufficient for clinically useful segmentation, effectively reducing bottlenecks for both image annotation and training time.

---

## 论文详细总结（自动生成）

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **研究背景**：医学图像分割在治疗规划、疾病评估等临床流程中至关重要，但训练高精度分割模型通常依赖大量高质量专家标注数据，获取成本高昂且难以扩展。专用工具（如 TotalSegmentator、MRSegmentator）性能强大，但需要训练超过千例的标注数据；nnU-Net 虽强，同样受限于大数据需求。医学基础模型通过大规模预训练降低了新任务的标注依赖，但零样本性能不可靠，尤其对胆囊、胰腺等小或低对比度器官。
- **核心问题**：使用参数高效微调方法（LoRA）适配医学基础模型时，**究竟需要多少例专家标注数据**才能达到临床有用的分割性能？目前缺乏系统性的、按“病例数”尺度变化的研究。
- **整体含义**：该论文通过系统实证，证明了**仅需约10例标注**即可让 MedSAM3+LoRA 在多个腹部器官和CT/MRI模态上达到与专用模型相当甚至更优的性能，显著缓解了标注瓶颈和训练开销。

## 2. 论文提出的方法论：核心思想、关键技术细节、公式或算法流程

- **核心思想**：不提出新网络架构，而是研究“现成基础模型 + 参数高效微调”在极少量标注下的行为。利用 MedSAM3 的大规模预训练知识，通过 LoRA 仅更新极少参数，快速适配特定器官和模态。
- **技术细节**：
  - 以 MedSAM3 为冻结主干，使用 LoRA 更新其视觉编码器、文本编码器、几何编码器、DETR 编码/解码器和掩码解码器，覆盖全模型深度。
  - 器官名称作为文本提示，模型输出对应结构的二值掩码。
  - 训练损失为 **Dice + Focal loss**，优化器为 AdamW，含权重衰减、混合精度、梯度裁剪、线性 warmup + 余弦衰减。
  - LoRA 原理：对权重矩阵 \(W \in \mathbb{R}^{m \times n}\)，学习低秩更新 \(W' = W + BA\)，其中 \(B \in \mathbb{R}^{m \times r}\), \(A \in \mathbb{R}^{r \times n}\)，\(r \ll \min(m,n)\)，仅训练 A 和 B。
- **固定训练协议**（避免验证集调参）：
  - Phase 1（开发阶段）：在肾脏任务上比较 LoRA rank \(r \in \{4,8,16\}\) 和训练轮数，选择 rank=16，30轮收敛且无过拟合。
  - Phase 2（最终评估）：固定 rank=16、30轮，不进行验证集早停或选模型，直接使用第30轮 checkpoint 测试，适用于所有器官和模态。

## 3. 实验设计：数据集、基准与对比方法

- **数据集**：
  - **训练/开发集**：TotalSegmentator 的 CT 和 MRI 数据，分别选取 1、2、5、10 例作为每器官每模态的 few-shot 训练集；额外预留 30 例CT、30 例 MRI 作为验证集（仅用于观察训练行为，不参与选模型）。
  - **腹部测试基准**：AMOS22（独立跨数据集测试集，完全与训练/验证分离）。
  - **心脏跨中心测试**：WHS 数据集（Whole Heart Segmentation），CT 模型用中心A的10例训练，测中心B的20例；MRI 模型用中心E的10例训练，测中心C和D的20例，评估左心室(LV)和右心室(RV)。
- **实验场景**：
  - 五种腹部器官：肝脏、肾脏、脾脏、胆囊、胰腺。
  - 两种模态：CT 和 MRI。
  - 四个标注预算：1、2、5、10例。
  - 额外扩展场景：心脏分割（LV/RV），验证泛化性。
- **对比方法**：
  - 零样本基础模型：SAM3、MedSAM3。
  - 专用监督系统：TotalSegmentator、MRSegmentator（训练数据超过1200例）。
  - 强监督基线：nnU-Net，使用 10、50、100 和完整训练数据（完整数据量因器官而异，如 CT 肝脏746例、肾脏652例等；MRI 肝脏193例、肾脏175例等），以及 CARE-WHS 2025 挑战赛最佳/中位模型（86例标注）。
- **评估指标**：3D Dice 相似系数；10-shot 实验重复三次报告均值±标准差。

## 4. 资源与算力

- **硬件**：单个 NVIDIA A40 GPU（48 GB VRAM）。
- **训练时长**：
  - MedSAM3+LoRA：每个器官约 **3–5 小时**（30轮），比 nnU-Net 快约 2–3 倍。
  - nnU-Net：每个器官需 **8.5–11 GPU-hours**，且不管标注预算多少都训练500轮，低数据场景下无计算优势。
- **参数量**：LoRA 仅更新 **2.15%** 的 MedSAM3 参数（约 1850 万 / 8.59 亿）。

## 5. 实验数量与充分性分析

- **实验数量**：
  - 主实验：5器官 × 2模态 × 4个标注预算（1/2/5/10），每组训练模型并在AMOS22测试，共40组；其中10-shot 每组重复3次。
  - 开发阶段消融：肾脏任务上3种 LoRA rank（4/8/16），观察训练曲线。
  - nnU-Net 对比：2模态 × 5器官 × 4个预算（10/50/100/完整），共40组。
  - 额外心臓实验：CT和MRI各2个目标结构（LV/RV），10-shot训练，跨中心测试。
- **充分性评价**：
  - **优点**：覆盖多器官、双模态和多种标注预算，且10-shot有重复实验和标准差，统计可信度较好；使用独立测试集，避免过拟合。
  - **不足**：器官仅限腹部5个+心脏2个；未与其它LoRA适配方法（如Medical SAM Adapter）直接比较；nnU-Net的训练数据采样方式为固定随机子集，未说明多个种子；胆囊、胰腺等低对比度器官虽有提升但Dice仍较低（0.6–0.7），临床可用性存在争议；未讨论“选择哪10例”对结果的影响。

## 6. 论文的主要结论与发现

- 性能随标注量增加而提升，**1→5例增益最大，5→10例趋于饱和**（CT平均Dice提升10.6% vs 4.4%，MRI 24.2% vs 3.5%），10例是实用工作点。
- **仅10例标注**即可在肝脏、肾脏、脾脏上与 MRSegmentator 差距在 5–10% 内，但标注量减少100倍以上（如CT肝脏0.928 vs 0.959，MRI肝脏0.927 vs 0.956）。
- **胆囊分割是最大突破**：专用工具在AMOS22上几乎完全失败（Dice ≤ 0.0004），而 MedSAM3+LoRA 仅用10例就达到 CT Dice 0.684、MRI Dice 0.633。
- 胰腺（CT）达到 0.711，超过 TotalSegmentator（0.624），但仍低于 MRSegmentator。
- **跨中心心脏分割**：CT LV Dice 0.909 超过 TotalSegmentator（0.896），证明方法可扩展到专用系统覆盖之外的任务（MRI心脏）。
- 零样本 MedSAM3 和 SAM3 表现差，说明没有适配就不可用。
- 训练协议固定、无需验证集调参，进一步降低标注负担；训练速度比 nnU-Net 快 2–3 倍，参数更新仅2.15%。

## 7. 优点

- **系统性的标注效率研究**：首次在“病例级”标注预算（1/2/5/10）下，对多器官、双模态基础模型适配进行大规模实证，填补了以往固定few-shot设置或单模态研究的空白。
- **无需验证集的设计**：固定训练轮数和rank，避免用验证集选择模型，符合实际低标注场景，增加实用价值。
- **发现意外结果**：胆囊分割中专用工具失效而本方法有效，揭示了基础模型+LoRA在特定结构上的鲁棒性。
- **跨中心泛化验证**：用WHS数据集测试心脏分割，证明了方法超出训练分布的能力。
- **计算效率透明**：给出明确的GPU时长、参数量，与nnU-Net对比，说明实际可行性。
- **实验设计严谨**：训练/验证/测试完全分离，10-shot重复三次报告均值±标准差，对比了多个强基线。

## 8. 不足与局限

- **器官覆盖有限**：仅腹部5个器官+心脏2个，未涵盖更多结构、肿瘤、病变区域等；胆囊和胰腺Dice仍在0.6–0.7区间，对临床精度要求高的场景可能不够。
- **未与其他PEFT方法比较**：只测了LoRA，未比较Adapter、Prompt-tuning等，无法说明LoRA是唯一或最优选择。
- **训练数据来源单一**：训练集来自TotalSegmentator，与AMOS测试集存在域偏移；虽然这证明了泛化性，但未分析不同标注来源或标注差异的影响。
- **标注案例选择策略**：未讨论随机选择10例 vs 主动选择最具代表性病例的影响，实际应用中案例选择可能显著影响性能。
- **对比公平性瑕疵**：nnU-Net在低预算下表现差，但其完整数据训练远超10例，且文中未报告nnU-Net在多个随机子采样下的方差；CARE-WHS比较中，best模型用了86例，而非同样10例，对比不够对齐。
- **未见统计显著性检验**：除了均值和标准差，没有配对检验或置信区间，部分差异（如CT vs MRI-trained）可能不是显著的。
- **计算环境单一**：仅用A40 GPU，未展示多卡或不同硬件下的可扩展性。
- **临床应用局限**：论文只测了Dice，未评估表面距离、分割体积误差等临床相关指标，也未讨论分割结果的后续临床影响。

（完）
